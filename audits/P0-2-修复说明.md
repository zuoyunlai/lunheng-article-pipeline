# P0-2 修复说明：pipeline-readme.md T2 派发话术残留

## 问题描述

**位置**：`references/pipeline-readme.md` 第 165-195 行（T2 数据检索员派发话术段）

**问题**：该段包含了 v2.1.8 之前的"案例检索并行化"旧内容（第 7-13 点），与 v2.1.8 设计冲突：

```markdown
**v2.1.8 案例检索并行化（教训 #56 + #58，T2 不再兼带案例）**：
7. **任何量级必 spawn T6 案例检索员**（含 0 条场景走空卡协议），与 T1∥T2 三方真并行，互不干涉
8. **cases = 0 场景**：T6 prompt 显式声明「无需案例」，输出空案例卡 + 显式声明「本次无案例需求，N 条案例卡」，不阻塞主流程
9. **cases ≥ 1 场景**：T6 按主题相关性筛选产出 [C01]–[Cxx]，每条 ≥2 个独立来源 + 时间锚点 + 多方立场并列
10. **T2 不再兼带案例**（v2.1.8 起）：案例检索职责完全移交 T6，T2 prompt 只输出 [Dxx] 数据卡

[强相关性铁律 · 2026-08-13 教训 #34]
9. **每条材料必答「它支撑哪个论点」**——卡片「与本文的关联」字段必填，答不出不收
10. **数量封顶 [Dxx] 30-50 + [Cxx] 5-8**，全套 50-70 条封顶
11. **反向淘汰自查**（交付前必走）：逐条问「删掉它哪条论点会塌」，无影响→砍；案例卡特别查「是示例还是证据？是示例就降级为正文引用不进案例卡」

[论据类型自标 · v2.3 新增，教训 #35]
12. **写手规则**：写任何「结构段落」（非「引用段落」）时，在段落末尾自标论据来源类型：...
13. **审计员必查项 G6**：结构段落未自标论据类型 → P1（读者无法判断可信度）
```

**问题根源**：
1. 第 7-10 点是"案例检索并行化"的**主控职责**，不是 T2 派发话术内容
2. 第 11 点"案例卡特别查"与 T2 铁律"T2 不产案例卡"矛盾
3. 第 12-13 点"论据类型自标"是**写手/审计员职责**，不是 T2 职责

## 修复方案

**删除**：第 7-13 点全部删除（行 188-202）

**保留**：
- T2 铁律段（行 184-187）
- [强相关性铁律] 8-10 点（重新编号为 8-10）

**修复后应为**：

```markdown
**⚠️ T2 铁律（v2.2.12 强化以回应 ClawHub scanner finding #3）**：
**T2 只输出 [Dxx] 数据卡，【不产出案例卡 [Cxx]】。案例卡职责完全由 T6 案例检索员承担**（v2.1.8 起）。

- 不要根据"案例"或"事件"检索产出 [Cxx] 卡片，即使检索结果看起来是事件——交给 T6 处理
- 争议案例（事件中带数字）：只产 [Dxx]（数据维度），事件部分不补
- 接到产 [Cxx] 的指令 → 拒绝并标 「本任务 T2 不产案例卡，由 T6 接手」

[强相关性铁律 · 2026-08-13 教训 #34]
8. **每条材料必答「它支撑哪个论点」**——卡片「与本文的关联」字段必填，答不出不收
9. **数量封顶 [Dxx] 30-50**，宁缺毋滥
10. **反向淘汰自查**（交付前必走）：逐条问「删掉它哪条论点会塌」，无影响→砍
```

## 手动修复步骤

由于 OpenClaw edit 工具存在 JSON 序列化问题，建议主人手动修复：

```bash
cd ~/.openclaw/workspace/skills/lunheng-article-pipeline/references/
# 备份
cp pipeline-readme.md pipeline-readme.md.bak

# 用编辑器打开 pipeline-readme.md
# 找到 "### 数据检索员（并行②）" 段（约第 167 行）
# 删除行 188-202（从 "**v2.1.8 案例检索并行化" 到 "**审计员必查项 G6**" 为止）
# 保留行 184-187（T2 铁律段）
# 重新编号 [强相关性铁律] 为 8-10 点

# 验证
grep -A 20 "T2 铁律" pipeline-readme.md
```

## 验证标准

修复完成后应满足：
- ✅ T2 派发话术不包含"spawn T6"等主控职责
- ✅ T2 派发话术不包含"案例卡特别查"等与 T2 铁律矛盾的内容
- ✅ T2 派发话术不包含"写手规则"/"审计员必查项"等其他角色职责
- ✅ [强相关性铁律] 编号连续（8-10）

## 影响范围

**ClawHub scanner finding #3**：T2 数据检索员职责混淆（medium severity）—— 修复后应降为 CLEAN

## 相关教训

- 教训 #56：三检索员独立并行架构
- 教训 #58：T1 不知道 T6 存在 / T3 触发条件
- 教训 #60：全文 grep 真源层失同步
