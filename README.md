> 版本：v2.12.63（自动同步 2026-09-19）

> 🛠️ **根级工具说明**：根目录 `Makefile` / `pyproject.toml` / `requirements.txt` / `tests/` / `.github/` 是**开发者工具**（主控自审门、pytest、净化链、CI）。ClawHub 净化包（`$OUTPUTS_ROOT/clawhub-release/<v>/`）不包含这些，只携带 `SKILL.md` + `LICENSE` + `references/` + `QUICKSTART.md`。**发布者无需为这些根级文件设置路径期望。**

> 🔐 **权限设计取舍（对审计方/扫描器明示）**：论衡是**纯 skill**，不附带任何宿主配置项。子代理角色白名单是**声明式调用边界**（描述本 skill 自身不越权，加载器不执行）。子代理实际工具面由 OpenClaw 平台负责；论衡的运行时处置 = 记录 + 披露 + **主人裁决**（敏感题材可要求主控接管该 worker）+ 铁律「**工具面超限 ≠ 调用许可**」。论衡不判断宿主配置状态，也不要求宿主做任何额外设置。**这是有意的设计取舍，不是未修复的缺陷。**

> 🌐 **语言政策**：产出语言由 Phase 0「目标语言」字段**显式选择**（中文 / English / 中英混 / 其他，**不设默认**），全流程以该字段为准；中文特化按**目标语言客观适用**——含中文时 **G14 中文 AI 痕迹闸必跑**（v2.12.40 起不再是可选项），纯外语时记 `n/a`（客观不适用，非「关闭」）；GB/T 7714-2015 引用规范为可选能力。二者均不构成使用者语种限制。

![GitHub Actions](https://img.shields.io/github/actions/workflow/status/zuoyunlai/lunheng-article-pipeline/quality.yml?branch=main&label=tests)
![Code Quality](https://img.shields.io/badge/code%20quality-87%2F100-brightgreen)
![License](https://img.shields.io/badge/license-MIT-blue)

# 论衡（lunheng-article-pipeline）— 中文学术 / 深度长文多 Agent 流水线

> **中文学术/深度长文专用**。多 Agent 编排 + 三角验证（文献/数据/案例）+ M 门形式合规 + 实战反馈驱动升级。5000+ 字强推。

**v2.12.63**（2026-09-19，当前版本；以 [`CHANGELOG.md`](CHANGELOG.md) 首节为准）**v2.12.63 = 发布面泄漏热修（维护者内档停止出厂 + 净化误删修复 + `_shared` 白名单准入）**：① **P0 · 维护者工程内档一直随包出厂**：`references/_shared/论衡仓库内教训.md`（`#R001` 编号空间，含 CI run id / commit hash / pytest 命令 / 「净化链」等维护者叙事）自 v2.12.42 引入后**每次构建都随包出厂**，且同时绕过三道门（不在排除清单 / 不匹配只认 `教训 #N` 的残留模式 / 是 git 跟踪文件故反向断言不拦）⇒ `rsync --exclude` + `cp` 分支 `rm -f` 双处排除，`FINAL_PATTERNS` 与 4e 自检清单各增 `#R\d{3}` 系列模式；② **根因治理（类别级）**：新增 **§2b' `_shared/` 目录准入清单门** —— `references/_shared/` 改**白名单准入**，包内文件集必须精确等于已登记的 **31** 项，否则**构建失败**并双向打印差异（「新文件默认入包」→「新文件默认被拦」）；③ **P1 · 净化规则误删**：规则 3h-5 的整行正则曾把 `00-主控-扩展职责.md` 主控必读清单的**层 1 整行**删掉（层号 0→2 跳档、`入口必读（启动清单 1-2 步）` 消失），现改「只删该 mention」并加**行级反向断言**（该串在净化后消失即构建失败）；④ 配套 **6 条单测**（准入门 4 + 行级断言 2）。此前 **v2.12.62**（2026-09-19）**v2.12.62 = 第四批审计整改（分层外移 + 体量软棘轮 + 编号去副本 + B12 接线）**：① **「必读文件体量软棘轮」新增（自审门 Y）**：审计 P1-3 指出「1.72M 字符仓库里唯一有硬棘轮的，恰恰是余量最紧的 SKILL.md」⇒ 给三个「每次必读」大文件（扩展职责卡 / `M-Gate-Algorithm.md` / `phase-order.yaml`）设**只许降**的 ⚠️ 提示级上限，并新增 SKILL.md **余量告警**（余量 < 300 字符即响）；同批把 SKILL.md 由 9827 压到 **9680**（余量 173 → **320**），使告警不靠「调阈值消音」而是靠**真的腾出空间**转绿；② **审计 P2-6 二次分层**：`00-主控-扩展职责.md` §二十 反哺报告处理 → 独立文件 [`_shared/反哺报告处理.md`](references/_shared/反哺报告处理.md)，§二十三 编排循环防空转（原与 `执行韧化协议-exec.md`／`-design.md` **三处承载**）→ 收敛为**一处真源**，两节均留编号指针段；**同步迁移** `build-clawhub-release.sh` 的净化规则（原文按节替换，内容外移后模式永不匹配 = 「规则静默空转」老坑）并**加硬断言**（替换未发生即构建失败）；本卡净减约 **12.6KB**；③ **审计 P2-7 编号去副本**：教训编号原在 **5 处**联动（索引 §一/§二/§三 + 快照 + 头部散文），v2.12.58 已实测一次 off-by-one ⇒ 索引三处硬编码**全部改为派生指针**（编号只在 `lessons-max.snapshot` 写一次），门 H 判据改为双判据：**快照单一真源可解析** + **索引不得出现硬编码最大编号**（出现即红）——把漂移面从「人工记得改 5 处」压成「机器拒绝第 2 处副本」；④ **审计 B12 接线（诚实边界）**：`sessions_spawn` 返回 accepted 但子会话不存在属**运行期**故障，零 exec 下**构建期无法机械校验** ⇒ 不假装有门：改为锁「协议不许从载体里静默消失」+ 新增 `status` 运行期留痕字段 `spawn_landing: ok|missing|retry:<N>`（主人可事后核验）+ 在真源与模板**双处写明「机械兜底边界」**（flow-check 规则 34）。此前 **v2.12.61**（2026-09-19）**v2.12.61 = 人环决策词表三处同源 + 卡片效力边界显式声明（主人 2026-09-19 问「checkpoint-card 有没有实质作用」引出）**：① **Phase 0 决策词表冲突修复**：`status-template` 原写 `decision=<start|补充信息|暂停|拒绝>`，而真源 `phase0_definition.decisions` = `approved`/`revision_requested`/`restart_phase` —— **交集为空**，`start`/`暂停`/`拒绝` 在全仓真源**零命中** ⇒ 主人选「暂停」时主控写下的字面值无对应枚举，人在环硬门必然判「未记录」；根因 = Phase 0 把「是否启动」（流水线**外**前置门）与「进线后决策」混成一张选项表。现口径：进线二态 `approved`/`revision_requested`，未进线出口记独立字段 `pre_pipeline_exit=<pause|reject>`；真源同步移除自指不可达的 `restart_phase`（其行内注释自曝系 v2.12.28「与其余人工节点结构对齐」复制而来）；② **flow-check 规则 33（新）**：`status-template` 四行 `decision=<...>` 字面值必须 ⊆ 对应节点 yaml `decisions`（四节点逐一点名），且卡片四段各须带「枚举真源」指针 —— 卡片自称「选项固定，不可自由发挥」，原先却只有 Phase 2.5 段有指针，其余三段无真源可对；③ **卡片效力边界显式声明**：机械门只守**内容完整性**，**不守运行期是否真的按它呈现过**（全仓零门校验 Checkpoint Card 呈现），后者属行为层，与 B12 同类，靠纪律 + 目视；④ **配套 4 条单测**（status⊆yaml 正向 + 四指针正向 + 自创词表反向注入 + 缺指针反向注入，全部整仓副本注入、真源零写入）。此前 **v2.12.60**（2026-09-19）**v2.12.60 = 图件嵌入机械锁 + 节点 kind 必填门（主人 2026-09-19 裁定「图件如果存在要机械嵌入」）**：① **M-11 图件嵌入锁**：`final/定稿.md` 的图位必须写成可渲染的 `![图N：标题](图件/图N_标题.svg)`，**双计数**均须 = 拍板 N（① `[图N：` 占位计数 + ② 嵌入行计数；嵌入式本身包含 `[图N：标题]` ⇒ 两条互为交叉校验）；嵌入是**论衡职责**（`final_assembly` 主控 `write`，零 exec）——修正了上一版「纯文本占位版 + 主人手动嵌入」的口径；PNG 转换仍归主人；② **flow-check 规则 20b**：三处载体（deliverables / 08-终检 / T8 dispatch）必须同时含「嵌入式图位规范」+「嵌入计数」口径，否则构建期红；③ **flow-check 规则 32（新发现的门缺口）**：`kind` 是规则 4/5 与 `owner_nodes` 归属的**分派键**，而本仓实测「改 final_assembly 时误删 `kind:` 行，全仓 flow-check **RC=0 零报错**」——即 kind 缺失 = 该节点对所有按 kind 分派的检查**静默隐身**（#427 同族）。新增规则：23 个节点必须有 `kind` 且在已知集合内；④ **配套 4 条单测**（三载体正向 + 嵌入锁反向注入 + kind 正向 + kind 反向注入，全部副本注入、真源零写入）；⑤ **补打 4 个缺失 tag**（v2.12.51 / v2.12.52 / v2.12.54 / v2.12.55 —— 均核实 release 提交为 HEAD 祖先，且 `git show <tag>:SKILL.md` 的版本号与 tag 名逐一比对一致）。此前 **v2.12.59**（2026-09-19）**v2.12.59 = 2026-09-19 全面审计第一批整改（悬空指针 + 门 U 扩扫描面 + 分层指针 + 定稿图件口径）**：① **补上悬空文档指针**：`references/_shared/host-verify-recipe.md` 被 2 处**活文档**（均在净化包可见面）引用却**从未存在**，且指向的正是主人手动补算 sha256 的那个动作 ⇒ 新建该文件作为「主人 host shell 补算与登记」唯一命令模板真源，并登记进三处版本戳载体清单；② **门 U 扩第三类扫描面**：原判据（markdown 链接 + SKILL.md 裸名）**不覆盖活文档反引号内联路径引用**——这正是 ① 漏检的原因 ⇒ 新增第三类检查（`references/**` 内联引用 268 条逐条解析 + 显式债务白名单），配 `tests/test_link_check.py` 5 条（反向注入必报错 + 防过度拦截 + 真源零写入护栏）；③ **§十六 补分层指针**（概览段 → 真源 §二十一/§二十二/§二十三，消除同主题两处承载的进入歧义）；④ **定稿图件口径显式化**（定稿为纯文本 `[图N：标题]` 占位版，嵌入属交付后排版、由主人手动执行；M-13 第 2 类扩为「图件落地与嵌入」）。此前 **v2.12.58**（2026-09-19）**v2.12.58 = M-Gate 围栏错位修复（教训 #426）+ 自审门 X（X.1-X.4）+ 教训 #424-#426 入库 + 判据全仓扩围抓到第二例同类缺陷**：① **围栏错位修复**：`references/_shared/M-Gate-Algorithm.md` 删 2 个多余围栏（L1048 / L1181），13 个 M 门标题归位、围栏外伪 H1 清零（1250 → 1248 行）；② **自审门 X 新增并扩围**：X.1 单文件围栏偶数 / X.2 **声明式锚点表**（M 门标题 + `pipeline-readme.md` 章节锚点，新增文件只需加一行）/ X.3 围栏外紧跟围栏的伪 H1 / **X.4 全仓 `.md` 围栏偶数（未闭合围栏检测）**，配 5 条变异单测（全部在临时副本注入，不写真源）；③ **教训 #424-#426 入库**：裸 grep/awk 不认围栏 / trigger 脚本 exec 大 JSON 静默不报 / 围栏错位「偶数」必要不充分（需叠加语义锚点块外 + 伪 H1 为零）；④ **扩围的真实收获**：X.1 由单文件扩到全仓后**立即抓到第二例同类缺陷** —— `references/pipeline-readme.md` 未闭合围栏（v2.12.54 全景收敛的删除残留）使文件尾部 57 行被吞进代码块，已修；⑤ 顺手修 README 43 行空行块 + `glossary-full.md` 13 行空行块（同族排版残留）。**此前 **v2.12.57**（2026-09-18）**v2.12.57 = 发布链 changelog 提取器兜底口径 + 教训 #421 入库（索引/快照同批）+ 提取器正向单测**：① **提取器兜底口径**：`extract_changelog()` 主口径（`> **主题：…**` + `### 小节标题`）保留不变，新增**顶层要点标题**兜底（写作风格迁移后提取恒空，fail-closed 的 `exit 3` 曾拦死 v2.12.56 发布；缩进子项不入正文）；② **教训 #421 入库**：压缩/提取类门必须对真源的实际书写格式有**正向样本**，否则门会静默空转 —— 索引最大编号 #420 → **#421**、`lessons-max.snapshot` 同步，排除表 `LUNHENG_LESSON_EXCLUDE` 未变（论衡类不入表）；③ **新增 `tests/test_publish_changelog_extraction.py`（6 条）**：从真脚本抽 `extract_changelog()` 函数真身执行（不复制实现），含「本仓 CHANGELOG 当前版本章节必须提取非空」的真源防线。**纯工具链/文档侧修复，无新增能力、无破坏性行为变更。** 此前 **v2.12.56**（2026-09-18）**v2.12.56 = 批次 C 运行时收尾协议（P-1~P-5）+ Layer 5 删除清理（D-1~D-3）+ M-Gate 规范补全（M-1~M-7）**：① **P-1 收尾协议（硬约束）**：worker 写完交接报告后**以正常最终消息结束回合**（该消息即 completion event）；**作废**「worker 自行 `sessions_yield`」（= 挂起 run 而非完成它）、**禁**把摘要塞进 `acknowledgment`（该字段不从子代理回合发出，实际不送达）；9 张角色卡 + 两份交接报告模板接线，禁用原语统一为 `sessions_yield` / `agents_wait` / `next_check` / `subagents` / `sessions_list` / `sessions_history`；② **P-2 主控兜底唤醒**：spawn 后**必须**安排一次定时自唤醒（= 该角色硬卡阈值 + 缓冲），到点**主动 `read` 核对磁盘产物**，**推进判据以磁盘产物为准**、completion event 仅作加速信号（`automations` 在 `denied` 内，本协议**不授权调用**）；③ **P-3 能力自检真收口**：必须**逐项列出本会话实际可见工具清单**（禁只写「通过」）；④ **P-4/P-5 版本链完整性**：每个 `drafts/初稿-vN.md` 须配同版本号交接报告或 `修订说明-vN.md`；⑤ **D-1~D-3 多格式导出彻底移出 Phase 0**（并入 M-13 建议清单，T8 命令与 `_shared/format-export.md` 同源）；⑥ **M-1~M-7 M 门规范补全**（抽取规则唯一真源 / 判定出口四档 / 未定义 helper 替代口径 / 依赖面补全）。**文档口径与机械门收口，无新增能力、无破坏性行为变更。** 此前 **v2.12.55**（2026-09-18）**v2.12.55 = 批次 B · Layer 2 真源修复（S-2~S-6）**：① **盲审节点禁主控代笔**（`t9_review` 删除通用 fallback，改 `independence_failure_policy`——只能重试 spawn 独立子代理，耗尽即记 `missing_blind_review` + 显式告知主人，**不得产出该节点结论**）；② **同 provider 连续 ≥3 次静默强制主人三选**（换 provider 族 / 换能力档 / 接受同源并披露；**无默认项、必须挂起**）；③ 修订回环轮次与字数上限口径归一（唯一真源 = `_shared/pipeline-overview.md`『修订回环仲裁规则』与任务简报 `body_limit`）；④ Phase 1.5 删首轮不可达的占位触发条件。**文档口径与机械门收口，无新增能力、无破坏性行为变更。** 此前 **v2.12.54**（2026-09-18）**v2.12.54 = Batch A 机械校验 + 表述收敛**（2026-09-18 全流程实测复盘后首批）：① **全景收敛为唯一派生视图**（`_shared/pipeline-overview.md` 重写为 23 节点全表；其余文档删除全景段只留指针，flow-check 规则 24 机械守；实测 11 份含 Phase 序列的文档无一与真源一致）；② 新增 **R-2 节点 id 合法性 / R-3 Done 记账一致性 / R-4 条件字段生产方 / R-5 T8 建议清单四类 / R-6 条件不可判定处置** 五条构建期校验；③ **T9 由 opt-in 改「默认启用 + 主人 opt-out」**并补生产方字段（原 `owner_peer_review_consent` 全仓无生产方 ⇒ T9 整节点静默消失）；④ **Phase 0 不再询问输出格式与是否配图**（格式转换 / SVG→PNG / 封面视觉 / SHA256 登记下行到 T8 终检后的「主人自行操作建议清单」，四类缺一即不合格）；⑤ 修订回环口径归一（不计轮 = Phase 3.5 洞察轮；轮 1 = 3.6/3.7 批判；轮 2 = 4.2 审计）。**文档口径与机械门收口，无新增能力、无破坏性行为变更。** 此前 **v2.12.53**（2026-09-18）**v2.12.53 = v2.12.52 能力边界收口 + 批次 C1 P0 + C2 P1 + C3 P2 一致性整改及教训索引收口**：① **封面与格式转换退出流水线**（`image_generate` 文档侧残留清零：服务级外发 4→3 类、轴 A 3→2 项、同意门删封面行并新增「能力移除登记」防回潮；统一表述为「T8 终检后写入主人自行操作建议清单」）；② **平台责任边界中立化**（「宿主加固 / 加固状态 / 加固配方」措辞全仓清除，四层表 ②/③ 行改「平台职责」，`status.md` 删加固状态记录位）；③ **附带一致性**（删 single-controller 残留、`<2000 字` 改「论衡不适用」、教训引用去第三方署名）。**文档口径收口，无新增能力、无破坏性行为变更。** 此前 **v2.12.51**（2026-09-17）**v2.12.51 = 批次 B 一致性收口（D-3 + D-4）**：① **只读档「报告写盘主体」唯一化**（T6/T7/T9/G14 真源改 `write_authority: owner`——报告正文随交接回传、由主控 `write` 落盘；`permissions.md` 删「两径定义」；flow-check 新增「只读档写权」规则，改回 `executor` 即红）；② **测试反向注入不再写真源**（`tests/test_flow_check.py` 11 处改「整仓副本注入」+ 真源 sha256 硬断言，kill/超时/并行不再污染真源）。此前 **v2.12.50 = 机械门空转止血（D-1 / D-2）**；**v2.12.49 = v2.12.48 后续修订 + 机制修复**：① **人环闸门真源化**（4 个 owner_checkpoint 补 `blocking: true` + `owner_visible: true` + `timeout_fallback`；阶段顺序与阻断关系的唯一真源 = `phase-order.yaml`，挡不住的真源不交主控猜；flow-check 规则 12 机械可验）；② **Phase 5 删除「不答 = 接受」fail-open 节点**（四节点一律 fail-closed，无应答 = 写 status `pending_owner` + 告警挂起，旧口径登记为「已删除的旧语义」防回潮）；③ **`image_generate` 退出 opt-in 入 denied**（封面与图件去留由主人手动操作，论衡 agent 零 exec 原则不变；服务级外发 4→3 类）；④ **零应答分钟数归一为真源**（`owner_timeout_policy.no_answer_minutes: 60`，三处字面重列改为引用）。**机制**: M-1 交付物指纹 / M-3 计数真源 / M-4 G14 严重度 / M-5 P2 量化锚点 / M-6 轮次耗尽出口三选一 / M-7 status 对账机械门 / M-8 审计对象一致 / M-9 48必查加严重度列 / M-10 图件路径归一 / M-11 图位决策门 / M-13 T8 后主人建议清单 / M-14 字数口径单一化（详 [CHANGELOG](CHANGELOG.md) §v2.12.49）。此前 **v2.12.48**（2026-09-16）**v2.12.48 = 纯 skill 定位收口 + C1 口径一致性修订**：① **平台与 skill 责任分离**（OpenClaw 原生支持多 Agent，宿主配置不再是论衡运行前提）；② **权限边界收口**（启动自检只核对自身声明与当前会话工具面，不产生“未加固”项目状态）；③ **worker 失败按节点接管并披露**。此前 **v2.12.47 = v2.12.46 扫描整改落地 + 仓库冗余清理 + changelog 分层**：① **扫描整改落地**（协调器工具面对齐 frontmatter 16 项 / 脱敏升为条件强制 / OpenAlex·Crossref 数据流声明 / 「fallback」术语消歧 + T9 禁主控 fallback / 会话管理原语作用域收紧 / 引用政策「不静默 fallback」/ T9 触发与交接口径）；② **仓库冗余清理**（删零 exec 下永久不可达的 `m_exist_1_diff.sh`；`lessons-max.snapshot` 加入发布包排除清单；清仓库外过期产物与工作区缓存）；③ **changelog 分层**（主文件只保留最近 5 期，历史迁入 [`CHANGELOG-archive.md`](CHANGELOG-archive.md)，校验脚本读两份 + 新增主文件容量门）。此前 **v2.12.46**（2026-09-15）**v2.12.46 = 架构定案 + Phase 编号真源化**：① **多 Agent 九角色为唯一标准架构**（删除与多 Agent 并列的「单主控模式」；worker 失败/不可用 ⇒ **只接管该节点**并披露 L1 独立性影响，不整轮降级、不跳质量门）；② **Phase 编号成为第一类真源字段**（`phase` / `phase_seq` / 顶层 `phase_order` 编号表 + flow-check 规则 11：完整性/唯一性/与编号表一致/序号不回退）；③ **角色产物写入边界**（角色写自己的产物，**T5 = 正式写手落盘职责保留**；`status.md` / `current_draft.md` / `final/` 由主控独占）；④ **人在环机械门**（全路径必经 4 个 owner checkpoint + 每个 checkpoint 必须有 decisions 枚举）。此前 **v2.12.45**（2026-09-15，一致性 pass）：消 T09 + SDI-4 两条 [unexpected] + SDI/SDI-2/SQP-2 配套。此前 **v2.12.44 = ClawHub 安全审计复核整改**：① **未加固主机改 fail-closed**（宿主未配 `tools.subagents.tools.deny` / `maxSpawnDepth` ⇒ **Phase 0 须主人显式确认才可多 Agent，未确认即单主控**；判据 = 子代理启动自检回执）；② **心跳写盘同意语去矛盾**（删「运行即视为接受」隐含同意 → 显式同意门，缺确认不写 `.tmp/`）；③ **期刊 / 发布渠道建议写入 manifest `description`** + T9 能力声明段；④ 配套**宿主加固落地**（子代理工具硬拒 44 项 + `maxSpawnDepth: 1`，hot reload）。AE4×2 / E1×2 经官方报告包判定 expected，不修。此前 **v2.12.43**（2026-09-15，实战复盘采 8 + 安全审计采 6）：① **投稿域 vs 工程域硬分离**（投稿版禁入清单 + `final_assembly` 只产投稿版 + T8 A1 判据扩充）；② **display-cap 截断应对**（磁盘产物优先 / 按角色会话捞取 / 标 `[主控 fallback 产物]`）；③ **口径收紧三项**（「零 exec」只指执行类工具 + **主控编排面显式申明** + **会话可见性收口**）；④ **产品中性披露**（默认不点名流水线；指名 = Phase 0 opt-in）；⑤ 反方段落位铁律 / 跨卡多口径三字段 / `audit_revision` 结构化任务书 / 测试模式三处校验 / 字数阈值示例。**附带修复 2 项既有红门**（M-Form-7 白名单回补 `## 致谢` / 仓库内教训文件补语言政策声明）。此前 **v2.12.42**（2026-09-15，扫描收尾九项）**v2.12.39 = 拆分发布（主人裁决）**：① T05 治本路径 **B 档**（未加固时披露 + 默认降级单主控，不拒跑）；② **移除 `view_image`**（denied 39→40；**数据图表 SVG 生成能力保留**）；③ **遥测分级**（`status.md` 保留 / `final/交付说明` 剥离宿主余额·模型可用性·session id）；另含 4 类口径修缺（denied 计数断言 / permissions 13→15 项 / `## 辅证文献` 归并口径 / flow-check 脆弱路径）。**20 项 P0-P2 推迟 v2.12.40**（一次发版只承载一类变更）。**v2.12.38**（2026-09-14）**v2.12.38 = 三源整合修订**（denied 27→39 + coordinator 6→9 + 通配白名单逐项化 / 超时六情形状态机 / 加固配方0 `tools.subagents.tools.deny` / 扫描措辞治理 / 实战报告 9 采纳 8 驳回）；**v2.12.37**（2026-09-13）**v2.12.37 = 第三方全量审计整改**（P0×4 + P1×5 + P2×4：官方校验失败修复 + 门 W / Phase 3.7 刷新 / final_assembly 节点 / flow-check 三新规则 / 权限口径订正）；**v2.12.36**（2026-09-13）**ClawHub 安全审计统一修订（v2.12.35 报告 21 条 → A–G 七项）**：语言政策横幅去「默认中文」（改 Phase 0 显式选择）、G14 改 Phase 0 显式勾选（不再按语言/体裁自动触发）、目标语言确认去身份声明、`phase-order.yaml` 两处 Vague Triggers 处置、description 披露外发与周期性写盘、**OpenAlex/Crossref 改默认关闭（行为取舍）**、Ae4（Unicode）判定非缺陷不修。**合规整改 + 一处行为取舍。** 此前 v2.12.35 = 两处口径重构（主人定案）：① 删除「记忆辅助」（三个 opt-in 记忆读工具；服务级外发 5→4 类；工具级 opt-in 4→1）；② 字数口径统一为「仅正文，不含文末附录」（废除双口径与「含/不含来源附录二选一」）。**口径收敛 + 能力移除，无新增功能。** 此前 v2.12.34 = 官方规范审计整改 + 独立复查修复：`SKILL.md` 11,335→9,996 字符（棘轮 11,370→10,000）；`description` 876→123 字节；门 H 反向差集改仓库内快照判据（hermetic）；`outputs/` 迁出技能根（37MB→2.8MB）；🔴 门 G 假绿灯阻断项修复；新增快照更新纪律。**规范合规 + 缺陷修复，无新功能、无破坏性行为变更。** 此前 v2.12.33 = 审计页复核修复（AIG `T05` 消歧义（主人裁决 **A 档「保持行为、只消歧义」** —— 启动自检重排「实际调用＝阻断级」在前 + 铁律「**工具面超限 ≠ 获得任何调用许可**」+ 主控裁决只放行「继续干活」不放行越权调用；**行为一字不改**）+ **T7.5 伪代码依赖倒置**修正（不再 check T8 产物 `final/M-Gate-Report-*.json`，改读本轮章节级 M 门 + `status.md`）+ `SKILL.md` 旧口径残留对齐 + **AE1（HIGH，必读清单三路径拆独立行）** + 只读档改「**两径定义**」（上游只读 / 自有报告可写，四处一致）。**纯缺陷修复，无新功能、无破坏性变更。** 此前 v2.12.32 = 无人值守实测修订（P0×2 + P1×3 + P2×2）+ 官方规范比对修复（P0×3 + P1×4）；v2.12.31 = ClawHub 扫描复核修复（AE1 HIGH + SQP-2 / SQP-1 两项 MEDIUM）+ 门 U 扩第二类检查。沿革见 CHANGELOG 对应章节。[MIT License](LICENSE)。

论衡把一篇深度长文 / 论文的生产拆成 **10 张角色卡 + 6 个阶段**，由主控用 OpenClaw `sessions_spawn` 编排三方真并行子代理（T1∥T2∥T3 互不干涉），产出有**证据底座、反方论证、独立审计、人工核验节点**的交付物。T8 终检有独立角色卡，**T9 同行评审**（6 维度评分 + 期刊匹配）。定位：学术论文 / 商业评论 / 行业分析 / 公众号深度长文通用（非 locale 缺陷）。经验证：~9500 字深度文全流程约 2 小时。

---

## 🌟 快速开始（5 分钟）

**第一次使用？** 读 [`QUICKSTART.md`](QUICKSTART.md)：

1. **写任务简报**（2 分钟）：新建 `<项目名>/01-任务简报.md`，含研究问题/类型/篇幅/引用格式 + Phase 0 同意关卡
2. **主控自动派发**：T1∥T2∥T3 并行检索 → T4 分析 → T5 写作 → T6 批判 → T7 审计 → T9 审稿 → T8 主控亲终检
3. **主人在 4 个节点介入**：Phase 0（定题）/ 2.5（大纲）/ 3.5（洞察）/ 5（终稿验收）——T6/G14 与 T9 均是内部动作，T9 只提供建议；T8 技术终检不能代替 Phase 5 主人验收

**预计项目时间**：轻量档（**2000-3000 字**，真源 = `字数判定表.md`）30-60 分钟 / 中段档 1-2 小时 / 重量档（≥5000 字）2-4 小时。

---

## ⚠️ 外发项与能力边界声明

**流水线默认会调用以下外部服务**（使用前需主人同意——Phase 0 同意关卡）。

> ⚠️ **本表为导读**；**外发类别唯一真源 = [`references/_shared/external-services.md`](references/_shared/external-services.md) 逐类表**。下表若与真源不一致，以真源为准（一条款一真源）；**类别数只在真源声明，本导读不复述数字**（v2.12.64：原写「4 类」与真源实际类别数不符）。另：**「大模型推理全文」属「数据形态」轴**（非服务类别轴），单独一行列出。

| 操作 | 第三方服务商 | 发送内容 |
|------|------------|---------|
| `web_search` | OpenClaw 内置 web provider | 检索关键词 |
| 学术元数据检索（**默认关闭，Phase 0 勾选**） | **OpenAlex / Crossref**（只读公开 API，无需 Key） | 检索关键词 |
| `tavily_search` / `tavily_extract` | **Tavily AI** | 研究主题/URL |
| **数据图表 SVG** | **本地内置**（主控 `write` 手写 SVG，零外发） | — |
| 大模型推理 | 当前模型 provider | 文献/数据/案例/草稿/大纲全文 |

**主控 agent 能力边界**（**不**会做的事）：
- ❌ 不调用 `exec` / `process` / `code_execution` 等**41 项 denied 特权工具**（清单真源 = `SKILL.md` frontmatter `metadata.tools.denied`）——⚠️ **声明式**，须宿主配置才生效
- ❌ 不读取运行时内部路径（`~/.openclaw/agents/<agent>/sessions/*.trajectory.jsonl` 等）
- ❌ 不直接计算 sha256（需要时由主人在 host shell 手动跑后回填）
- ❌ 不主动采集一手数据（实验/调查/访谈）—— 主人投喂后使用

**Phase 0 强同意关卡**：所有外发项在流水线启动前需主人明示同意（4 选 1：全部同意 / 脱敏+SVG+本地 Ollama / 部分同意 / 全部拒绝）。**未同意前不可跳到 Phase 1**。

完整说明 + 错误信息友好化（12 类常见错误）：详见 [`SKILL.md`](SKILL.md) + [`references/errors.md`](references/errors.md)。

---

## 核心特点

### 三不原则（质量底线）
- **不编造** — 引用带 URL/DOI + 数据带来源年份 + 审计分级抽验
- **不堆砌** — 强相关性铁律（每条材料必答「它支撑哪个论点」+ 反向淘汰自查）
- **不重复** — 先行者检索 + 差异点声明 + 原创性审计

### 九角色流水线

```
主控（Coordinator）—— 定题/拆解/派发/T8 终检亲完成/状态机
  │
  ├─ T1 文献检索员（Literature Scout）── 文献卡 [Lxx]      ┐
  ├─ T2 数据检索员（Data Scout）──────── 数据卡 [Dxx]      │ 三方真并行
  ├─ T3 案例检索员（Case Scout）────── 案例卡 [Cxx]      ┘ 互不干涉
  │
  ├─ T4 分析员（Analyst）────────── 分析大纲（论证主线 + 反方论证 + 建议图表）
  ├─ T5 写手（Writer）────────────── 初稿（AI 去味 10 项）
  ├─ T6 批判伙伴（Critical Companion）── 批判报告（C1-C7 反方攻击 v2）
  ├─ T7 审计员（Auditor）────────── 审计报告（G0-G14 = 17 项（含 G0.5 / G2.5））
  └─ T9 同行评审（Peer Reviewer）── 审稿报告（6 维度评分 + 期刊匹配）
              ↓
         T8 终检 = 独立角色卡
```

**T9 同行评审**：论文投稿前的「预演审稿人」，6 维度（原创性/方法论/证据强度/论证结构/写作质量/引文规范，总分 30）→ accept / minor / major / reject 建议。**期刊匹配助手**：基于 T9 评分 + 主题关键词，从 24 中文 CSSCI/北大核心 + 12 英文 SSCI 数据库输出 Top 3 + 综合匹配度（主题契合 50% + 风格匹配 30% + T9 评分 20%）。**行业分析/学术论文默认开启**，公众号默认关闭（主人可选）。详见 [`09-审稿-peer-reviewer.md`](references/agents/09-审稿-peer-reviewer.md)。

**G14 中文 AI 痕迹深度检测闸**：**Phase 4.4 前置**（`g14_style_gate`）—— **定稿前最后一道闸，全流程只审一次、不再复检**；适用性由 Phase 0「目标语言」**客观决定**（含中文必跑 / 纯外语记 `n/a`）。8 类判定（真源 = [`14-中文AI痕迹-gate.md`](references/gates/14-中文AI痕迹-gate.md) §二，**本节不重列**），LLM 推理判定（**零 exec**）。0-2 类 Pass / 3-4 类 Warning（主控呈报 3 选 1，不自动修订）/ 5+ 类 Fail → T5 最后一次风格层修订（`t5_style_revision`）。

### 三角验证（证据底座）

| 卡片 | 给的是 | 适用论点 |
|------|--------|---------|
| 文献卡 [Lxx] | 理论 / 学术依据 | 「这事有没有人研究过」 |
| 数据卡 [Dxx] | 量级 / 趋势 | 「这事多大、占比多少」 |
| 案例卡 [Cxx] | 事件结构（who/when/what/各方说法） | 「这事具体怎么发生的」 |

**数据信任级别**（**文字取值，非 emoji**）：
- **已发布公开数据**（最高信任）— 官方统计年鉴 / 学术论文 / 行业协会公开报告
- **主人投喂数据**（中信任）— 一手调研 / 访谈记录 / 田野调查
- **二手转引**（低信任，严格限制）— 必须回溯一次文献 + 顶部标注

> ⚠️ **两轴不混用**（v2.12.64 修复，回应审计 V-9；真源 [`_shared/glossary-full.md`](references/_shared/glossary-full.md) §三）：**信任级别 = 上述文字取值**（数据卡字段）；**emoji 🟢🟡🔴 专用于「时效评级」**（🟢当前 / 🟡1-3 年 / 🔴3 年以上）。

### 人在环四节点
- **Phase 0 定题** · **Phase 2.5 大纲** · **Phase 3.5 洞察补充** · **Phase 5 终稿验收**
- 四节点均须向主人呈现材料并记录明确决策；Phase 3.5 可记录「无补充」，不可静默跳过。T8 技术终检先完成，不能代替 Phase 5 主人验收。

### 三层防御体系

| 层 | 性质 | 用途 |
| --- | --- | --- |
| **M 门**（形式合规）| LLM 结构化自评（**零 exec**；规则硬性、非机器强制）| 引用/数据/案例的形式完整性（M-Form 8 项 + M-Exist 3 项 + M-Integrity 2 项 = 13 项）|
| **F 模式**（失败模式）| 面向用户的叙事 | F1-F9 失败模式清单（幻觉/格式/数据信任/论证强度）|
| **G 清单**（质量审计）| 面向审计员 | G0-G14 共 17 项（含 G0.5 / G2.5）|

**常规修订 ≤2 轮硬约束**（例外通道须主人拍板）：例外通道触发 → Acknowledged Limitations 模式（未关闭 P0/P1 搬入 `final/局限性.md`，论文正常交付不假装完美）。**轮次映射与各通道判定 → 见 [`references/_shared/pipeline-overview.md`](references/_shared/pipeline-overview.md)『修订回环仲裁规则』（单一真源；本文件不复述轮次数字）**。

完整定义：详见 [`references/_shared/glossary-full.md`](references/_shared/glossary-full.md)。

---

## 全景与阶段顺序（指针）

> 📎 **流水线全景与阶段顺序** → [唯一派生视图](references/_shared/pipeline-overview.md)（23 节点全表 + 修订回环仲裁规则）；顺序与阻断关系的唯一真源 = `phase-order.yaml`。本文件**不重列全景**（重列即构建期红，flow-check 规则 24）。

## 怎么用

**方式一（推荐）**：ClawHub 安装
```bash
openclaw skills install @zuoyunlai/lunheng-article-pipeline@2.12.63  # pin 审计版本（回应 ClawHub T08）
# 或本地：openclaw skills add /path/to/lunheng-article-pipeline
```

**方式二：参考方法论**——九角色 + 三角验证 + 独立审计 + 三不原则可移植到任意支持子代理编排的框架。

**模型更换**：能力分层（检索/分析写作/审计/主控）+ 候选池集中管理，任何 OpenAI 兼容模型可替换。详见 [`SKILL.md 模型分档`](SKILL.md)。

---

## 实战验证案例

完整案例库见 [`references/case-studies.md`](references/case-studies.md)（5 个案例）：原创性悖论 ~9500 字（A-）/ AI 安全对齐阿克琉斯之踵 ~7200 字（A-）/ AI 安全隐患 ~5700 字 / 商业热点 7650 字 / 品牌一致性 ~7900 字。

---

## 论衡哲学

> **「独立角色 + 结构化自评门 > 自我复核」** —— 论衡的核心命题。

主控自我复核是 LLM 的舒适区陷阱。M 门通过「LLM 兜底执行伪代码」把「自我审视」变成「结构化核验」（**零 exec 依赖**，不引入 Python 脚本，不扩大 exec 白名单）；F 失败模式（借鉴 ARS Lu et al. *Nature* 2026）把质量维度变成「AI 失败模式清单」威慑设计；**T6 批判伙伴**从反方独立攻击论证（v2 含主人洞察）。

---

## 版本演进（重大里程碑）

| 版本 | 时间 | 阶段 | 核心改进 |
|------|------|------|---------|
| v2.0.5 | 2026-08-12 | 初始发布 | 7 角色基础流水线 |
| v2.2.0 | 2026-08-15 | 机械化门 | M 门 + F1-F7 失败模式 + 修订 ≤2 轮 |
| v2.3.0 | 2026-08-21 | 角色重构 | 9 角色编号统一 + Phase 3.6 批判节点 |
| v2.4.0 | 2026-08-23 | 功能增强 | G14 中文 AI 痕迹闸 + T9 同行评审 + 方法论足迹面板 |
| v2.5.0 | 2026-08-24 | 可选项 | 期刊匹配 + 中文数据源 3 梯队 + 多格式导出 |
| v2.7.0 | 2026-09-07 | 人在环 | Checkpoint Card + T8 独立角色卡 + 安全收敛 |
| v2.10.0 | 2026-09-08 | 审计治理 | 章节级 M 门 + T4→T5 准并行 + 独立审计修订 |
| v2.11.0 | 2026-09-09 | 实战机制化 | 主控执行协议机制化（教训 #238/#239）|
| v2.11.1 | 2026-09-09 | 一致性 | 文档/脚本一致性缺陷全修（check-version 补 17 项 + CI 改调单一真源）|
| v2.12.0 | 2026-09-09 | 判据单源 | 判据单源 + 内容质量门脚本化 + 双视图同步（教训 #251/#252/#253）|
| v2.12.1 | 2026-09-09 | 瘦身重构 | 瘦身重构（-41.8%）+ cwd_default 陷阱修复 + 教训索引补全（教训 #255）|
| v2.12.2 | 2026-09-09 | 假前提修复 | token 统计假前提修复（教训 #256）+ 版本一致性盲区补入 |
| v2.12.3 | 2026-09-09 | 安全审计 | ClawHub T05 修复：路径校验改 in-process、净化脚本不碰代码块（教训 #257）|
| v2.12.4 | 2026-09-09 | 编号统一 | 教训编号体系统一（方案 A）+ 净化包剥离后残留清零 + 自审门 O |
| v2.12.5 | 2026-09-10 | 注释清理 | 语义锚点型版本注释清理（36 文件 / 120 处）+ 安装 pin 同步补入 sync 脚本 |
| v2.12.10 | 2026-09-10 | 语义审计 | ClawHub 语义审计（对象＝已发布 v2.12.9，15 条）5 真问题全修 + 加固模型收紧为二选一 + 净化链代码保真（教训 #300）+ Release 单一入口脚本 |
| v2.12.11 | 2026-09-10 | — | **全仓四路专项审计（口径漂移 / 执行衔接 / 净化链内泄漏 / 交叉引用）真问题全修：加固口径 fail-closed 统一 + 人在环超时兜底 + status.md 写入者收口 + G14/T7/T9 时序校正 + 净化链规则补漏** |
| v2.12.34 | 2026-09-13 | 规范合规 | 官方规范审计整改：SKILL.md 11,335→9,996 字符（棘轮 10,000）+ description 876→123 字节 + 门 H 改仓库内快照判据 + `outputs/` 迁出技能根（37MB→2.8MB）|
| v2.12.46 | 2026-09-15 | 架构定案 | 九角色多 Agent 为唯一标准架构 + Phase 编号成为第一类真源字段（flow-check 规则 11）+ 角色产物写入边界 + 人在环 4 个 checkpoint 机械门 |
| v2.12.54 | 2026-09-18 | 真源收敛 | 全景与阶段顺序收敛为唯一派生视图（`pipeline-overview.md` 23 节点全表，flow-check 机械守）+ R-2~R-6 构建期校验 + T9 改默认启用 |
| v2.12.58 | 2026-09-19 | 门扩围 | M 门文档围栏错位修复（教训 #426）+ 自审门 X 新增并**全仓扩围**（X.1-X.4，扩围即抓到第二例同类缺陷）+ 教训 #424-#428 |
| v2.12.59 | 2026-09-19 | 判据面 | 全面审计第一批整改：悬空指针补建（`host-verify-recipe.md`）+ 门 U 扩第三类扫描面（活文档内联引用）+ §十六 分层指针 + 定稿图件口径显式化 |
| v2.12.60 | 2026-09-19 | 机械锁 | 图件**嵌入式**图位机械锁（M-11 双计数 + flow-check 20b）+ 节点 `kind` 必填门（32）+ 补打 4 个缺失 tag |
| v2.12.61 | 2026-09-19 | 词表同源 | 人环决策词表三处同源（flow-check 33：status ⊆ yaml）+ Phase 0 拆「未进线出口」+ 卡片效力边界显式声明 |
| v2.12.62 | 2026-09-19 | 分层+棘轮 | P2-6 二次分层（§二十/§二十三 外移，净减 12.6KB）+ 门 Y 体量软棘轮 + P2-7 编号去副本（门 H 双判据）+ B12 三点接线与诚实边界 |
| v2.12.63 | 2026-09-19 | 发布面热修 | 维护者内档停止出厂（`_shared` **白名单准入门**）+ 净化规则误删修复（3h-5 mention-only + 行级断言）|
完整 changelog 见仓库内 [CHANGELOG.md](CHANGELOG.md)（最近 5 期）+ [CHANGELOG-archive.md](CHANGELOG-archive.md)（v2.12.47 起拆分轮转，两份合起来仍是逐版本全量记录；轮转边界见归档文件头部）；GitHub [Releases](https://github.com/zuoyunlai/lunheng-article-pipeline/releases) 为同一内容的发布视图。教训沉淀见 [memory/lessons.md](https://github.com/zuoyunlai/openclaw-workspace)（编号以索引为准）。

---

## 许可

[MIT License](LICENSE) — Copyright (c) 2026 左运来 (zuoyunlai)。允许商业使用、修改、再分发，需保留版权声明。
