*.bak.*
.bak-*
audits/
docs/
references/_shared/archive/
outputs/
scripts/__pycache__/
*.pyc
# Python test/cache directories (pytest_cache 自带 .gitignore 会被屏蔽，根目录兜底防 rsync 污染净化包)
.pytest_cache/
__pycache__/
run/
# OpenClaw 工作区种子文件：若有会话把本仓目录当作「全新工作区」初始化，运行时会播种这 5 个文件。
# 它们不属于本仓（本仓给的是技能，不是工作区），已在 2026-09-18 被误播种一次，故在此硬拦。
# 注意：若将来确实需要仓库级 AGENTS.md，用 `git add -f AGENTS.md` 显式放行。
AGENTS.md
BOOTSTRAP.md
IDENTITY.md
SOUL.md
USER.md
# 子会话在仓库内写的验证/审计报告：属「工程过程产物」，不进版本库（主人 2026-09-19 裁定）。
# 注：若某一份报告要正式归档，用 `git add -f reports/<file>` 显式放行。
reports/
# 同上：工作区式会话会把「agent 记忆」写进仓库根 memory/（2026-09-18 实际发生，内为真实工作日志，
# 已另行搬运到 ~/.openclaw/workspace/memory/，仓库内副本删除）。同样用 `git add -f` 可显式放行。
memory/
