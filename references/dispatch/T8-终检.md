> 版本：v2.12.72（自动同步 2026-09-22）

> 🌐 **语言政策**：产出语言由 Phase 0「目标语言」字段**显式选择**（中文 / English / 中英混 / 其他，**不设默认**），全流程以该字段为准；中文特化按**目标语言客观适用**——含中文时 **G14 中文 AI 痕迹闸必跑**（v2.12.40 起不再是可选项），纯外语时记 `n/a`（客观不适用，非「关闭」）；GB/T 7714-2015 引用规范为可选能力。二者均不构成使用者语种限制。

> 权威源：references/agents/08-终检-final-inspector.md（本文件为派生视图，冲突以角色卡为准）
> 从 pipeline-readme.md 派发话术段拆出。主控执行终检（T8，主控亲完成，不 spawn） 时按需读本文件，避免一次加载全部派发话术。完整角色卡见 references/agents/08-终检-final-inspector.md。

> 公共工具白名单 / 零 exec / 降级自报 / token 统计：见 [`_shared/真源/dispatch-header.md`](../_shared/真源/dispatch-header.md)


### 终检（T8，主控亲完成）

> **v2.3.0 关键澄清**：T8 终检由 T0 主控亲自完成（不 spawn 子代理）。有独立角色卡 `08-终检-final-inspector.md`（职责单一真源）。论衡有 10 张角色卡（T0-T7 + T8 终检 + T9 同行评审）。
> **语言边界**：本节模板文本为中文，但**终检与交付语言以任务简报「目标语言」字段为准**；目标语言非中文时，T8 用该语言出具交付说明与结论（详见 SKILL.md「语言边界」表）。
> **主控终检必查项**（详见角色卡 [`08-终检-final-inspector.md`](../agents/08-终检-final-inspector.md) § 终检必查项）：
> 1. `final/定稿.md` 纯净（无交接报告/角色元数据混入）+ **投稿版禁入清单逐条 0**（头部工程元数据段 / `## 投稿就绪附录` 段 / 图表清单 / 主控签字 / 引用规范等操作员报告内容；真源 = [`deliverables.md`](../deliverables.md)「投稿域 vs 工程域」，v2.12.43）
> 2. 引用标注完整（编号残留 grep 清零，或已转内联标注）
> 3. PDF 字体/乱码验证（如需，见 format-export §四·六）
> 4. 三处同步（run/ ↔ outputs/ ↔ PDF）
> 5. [Cxx] 引用完整（与正文互引）
> 6. 原创性声明 + G7 原创性审计通过
> 7. 统计口径动态更新（grep 重新统计）
> 8. 先行者引用闭环（[先xx] 文末对应）
> 9. 文末「引用来源」四节完整性闭环（双向 diff，教训 #264）
> 10. M 门四档结论为通过（读取 M-Gate-Report-v2.2.12.json）
> 11. AI 使用披露（学术期刊投稿加「AI 使用声明」段）
> 12. **多格式导出（v2.12.56：归入 M-13 建议清单，不再是 Phase 5 对话式选项卡）**：格式转换（md/latex/docx/pdf）**不再是 Phase 0 选项，也不再是 T8 终检时向主人呈现的 A–F 选择卡**——**并入第 19 条「主人自行操作建议清单」第 1 项**。语义不变：**执行者 = 主人 host shell**，**agent 不执行**任何转换命令；命令模板见该清单（命令真源仍 = `_shared/真源/format-export.md` § 二）。主人需要哪种格式 → 直接按建议清单自行执行；`交付说明.md` 的「导出文件清单」字段由主人执行后自行登记（agent 不代写）。注：**图文嵌入定稿不属本条的「格式转换」**，它由 `final_assembly` 在流水线内完成（v2.12.60，见 M-11 第 3 条）。

> 13. **本文差异化段检查**：文末「先行者文献」节每条 [先xx] 是否带「本文差异化：」段，按任务简报拍板的策略（每条加/仅主论点加/不加）逐条核验。缺失 → spawn T5 v(N+1) 补（B 类补齐修订，登记，不占用常规 2 轮预算——见 pipeline-overview.md「修订回环仲裁规则」）。
>
> 14. **Token 消耗记录**：T8 终检交接报告「token 消耗」字段由主控亲自汇总——三个来源，**全精确**：
>     - **子代理**：主控 `sessions_yield` 收到完成事件时，从事件末尾 Stats line 提取 `Token usage`（input/output/total；sessions_spawn 返回值无 stats 字段，教训 #256）
>     - **主控自身**：T8 终检前用 `session_status({sessionKey: "current"})` 拿主会话精确值（含 cost）
>     - **汇总规则**：主控 + T1-T7 + T9 + G14 Σ 填入 `final/交付说明.md`「成本指标」字段，并在对话中向主人呈现
>
> 15. **测试模式三处披露一致性（v2.12.43）**：任务简报「运行性质」= `测试模式` 时，T8 机械核对三处一致披露（任务简报 / `status.md`「项目元数据」/ `final/交付说明.md`「测试模式披露」段）；缺任一或三处不一致 = **P1**，补齐后**重跑终检**（不得只改一处、不得放行）。
>
> 16. **【v2.12.49 M-1】交付物指纹绑定与一致性断言**：
>  - T8 终检 `read` 比对四处报告的 `path` + 文本度量**同源**（agent 侧唯一可执行项）
>  - `sha256` / 精确 `bytes` 属**主人侧量值**（零 exec 不可得）：主人 host shell `sha256sum` / `wc -c` 补算后回填；**未回填记 `unavailable` ⇒ 判 `pending_owner_verification`（无法判定），禁止判通过**
>  - 主人回填后与 T7 / G14 / T9 三处 `audited_artifact.sha256` 两两相等 ⇒ 才判「指纹合格」
>  - 任一处**路径/度量**不一致 = 审计对象漂移，判不合格，返回 T5 v(N+1) 重走
>  - 交付说明头部必填 `deliverables_fingerprint` 字段（未回填项记 `unavailable`）
>
> 17. **【v2.12.49 M-2】终态冻结**：
>  - `phase5_acceptance.terminal = accepted` 标记后任何写入 `final/` 或 `drafts/current_draft.md` 使终态失效
>  - 主控必须重跑 `g14_style_gate → t9_review → final_assembly → t8_technical_final`，重新取 `phase5_acceptance` 拍板
>  - `status.md` 记 `post_acceptance_reopen` + 原因 + 重跑链 sha256
>  - 默认保留旧 accepted = 不合格
>
> 18. **【v2.12.49 M-8】审计对象一致断言**：
>  - T7 / G14 / T9 / T8 四处 `audited_artifact` 的 `path` 同源；`sha256` 在主人回填后两两相等（未回填记 `unavailable` + `pending_owner_verification`）
>  - `current_draft_sync` 为锚点真源：下游报告的**路径与文本度量**必须以该点为基准（`sha256` 由主人依该点回填）
>  - `t7_5_integrity` 与 `t8_technical_final` 做单点对多点集合比对；差集非空 = 多版本路由漂移
>
> 19. **【v2.12.49 M-13；v2.12.54 扩四类；v2.12.56 收口】主人自行操作建议清单**（与 M-12 互补；**原第 12 条「多格式导出主动呈现」已并入本清单第 1 项，不再单列**）：
>  - T8 终检后**必输出一张「主人自行操作建议清单」**（下表四类；**多格式导出 = 第 1 项**），**必须四类齐全，缺任一项 ⇒ T8 不合格**：
>
>  | # | 建议动作 | 执行者 | 命令来源 |
>  |---|---|---|---|
>  | 1 | 文档格式转换（docx/pdf/latex） | **主人 host shell** | `_shared/真源/format-export.md` §二 |
>  | 2 | SVG → PNG 转换（公众号排版等） | **主人 host shell** | `templates/图表-SVG-template.md` §6.1 |
>  | 3 | 封面视觉（文生图 / SVG 矢量；论衡 v2.12.52 起不再调用 `image_generate`，由主人自行生成） | **主人自行生成** | `operations.md` 封面段 |
>  | 4 | SHA256 校验和登记（终检后对 `final/定稿.md` + 图件） | **主人 host shell** | `_shared/治理/project-archive-sop.md` |
>
>  - **可直接复制的命令模板**（四类对应，**无 `#` 注释行**，逐行原样复制到 host shell 即可运行；命令与依赖同源 = [`_shared/真源/format-export.md`](../_shared/真源/format-export.md) §〇/§二 —— latex/docx/pdf 所需模板 / `.bib` / `.csl` **需主人自备**；**第 3 类「封面视觉」无统一可复制命令**、由主人自行生成，故本模板覆盖第 1/2/4 类）：

```
pandoc final/定稿.md -o final/定稿.tex --template=academic-paper --bibliography=final/证据包/参考文献.bib --csl=chinese-gb7714-2015-numeric
pandoc final/定稿.md -o final/定稿.docx --reference-doc=academic-paper-template.docx --bibliography=final/证据包/参考文献.bib --csl=chinese-gb7714-2015-numeric
pandoc final/定稿.md -o final/定稿.pdf --pdf-engine=xelatex -V CJKmainfont="Noto Serif CJK SC" --template=academic-paper --bibliography=final/证据包/参考文献.bib --csl=chinese-gb7714-2015-numeric
rsvg-convert -w 1600 -o final/图件/图1.png final/图件/图1.svg
sha256sum final/定稿.md final/图件/*.svg > final/定稿.sha256
```

>  - **图位嵌入已在流水线内完成（v2.12.60 定案，**不再是主人步骤**）**：`final_assembly` 必须把每个图位写成可渲染的 `![图N：标题](图件/图N_标题.svg)`（与定稿同目录相对路径），使**嵌入计数 = 拍板 N**（M-11 第 3 条双计数：① `[图N：` 占位计数 + ② 嵌入行计数）。口径真源 = [`deliverables.md`](../deliverables.md)「定稿图件口径」。**只写纯文本 `[图N：标题]` = T8 不合格**。主人这边只剩第 2 类格式转换（SVG → PNG）。

>  - **硬约束（v2.12.54 强化）**：清单**只给建议 + 可直接复制的命令模板**（**不用 `#` 注释行**）；**agent 不执行**任何一条；每项**必须**标「执行者 = 主人」（封面项 = 主人自行生成）；封面项必须披露**外发范围**（图像 prompt 可能含项目上下文）——由主人自行调用时不涉论衡外发面
>  - **封面口径澄清**：M-12 后封面**不在 agent 流水线内**（论衡不生成封面），**但封面仍须出现在 T8 终检后的「主人自行操作建议清单」中**——「不在流水线内」≠「不用管」
>  - **旧路径作废 + 与 M-1 衔接**：原「agent 自动写 `final/定稿.sha256`」路径自 v2.12.54 起作废；校验和一律由主人 host shell `sha256sum` 补算后回填登记（命令见上，登记规范见 `_shared/治理/project-archive-sop.md`），与第 16 条 M-1「主人 host shell 补算」同口径
>  - 清单缺失、**四类缺任一项**、或未标执行者 ⇒ **T8 不合格**
>
> 20. **【v2.12.49 M-14】字数口径单一化**：
>  - **唯一真源 = 正文汉字数**（`字数判定表.md` §一）；不计图注 / 图题 / 参考文献 / 附录 / 声明段 / 角色元数据
>  - T8 必填 `body_char_count`（正文汉字数）+ `body_limit`（上限）
>  - **全库不得重报「字数」精确值**（仅报档位，见 part-3A M-3）；全流程一律引用字数判定表，不重述口径
>  - 交付说明中任一百分比必可由 `body_char_count / body_limit` 复算得出；复算不一致 = **不合格**

> **派发文件改造（教训 #252，判据单源化）**——T8 15-19 项「可发表性 + 图表」判据**全部下沉到单一真源** [`../_shared/真源/可发表性判定表.md`](../_shared/真源/可发表性判定表.md)。本派发文件不再重复罗列判据，主控执行 T8 时：

> 1. **必读判据单源**：`references/_shared/真源/可发表性判定表.md` § 一 6 维度 + § 二 汇总表
> 2. **主控亲为流程**：按 § 三执行流程跑完 48 项（read + grep + LLM 推理）
> 3. **本地自动验证**：`bash scripts/paper-ready-check.sh <项目名>`（开发者工具）  <!-- 零 exec：维护者/主人在 host shell 运行；论衡 agent 不执行 -->
> 4. **失败处置**：按判定表 § 一各维度末列执行（spawn T5 v(N+1) 改 / 主控亲修 / 主人核验）
>
> **关键判据**（教训 #251，核心修复，专治「类型标识齐全 ≠ 顺序编码闭环」假绿灯）：
> - 国标引用顺序编码闭环 → 判定表 § 维度 4.3 顺序编码验证器（python 伪代码 + `scripts/paper-ready-check.py::check_citation_ordering`）
> - 国标引用类型标识齐全 → 判定表 § 维度 4.2（≥ 5 种类型标识，但**不构成顺序闭环**）
> - **⭐ SVG 嵌入文本孤儿（v2.12.13 承接自 T7）** → 判定表 § 维度 5.4：提取 `final/图件/*.svg` 内嵌文本节点（text/desc/title/tspan）中的 `[Dxx]`/`[Cxx]`/`[Lxx]` → 与数据卡/文献卡/案例卡 diff，**孤儿 = P0 拦截**。T7（Phase 4.2）运行时图件尚未生成，原门实为**空集通过（静默跳闸门）**，现由 T8（Phase 4.5/T9 之后）承接。
>
> **48 项分组**（详见判定表 § 二汇总表）：
>
> | 组 | 项数 | 引用 |
> |----|------|------|
> | A 交付物纯净性 | 4 | 判定表 § 二 组 A |
> | B 引用闭环 | 5 | 判定表 § 二 组 B + 维度 4 |
> | C 统计+原创性 | 4 | 判定表 § 二 组 C + `字数判定表.md` |
> | D M 门+同步 | 2 | 判定表 § 二 组 D |
> | E Token+差异化 | 2 | 判定表 § 二 组 E |
> | F 可发表性 6 维度 | 31 | 判定表 § 一 维度 1-6 |
> | **合计** | **48** | — |
>
> **触发条件**：T8 在 T7 审计通过 + T7.5 完整性门闸门后，由 T0 主控亲自执行（主人签字在 Phase 5 终稿交付时，教训 #138）
