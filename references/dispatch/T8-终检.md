> 版本：v2.5.17（自动同步 2026-08-26）





> 从 pipeline-readme.md 派发话术段拆出（v2.5.6 token 优化）。主控 spawn 终检（T8，主控亲完成） 时按需读本文件，避免一次加载全部派发话术。完整角色卡见 references/agents/。

### 终检（T8，主控亲完成）

> **v2.3.0 关键澄清**：T8 终检 **不是独立角色**，由 T0 主控亲自完成。论衡有 9 张角色卡（T0-T7 + T9，v2.4.0 加 T9 同行评审），T8 终检无独立角色卡。
> **主控终检必查项**（15 项，详见主控卡 §63-65 + pipeline-readme.md §4.5）：
> 1. `final/定稿.md` 纯净（无交接报告/角色元数据混入）
> 2. 引用标注完整（编号残留 grep 清零，或已转内联标注）
> 3. PDF 字体/乱码验证（如需）
> 4. 三处同步（run/ ↔ outputs/ ↔ PDF）
> 5. [Cxx] 引用完整（与正文互引）
> 6. 原创性声明 + G7 原创性审计通过
> 7. 统计口径动态更新（grep 重新统计）
> 8. 先行者引用闭环（[先xx] 文末对应）
> 9. 文末「引用来源」四节完整性闭环（双向 diff，教训 #47）
> 10. M 门 exit 0（M-Gate-Report-v2.2.12.json）
> 11. AI 使用披露（学术期刊投稿加「AI 使用声明」段）
> 12. **多格式导出主动呈现（v2.5.5 P1 新增，教训 #172 主人实测反馈）**：T8 终检**必在对话里**向主人呈现多格式选项，不依赖任务简报预先拍板：

```
## Phase 5 多格式导出选择（v2.5.5 新增）

**当前 final/定稿.md 已就绪**，请主人选择导出格式：

- □ A. **md**（Markdown，默认，零额外 token）— 公众号/知乎/小红书
- □ B. **latex**（LaTeX 学术模板）— 学术论文投稿（pandoc + xelatex）
- □ C. **docx**（Word 学术模板）— 学术论文投稿（pandoc + reference-doc）
- □ D. **pdf**（含 SVG 图表嵌入）— 终稿存档/打印（pandoc + xelatex + rsvg-convert）
- □ E. **多格式全产**（md + latex + docx + pdf 一起）
- □ F. **跳过**（只要 md，其他都不要）

**默认**：主人不答 = A (md) 零额外 token（**节省 token 哲学**）。

**触发细节**：
- B/C/D 需主人手工跑 pandoc（零 exec 哲学，论衡 LLM 不直接调 pandoc）
- 命令模板见 `_shared/format-export.md` § 二
- 多格式导出后主控在 `final/交付说明.md` 加「导出文件清单」字段
```

> 13. **本文差异化段检查（v2.5.5 P2 新增，教训 #171 主人反馈）**：文末「先行者文献」节每条 [先xx] 是否带「本文差异化：」段，按任务简报拍板的策略（每条加/仅主论点加/不加）逐条核验。缺失 → spawn T5 v(N+1) 补（不计 ≤2 轮，B 类修订）。
>
> **触发条件**：T8 在 T7 审计通过 + T7.5 完整性门闸门后，由 T0 主控亲自执行（v2.3.3 删主人签字 Phase 5，主人签字在 Phase 5 终稿交付时，教训 #138）
