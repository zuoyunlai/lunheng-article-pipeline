> 版本：v2.11.1（自动同步 2026-09-09）





















> 权威源：references/agents/08-终检-final-inspector.md（v2.7.0 起有独立角色卡，本文件为派生视图，冲突以角色卡为准）
> 从 pipeline-readme.md 派发话术段拆出（v2.5.6 token 优化）。主控执行终检（T8，主控亲完成，不 spawn） 时按需读本文件，避免一次加载全部派发话术。完整角色卡见 references/agents/08-终检-final-inspector.md。

> 公共工具白名单 / 零 exec / 降级自报 / token 统计：见 [`_shared/dispatch-header.md`](../_shared/dispatch-header.md)


### 终检（T8，主控亲完成）

> **v2.3.0 关键澄清**：T8 终检由 T0 主控亲自完成（不 spawn 子代理）。v2.7.0 起有独立角色卡 `08-终检-final-inspector.md`（职责单一真源）。论衡有 10 张角色卡（T0-T7 + T8 终检 + T9 同行评审）。
> **主控终检必查项**（详见角色卡 [`08-终检-final-inspector.md`](../agents/08-终检-final-inspector.md) § 终检必查项）：
> 1. `final/定稿.md` 纯净（无交接报告/角色元数据混入）
> 2. 引用标注完整（编号残留 grep 清零，或已转内联标注）
> 3. PDF 字体/乱码验证（如需；v2.7.15 加 ⚠️ 不渲染成 △ 检查，见 format-export §四·六）
> 4. 三处同步（run/ ↔ outputs/ ↔ PDF）
> 5. [Cxx] 引用完整（与正文互引）
> 6. 原创性声明 + G7 原创性审计通过
> 7. 统计口径动态更新（grep 重新统计）
> 8. 先行者引用闭环（[先xx] 文末对应）
> 9. 文末「引用来源」四节完整性闭环（双向 diff，教训 #47）
> 10. M 门 exit 0（M-Gate-Report-v2.2.12.json）
> 11. AI 使用披露（学术期刊投稿加「AI 使用声明」段）
> 12. **多格式导出主动呈现（v2.5.5 P1 新增，教训 #172 主人实测反馈）**：T8 终检**必在对话里**向主人呈现多格式选项，不依赖任务简报预先拍板：

```
## Phase 5 多格式导出选择（v2.5.5 新增）

**当前 final/定稿.md 已就绪**，请主人选择导出格式：

- □ A. **md**（Markdown，默认，零额外 token）— 公众号/知乎/小红书
- □ B. **latex**（LaTeX 学术模板）— 学术论文投稿（pandoc + xelatex）
- □ C. **docx**（Word 学术模板）— 学术论文投稿（pandoc + reference-doc）
- □ D. **pdf**（含 SVG 图表嵌入）— 终稿存档/打印（pandoc + xelatex + rsvg-convert）
- □ E. **多格式全产**（md + latex + docx + pdf 一起）
- □ F. **跳过**（只要 md，其他都不要）

**默认**：主人不答 = A (md) 零额外 token（**节省 token 哲学**）。

**触发细节**：
- B/C/D 需主人手工跑 pandoc（零 exec 哲学，论衡 LLM 不直接调 pandoc）
- 命令模板见 `_shared/format-export.md` § 二
- 多格式导出后主控在 `final/交付说明.md` 加「导出文件清单」字段
```

> 13. **本文差异化段检查**：文末「先行者文献」节每条 [先xx] 是否带「本文差异化：」段，按任务简报拍板的策略（每条加/仅主论点加/不加）逐条核验。缺失 → spawn T5 v(N+1) 补（B 类补齐修订，登记，不占用常规 2 轮预算——见 SKILL.md 修订回环仲裁表）。
>
> 14. **Token 消耗记录**：T8 终检交接报告「token 消耗」字段由主控亲自汇总——三个来源，**全精确**：
>     - **子代理**：主控 spawn 时拿到的 `stats.tokens.in/out + prompt/cache`（sessions_spawn 返回值）
>     - **主控自身**：T8 终检前用 `session_status({sessionKey: "current"})` 拿主会话精确值（含 cost）
>     - **汇总规则**：主控 + T1-T7 + T9 + G14 Σ 填入 `final/交付说明.md`「成本指标」字段，并在对话中向主人呈现

> **v2.11.1 主控亲为「可发表性」全维检查（教训 #247 + #248，论衡 v2.11.0 实战打脸）**——T8 14 项扩展为 **18+ 项**，新增 4 项「可发表性」+ 1 项「图表」，是任何学术长文/报告/论文**必过**的 T8 内文检查：
>
> 15. **【可发表性-1】头部洁净**（v2.11.1 必修，教训 #247）——`final/定稿.md` 第 1-10 行**禁止**：
>     - **版本号 / 修订轮**：`v1/v2/v3`、`T5 修订轮`、`T6 修订`、`drafts/初稿-vN.md` 等工程术语
>     - **过程元数据**：`修订时间`、`修订依据`、`底本信息`、`修订要点`、`本稿由...修订而成` 等
>     - **过程稿路径**：`drafts/`、`audits/`、`analysis/` 等子目录引用
>     - **审计报告引用**：`T6 批判报告`、`T7 审计`、`G14 检测` 等过程卡引用
>     - **执行命令 grep 实测**：
>       ```bash
>       grep -E 'v[0-9] ?(· |-) ?T[0-9]|修订时间|修订依据|底本信息|修订要点|T6 批判|T7 审计|G14 检测|audits/|drafts/' final/定稿.md | head -20
>       ```
>     - **失败处置**：全部 0 命中 = PASS；任一命中 → 主控亲为清理头部
>
> 16. **【可发表性-2】前置要素**（v2.11.1 必修）——`final/定稿.md` 第 1-50 行**必有**：
>     - **作者署名**：`**作者**：<姓名>` 格式
>     - **摘要 / Abstract**：`## 摘要`（200-300 字客观陈述，含 3-5 个核心发现）
>     - **关键词 / Keywords**：`**关键词**：`（5-8 个逗号分隔）——中文学术、英文论文用 `**Keywords**:`
>     - **执行命令 grep 实测**：
>       ```bash
>       grep -cE '^## (摘要|Abstract)|^## (关键词|Keywords)|^\*\*作者\*\*' final/定稿.md
>       ```
>     - **失败处置**：每项必出 = PASS；任一缺失 → 主控亲为补
>
> 17. **【可发表性-3】AI 使用声明**（v2.11.1 强化，论衡 G13 · 教训 #247）——`final/定稿.md` 文末**必有**「## AI 使用声明」章节，含：
>     - **流水线角色**：检索/分析/写作/批判/审计/终检五阶段角色清单
>     - **模型选择**：子代理 + 主控具体模型（含 fallback 透明披露）
>     - **人类决策**：Phase 0/2.5/3.5/5/5.5 主人拍板记录
>     - **不试图隐瞒 AI 痕迹**：破折号 75 处等写作特征未消
>     - **执行命令 grep 实测**：
>       ```bash
>       grep -cE '^## (AI 使用声明|Author Contribution|人工智能辅助声明)' final/定稿.md
>       grep -cE '论衡 G13|诚实透明|不试图隐瞒 AI 痕迹' final/定稿.md
>       ```
>     - **失败处置**：每项必出 = PASS；任一缺失 → 主控亲为补（v2.11.0 实战教训：「一句 AI 辅助生成」不算正式 AI 声明，必须有完整章节）
>
> 18. **【可发表性-4】国标引用格式**（v2.11.1 必修，教训 #247）——附录格式按任务简报拍板的引用模式：
>     - **编号模式 → GB/T 7714-2015**（默认）：附录条目录录格式 `[J]/[C]/[D]/[M]/[R]/[S]/[Z]/[N]/[DB]/[EB/OL]` 文献类型标识 + 引用日期 + URL/DOI
>     - **内联模式 → APA / MLA**：作者. 年份. 标题. 出版地: 出版者. 格式
>     - **执行命令 grep 实测**（编号模式）：
>       ```bash
>       # 学术文献 GB/T 7714 类型标识
>       grep -oE '\[[MJCSDRPZNEB]+\]' final/定稿.md | sort -u
>       # 期望 ≥ 5 种类型标识 + 总条目数 ≥ 15
>       ```
>     - **失败处置**：类型标识齐全 = PASS；类型标识缺失 → 主控亲为重写附录
>
> 19. **【图表】配图完整性**（v2.11.1 必修，教训 #248）——5 节正文 ≥ 5 张 mermaid 图源：
>     - **图源目录**：`final/figures/*.mmd` ≥ 5 个文件
>     - **正文引用**：正文 ≥ 5 处「```mermaid 代码块」或「![图 N](../figures/...)」Markdown 图引用
>     - **数据溯源**：每图标注 `[Dxx]/[Lxx]` 数据源
>     - **执行命令 grep 实测**：
>       ```bash
>       ls final/figures/*.mmd | wc -l     # 期望 ≥ 5
>       grep -c '```mermaid\|\!\[' final/定稿.md   # 期望 ≥ 5
>       ```
>     - **失败处置**：图源齐全 = PASS；图源不足 → 主控亲为补 mermaid 图源（论衡零 exec 原则下，主人手工跑 `mmdc` 渲染 PNG）
>
> **触发条件**：T8 在 T7 审计通过 + T7.5 完整性门闸门后，由 T0 主控亲自执行（v2.3.3 删主人签字 Phase 5，主人签字在 Phase 5 终稿交付时，教训 #138）
