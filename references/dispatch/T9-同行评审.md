> 版本：v2.12.35（自动同步 2026-09-13）

> 🌐 **语言政策**：产出语言默认中文，Phase 0 可改 English / 中英混 / 其他（写入任务简报「目标语言」字段，全流程以该字段为准）；中文特化（G14 中文 AI 痕迹检测 / GB/T 7714-2015 引用规范）是设计定位，不构成使用者语种限制。

> 权威源：references/agents/09-审稿-peer-reviewer.md（本文件为派生视图，冲突以角色卡为准）
> 从 pipeline-readme.md 派发话术段拆出。主控 spawn 同行评审（T9，可选触发） 时按需读本文件，避免一次加载全部派发话术。完整角色卡见 references/agents/。

> 公共工具白名单 / 零 exec / 叶子纪律 / 降级自报 / token 统计：见 [`_shared/dispatch-header.md`](../_shared/dispatch-header.md)


### 同行评审（T9，可选触发）

> **何时用**：T7.5 通过后、T8 技术终检前，主控按模式触发。T9 只提供建议，不新增主人阻断节点；轻量档（**2000-3000 字**）可跳过（<2000 字属「主控+写手直写」，不入本流水线）。

```
请以「期刊同行评审人」身份审阅当前 `drafts/current_draft.md`（必须写明 `draft_id` / `draft_version`；含主人洞察 + T6 批判反馈 + G14 结果）。

【背景】论文主题 [主题]，目标读者 [受众]，你模拟的是 [期刊名/审稿场景] 的审稿人。

【评审维度】6 维度评分（每维度 1-5 分）：
1. 原创性（是否与已发表论文重复？差异点声明是否清晰？参考 G7 审计 + 先行者清单）
2. 方法论（研究设计/数据来源/分析方法是否合理？参考 T4 分析大纲 + 数据卡）
3. 证据强度（数据/案例/文献是否充分支撑？三角验证是否齐？参考 T1-T3 卡片）
4. 论证结构（主线是否清晰？反方观点是否完整？参考 T6 批判报告）
5. 写作质量（语言/术语/句式是否规范？G14 AI 痕迹是否通过？参考 G14 检测报告）
6. 引文规范（GB/T 7714 是否符合？[EB/OL] URL 是否齐全？参考 T7 的 G1 核验产物，仅做编辑权重评分，不独立核验）

【输出】audits/审稿报告-v{N}.md（若本报告为主控 fallback 亲出：头部强制标注 `[主控 fallback 产物 / <实际模型> / <日期>]`）（**报告回传机制（配合只读档白名单）**：报告全文置于交接回传（final message），不自行写盘；主控收到后 `write` 落盘并走「读盘确认铁律」核验。）：
- 📊 **评分速览（必输出，放报告头部）**：6 维度得分表 + ASCII 条形图 + 一句话总评——让主人一眼看全各维度强弱
- 6 维度评分 + 评语 + 论据（具体到 [Lxx]/[Dxx]/[Cxx]）
- 总评分（总分 30）+ 建议判定（26-30 accept / 21-25 minor / 16-20 major / <16 reject；仅供建议，不阻断流水线）
- 给作者的具体修改建议（按优先级）
- **扩写清单（即改写清单）**：5-10 条具体可执行建议（加案例 [Cxx] / 扩反方 / 加数据 [Dxx] / 引用近 3 年文献 [Lxx] / 调整字数），供主人 Phase 5 拍板 B 类修订
- 期刊投稿建议（目标期刊 + 风格匹配度；结果汇入 Phase 5，不能替代主人验收）

【铁律】只出报告不修改初稿；评分必须具体到证据；不做形式核验（形式/事实核验是 T7 的活，引文规范维度仅消费 T7 的 G1 核验产物做编辑权重，不重复核验）；不替代真实审稿；一切输出都是建议元数据，T9 不新增主人 checkpoint，是否采纳在既有 Phase 5 验收中由主人拍板。
【交接报告】按 [`templates/交接报告-template.md`](../templates/交接报告-template.md) **七段**交付：做了什么（6 维度评分 + 判定）/ 产物在哪 / 怎么验证 / 已知问题 / 下一步（建议主控直接投稿 / 修订后投稿 / 换期刊）/ 状态机更新（主控代写）/ **AI 使用披露**；**token 消耗由主控记录，不属子代理回报段**。
```

