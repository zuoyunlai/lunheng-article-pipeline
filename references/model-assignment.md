> 版本：v2.12.45（自动同步 2026-09-15）

> 🌐 **语言政策**：产出语言由 Phase 0「目标语言」字段**显式选择**（中文 / English / 中英混 / 其他，**不设默认**），全流程以该字段为准；中文特化按**目标语言客观适用**——含中文时 **G14 中文 AI 痕迹闸必跑**（v2.12.40 起不再是可选项），纯外语时记 `n/a`（客观不适用，非「关闭」）；GB/T 7714-2015 引用规范为可选能力。二者均不构成使用者语种限制。

# 模型分档 → 指针文件

> **本文件已降级为指针（2026-09-14）**：论衡模型能力档 + 候选池的**唯一完整定义**在 [`_shared/模型候选池.md`](_shared/模型候选池.md)。
>
> 本文件此前与候选池**全文重复**（5 档表 / 映射规则 / 预算闸门 / 降级规则），构成双「单一真源」；为消除漂移，现**只保留导航，不再复述任何档位、模型或规则细节**。

## 导航

- **模型能力档 + 候选池（唯一真源）**：[`_shared/模型候选池.md`](_shared/模型候选池.md)
- 设计文档：[`设计文档.md`](设计文档.md)
- 运行手册（模型配置与更换指南 / 调度健康度预检）：[`pipeline-readme.md`](pipeline-readme.md)

> **冲突时以候选池为准**。本页保留在包内仅为兼容历史链接（`_shared/asset-index.md` / `_shared/skill-entry-appendix.md` 等仍指向此处，待后续批次统一改指真源）。
