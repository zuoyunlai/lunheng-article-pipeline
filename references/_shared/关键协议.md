> 版本：v2.12.27（自动同步 2026-09-11）

> 🌐 **语言政策**：产出语言默认中文，Phase 0 可改 English / 中英混 / 其他（写入任务简报「目标语言」字段，全流程以该字段为准）；中文特化（G14 中文 AI 痕迹检测 / GB/T 7714-2015 引用规范）是设计定位，不构成使用者语种限制。

## 四、关键协议

### workspace 路径收口（read/write/edit 边界）

- 主控 + 所有子代理的 `read/write/edit` 仅允许 `run/<项目名>/` 子树；**拒绝**绝对路径、父路径穿越（`..`）、symlink 逃逸、工作区外访问。
- 默认 cwd = workspace 根（**不设 `cwd_default`**，否则 run/ 被解析到 skill 目录内，教训 #255）；主控 spawn 时显式传 `cwd: run/<项目名>/`（相对 workspace 根），产出写盘必须落在 `run/<项目名>/<子目录>/` 内。
- 子代理每次任务首句必读本节；子代理拒绝改 cwd。
- **无例外**：论衡在任何阶段都不读宿主配置文件（含 `~/.openclaw/openclaw.json`，v2.12.13 起删除原「spawn 前加固核对」例外）；除此之外不读、不写工作区外。

### Phase 0：同意关卡

> **触发边界澄清**：Phase 0 **只在主控识别到「严肃长文写作请求」时触发**——必须**同时满足**：① 主题明确（≥1 个具体题目/关键词）；② 篇幅定位（≥1500 字）；③ 主人语气含「写」「做」「按这个题目来」等执行动词。**显式不触发场景**：① 闲聊 / 头脑风暴 / 探讨方法论；② 用户问「论衡能做什么 / 怎么用」（查问类）；③ 用户在跑别的任务（写代码 / 修图 / 发邮件）；④ 主控判断后请求主人「是否真要启动 Phase 0」——主人说「不」→ 直接退出，不写 status.md 不 spawn。
>
> **二次确认**：当主控不确定用户意图（「帮我想想 AI 对教育的看法」可解释为讨论 / 也可解释为开题），主控**必须**先在对话里问一句「你希望我现在就开 Phase 0 还是先聊聊」，主人回复后才走 Phase 0 流程，禁止自动判定。

**触发**：主控接收到论文写作请求后，第一时间执行

**两个「4 选 1」是两道不同关卡，不得混用（一致性要求）**：
- **定题四态**（是否启动流水线）：① 开工 ② 补充信息 ③ 暂停讨论 ④ 拒绝任务——这是主控对「要不要启动」的意图确认
- **外发同意 4 选 1**（外发数据范围）：① 全部同意 ② 脱敏+SVG+本地 Ollama ③ 部分同意 ④ 全部拒绝——这是对外部服务清单（SKILL.md「⚠️ 执行前安全须知 + 外部服务声明（精简合并）」节）的逐项知情同意；**无有效选择记录 = 未同意 = 不得进入 Phase 1（fail-closed）**；「开工」不隐含外发同意，必须单独问、单独记录到任务简报「外部服务同意记录」段

**Phase 0 必确认范围（缺一不可，全部确认后才可进入 Phase 1）**：
- 主题 / 篇幅 / 目标受众 / 目标语言
- 定题四态确认（是否启动）
- **外发同意 4 选 1**（**5 类外发类别的逐项知情同意**；类别唯一真源 = [`external-services.md` 逐类表](external-services.md)；写入任务简报「外部服务同意记录」段）
- **过程可见项**：方法论足迹面板（**默认启用，无开关**；按档位裁剪字段集）
- **按条件自动项**：G14 闸（中文 + 学术/商业评论/行业分析 → 必跑；轻量档 → 自检；纯外语交付 → 不适用；主人显式豁免须在交付说明披露）
- **功能可选项（2 项 + 多格式 6 选项）**：期刊匹配 / 多格式导出 —— **中文数据源已转为默认启用，不再是选项**（写入任务简报「▲ v2.5.0 可选项决策」段）
- 将创建的文件清单（主人过目确认）

**4 选 1（外发同意）选项**：

> ⚠️ **隐私后果告知（必读）**：以下任一选项同意外发，意味着**未公开草稿全文 / 专有研究 / 访谈材料的实质内容会离开本机**，发往的第三方 provider（Tavily / OpenAI / Google / 模型厂商等）可能按其隐私政策留存、加工这些内容，且不受本地控制。敏感内容一旦发出无法撤回。选择前请确认：这份稿子若被第三方留存，可接受吗？拿不准 → 选 ② 或 ③。

1. ✅ **全部同意**：检索关键词 + 大模型推理全文 + 图像 prompt 全部外发
2. 🛡️ **脱敏+SVG+本地 Ollama**：脱敏措辞 + SVG 封面 + 本地推理，全文不上外部模型
3. 📌 **部分同意**：逐项勾选（□ 检索关键词 □ 大模型推理全文 □ 图像 prompt）
4. ❌ **全部拒绝**：改纯本地，不外发任何数据

**禁止**：未经 Phase 0 直接 spawn 子代理；把定题「开工」当外发同意；无外发同意记录进入 Phase 1

---

#### 确定性同意门（fail-closed，逐调用过）

> **同意记录是「授权输入」，不是文档。** 每次外发 / opt-in 调用前，主控与子代理都**必须**过下表；判据不满足 = **阻断该调用**，不得按 prose 推定「应该已同意」。

| 调用类别 | 放行判据（**全部**满足） | 不满足时的动作 |
|---|---|---|
| 检索层（web_search / web_fetch / tavily_search / tavily_extract） | 记录存在 ∧ mode ∈ {①,②,③} ∧ 轴 A 含「检索关键词」 | **阻断**，改用主人自带材料 + 本地推理 |
| 学术元数据（OpenAlex / Crossref） | 上式 ∧ 轴 B 含「学术元数据」 | **阻断** |
| **大模型推理全文**（文献卡/数据卡/案例卡/草稿/大纲/正文 → 模型 provider） | mode=① ∨（mode=③ ∧ 轴 A 含「大模型推理全文」） | **阻断全文外发**；mode=② 时必须**脱敏 + 本地 Ollama** |
| 封面（image_generate） | [mode=① ∨（mode=③ ∧ 轴 B 含「封面」∧ 轴 A 含「图像 prompt」）] ∧ 工具级 opt_in 含 image_generate | **阻断**，改 SVG 封面（零外发） |
| 抓取层（Firecrawl / paper.edu.cn） | mode=① ∨（mode=③ ∧ 轴 B 含「抓取层」） | **阻断** |
| 记忆辅助（memory_get / memory_search / memory_recall） | mode ∈ {①,②,③} ∧ 工具级 opt_in 含 memory | **阻断** |
| **mode=④ 全部拒绝** | 上表全部阻断 | 纯本地；出网工具**一次都不调** |

**三条确定性规则**：
1. **两轴都必须有明确取值** —— 轴 A（数据形态）/ 轴 B（服务类别）任一为空、写「待定」或含糊 ⇒ 整条记录判为**无效** ⇒ 全部阻断。
2. **不一致即阻断 + 披露** —— 记录与实际调用类别不一致（如记③但轴 B 未勾某类却仍调用）⇒ 阻断，并写入 `final/交付说明.md`。
3. **缺失/不可读 ⇒ 按「全部拒绝」处理**（fail-closed），停止并回报主控。

---

### 0 条空卡协议

> **标题扫描纪律（本修订起）**：凡「小节计数 / 段数对账 / 标题扫描」类检查，**必须先剔除代码围栏（``` 块）内的内容** —— 多个文件含示例块（本文件下方即有），**示例内的 `##` 不是文档小节**。用 grep 逐行扫描会误计，须先判断围栏状态。

**适用场景**：**T3** 案例检索员检索结果为 0 条

**输出格式**：
```markdown
## [C-空] 案例检索结果（0 条）

**检索简报**：
- 检索关键词：xxx
- 检索范围：xxx
- 结果：0 条
- 原因分析：xxx
```

**后续流程**：主控读取 `[C-空]` 后**记录** `[C-空]` 并在对话中**告知**主人（**不等待**），默认按「不使用案例」继续（与 `agents/03` / `dispatch/T3` 的「不阻塞 T4 启动」一致）；主人若要调整检索策略 / 投喂案例 / 不使用案例，再回退对应步骤。

---

### 三检索员并行独立运行
**并行模式**：T1 ∥ T2 ∥ **T3** 任何量级必须独立 spawn

**互不干涉铁律**：
- T1 不产数据卡/案例卡
- T2 不产文献卡/案例卡
- **T3** 不产文献卡/数据卡

**触发条件**：**T4** 分析员在 T1 + T2 + **T3** 全部完成后才 spawn

---

### 叶子纪律（子代理不再委托，v2.12.8 新增）

**背景**：平台可能默认开启有界递归委派——子代理因而**拿得到** `sessions_spawn` / `subagents` / `sessions_list` / `sessions_history`。论衡架构假设「角色卡 = 叶子 worker」（主控只等**直接**子级；announce 链逐级上传），故角色卡一律按叶子纪律执行。

**规则（所有非主控角色适用）**：
- **不得**调用 `sessions_spawn` 派生任何子代理——需要额外检索/额外人手时，在**交接报告**写「需求回执」（缺什么、为什么缺、建议派谁），由主控决定是否 spawn
- **不得**调用 `subagents` / `sessions_list` / `sessions_history` 查看或管理其他会话（该能力属主控；叶子拿到是平台默认，不是授权）
- 子代理之间**不直接通信**（不调 `sessions_send`）——回传路径只有「交接报告 → 主控」一条
- 「未被平台剥夺」≠「被授权」：遵论衡纪律层，不因工具可得而扩大行为面

**三层冗余落地**（全部为纪律层，已内置）：
| 层 | 位置 | 宿主需做 |
|---|---|---|
| 纪律·角色卡 | `agents/0X-*.md` 头部声明 | 无（已内置）|
| 纪律·任务书 | 主控每次 spawn 必含叶子声明（见 `00-主控-扩展职责.md` §三）| 无（已内置）|

**代价（为何必须锁）**：子代理自行再 spawn 时，孙辈完成事件**不上传到主控**（announce 链逐级上传，主控只收直接子级）——主控看不到孙辈产物，等于「产出静默丢失 + token 已花」。

---

### 修订回环 ≤2 轮降级（教训 #270，+ #276）
**规则**：
- **「轮」的定义**（单一真源 = SKILL.md「修订回环仲裁规则」）：0 轮=T5 出 v1；1 轮=v1→v2（Phase 3.5 洞察 + T6/G14 批判反馈融入）；2 轮=v2→v3（T7 审计打回 / G14 Warning+ 修订，或 T8 亲修）；minor 修补（≤5% 字 / 引用格式 / 拼写 / 元叙事清理 / P1-D 事实错误）走 T8 inline 独立通道**不计轮**；超 2 轮 = 结构性 P1（A/B/C）+ P0 → spawn T5 v4 + Acknowledged Limitations（例外通道，须主人拍板）
- **T8 小幅修补权限**：≤字数 5% / AI 披露修正 / 元叙事清理 / P1-D 事实错误 → T8 亲修；结构性 P1-A/B/C + P0 → T5 独立写手
- **P1 分级**：P1-A/B/C（论证补强/结构缺失/反方缺失）→ T5 重启；P1-D（引用格式/元数据/漏引/拼写）→ T8 亲修
- 第 3 轮：自动降级为"人工介入模式"（Acknowledged Limitations）

**降级原因**：
- 防止无限循环（教训 #270）
- 强制人工决策介入（主人判断是否继续/终止/调整方向）

---

### 完成验证铁律（教训 #263）

> ⚠️ **零 exec**：下面代码块是**语义等价动作的说明**（产物存在性 / 非空 / 数量），主控用 `read` + 文件元数据**推理实现**；论衡 agent **不执行任何 shell**，下列命令形态仅供主人手工复核。

**主控收到子代理完成报告后，第一动作**：

```bash
ls -lh drafts/[产物文件]  # 验证文件存在 + 非空
grep -c "关键特征" drafts/[产物文件]  # 验证内容完整性
```

**不采信报告**：T5 写手两次"报告完成但未写盘"事故触发此铁律

---

### 执行韧化协议
**四层防御**：

1. **心跳机制**：启动 30 秒内首条 + 之后每约 5 分钟一次（与 [`执行韧化协议-exec.md`](执行韧化协议-exec.md) 同源）
2. **分阶段 ACK**（3 档）：
   - 短任务（<3 分钟）：不需要
   - 中任务（3-8 分钟）：1 次中途 ACK
   - 长任务（>8 分钟）：多次 ACK
3. **LLM 可用性初判**：子代理观察首次 LLM 调用响应与首 token 延迟；30s 无首响应 → 降级 fallback
4. **超时硬卡（角色分级）**：阈值见主控扩展职责 §二十二（T1-T3 10 / T4 12 / T5 15 / T6 15 / T7 12 / T9 10 / G14 8 分钟）

**Fallback 链**：主模型 30s 无响应时的降级次序由**宿主 agent 的 `model.fallbacks` 配置**决定（宿主维护项，论衡文档不绑定具体模型 ID；映射方法见 [`模型候选池.md`](模型候选池.md)）——子代理感知的行为：主模型静默 → 宿主自动切下一档，角色在产物头部标注降级事实

---

---

## 上下文压缩与引用标识符保全（v2.12.27 新增）

> **为什么论衡必须写这一条**：论衡的引用体系（`[Lxx]` / `[Dxx]` / `[Cxx]` / `[图N]`）是**不透明标识符**，而 M 门 / G 项**全部靠"读文本对账"**。长流水线（多 Phase + 修订回环 + 大卡片）**必然触发平台的自动压缩（auto-compaction）**——若压缩把标识符摘要掉，**对账会静默失败**（不报错，只是对不上）。

**平台事实**：压缩默认保全不透明标识符（`identifierPolicy: "strict"`），**但可被关闭**，且**自定义压缩 provider 必须自行实现保全**。

**论衡的立场与动作**：
1. **依赖默认保全**：论衡依赖 `identifierPolicy: "strict"`（平台默认值）——**请勿改为 `off`**。
2. **自定义 provider 须自证**：若宿主使用自定义压缩 provider，**必须**在其 `summarize()` 中保全 `[Lxx]/[Dxx]/[Cxx]/[图N]` 等不透明标识符；论衡**不校验**此点（宿主责任，见 [`host-hardening-recipe.md`](host-hardening-recipe.md)）。
3. **落盘即抗压缩**：所有关键状态（卡片、大纲、草稿、审计报告、决策记录）**一律落盘**——落盘产物**不受压缩影响**，这正 `status.md` / `run/<项目名>/` 设计的原因。**上下文里的引用编号可能被压缩，磁盘上的不会**。
4. **对账以磁盘为准**：M 门 / G 项对账**读磁盘产物**，不依赖对话历史里的引用编号。

**补充（平台事实）**：**上下文溢出错误不走模型 fallback**（`request_too_large` / `input exceeds the maximum number of tokens` / context length exceeded 等）——它们**留在压缩/重试逻辑内**处理；把它当 fallback 触发器是错的。真正的 fallback 触发项是：认证失败 / 限流耗尽 / 过载 / 超时型 / 计费禁用 / `model_not_found`。
**provider 拒答（refusal）是终态** —— 平台**不做**自动恢复、压缩重试或切到无关模型；下一轮用户消息才开启新 turn。论衡对此应**直接呈报主人**，不要期待自动兜底。

---

## 非节点期间的输入处理（主人中途插话 / steer）（v2.12.27 新增）

论衡的**4 个节点**（Phase 0 / 2.5 / 3.5 / 5）是**必须**停下来请示的时刻；但**平台允许主人在任何时刻插话**（队列模式 `steer` / `followup` / `collect` / `interrupt`，由宿主配置决定）。因此必须明确**非节点期间收到输入怎么处理**：

| 输入类型 | 处理 |
|---|---|
| 补充材料 / 事实纠正（不改变方向） | **吸收**进当前阶段产物；记 `status.md`「人在环决策记录」段并标注「**非节点输入**」 |
| 改变方向（换题目 / 换结构 / 换体裁） | **不擅自改**：暂停当前阶段 → 向主人确认（等同**提前触发**最近的节点）→ 记录 |
| 纯提问 / 进度询问 | 直接答；不动产物、不记决策 |
| 紧急停止 | 立刻停（平台 `interrupt` 会中止运行）；产物留在磁盘，记 `status.md` |

**三条纪律**：

1. **记录即生效** —— 任何非节点输入都要落 `status.md`（时间 + 原文摘要）；**口头吸收不落盘 = 无法追溯**（同「无明确决策 = 未通过」）。
2. **不因插话跳过节点** —— 插话**不能**替代 Phase 2.5 / Phase 3.5 的正式拍板。
3. **权威记录在 `status.md`** —— 对话历史可能被压缩吞掉（见上节）。

---

## 长文交付与渠道分块（v2.12.27 新增）

论文/长文远超单条消息上限：**平台按渠道对出站文本分块**（不同渠道上限不同，实测约 **2000–5000 字符**）。因此：

- **定稿以「文件」为准**：`final/定稿.md` 是权威交付物；渠道里发出的正文是**分块的副本**。
- **交付说明写清路径**：`final/交付说明.md` 必须给出**文件路径**（而不是让主人在聊天记录里找拼块）。
- **不因分块丢结构**：若主人要求「发到聊天」，先说明会被分块，并附文件路径。