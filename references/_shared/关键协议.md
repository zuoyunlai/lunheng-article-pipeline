> 版本：v2.5.6（自动同步 2026-08-25）

## 四、关键协议

### Phase 0：同意关卡（v2.1.7 金标准）
**触发**：主控接收到论文写作请求后，第一时间执行

**4 选 1 模式**：
1. ✅ **直接开工**：主人明确说"开始写"/"按这个题目来"
2. 📋 **补充信息**：主人提供素材但题目/角度模糊
3. ⏸️ **暂停讨论**：主人说"先别动手"/"我再想想"
4. ❌ **拒绝任务**：违反论衡适用边界

**禁止**：未经 Phase 0 直接 spawn 子代理

---

### 0 条空卡协议（v2.1.8）
**适用场景**：**T3** 案例检索员检索结果为 0 条（v2.3.0 重命名原 T6→T3）

**输出格式**：
```markdown
## [C-空] 案例检索结果（0 条）

**检索简报**：
- 检索关键词：xxx
- 检索范围：xxx
- 结果：0 条
- 原因分析：xxx
```

**后续流程**：主控读取 `[C-空]` 后告知主人，询问是否需要：
- 调整检索策略
- 主人投喂案例
- 不使用案例

---

### 三检索员并行独立运行（v2.1.8 核心架构）
**并行模式**：T1 ∥ T2 ∥ **T3** 任何量级必须独立 spawn（v2.3.0 重命名原 T6→T3）

**互不干涉铁律**：
- T1 不产数据卡/案例卡
- T2 不产文献卡/案例卡
- **T3** 不产文献卡/数据卡（v2.3.0 改 T6→T3）

**触发条件**：**T4** 分析员在 T1 + T2 + **T3** 全部完成后才 spawn（v2.3.0 改 T3→T4）

---

### 修订回环 ≤2 轮降级（v2.2.0，v2.3.1 定义细化，教训 #64 + #120）
**规则**：
- **「轮」的定义（v2.3.1 重定义）**：0 轮=主控启动 T5 出 v1；1 轮=T6 批判攻击 + T5 出 v2；2 轮=T8 主控亲修出 v3（小幅修补）；超 2 轮=结构性 P1（A/B/C）+ P0 才启 T5 v3
- **T8 小幅修补权限（v2.3.1 新增）**：≤字数 5% / AI 披露修正 / 元叙事清理 / P1-D 事实错误 → T8 亲修；结构性 P1-A/B/C + P0 → T5 独立写手
- **P1 分级（v2.3.1 新增）**：P1-A/B/C（论证补强/结构缺失/反方缺失）→ T5 重启；P1-D（引用格式/元数据/漏引/拼写）→ T8 亲修
- 第 3 轮：自动降级为"人工介入模式"（Acknowledged Limitations）

**降级原因**：
- 防止无限循环（教训 #64）
- 强制人工决策介入（主人判断是否继续/终止/调整方向）

---

### 完成验证铁律（v2.1.3，教训 #46）
**主控收到子代理完成报告后，第一动作**：

```bash
ls -lh drafts/[产物文件]  # 验证文件存在 + 非空
grep -c "关键特征" drafts/[产物文件]  # 验证内容完整性
```

**不采信报告**：T5 写手两次"报告完成但未写盘"事故触发此铁律（v2.3.0 改 T4→T5）

---

### 执行韧化协议（v2.1.0）
**四层防御**：

1. **心跳机制**：每 30 秒一次心跳 ACK
2. **分阶段 ACK**（3 档）：
   - 短任务（<3 分钟）：不需要
   - 中任务（3-8 分钟）：1 次中途 ACK
   - 长任务（>8 分钟）：多次 ACK
3. **模型健康度预检**：spawn 前 1-token ping（30s 无响应降级 fallback）
4. **8 分钟硬卡**：6 分警告 / 7 分 partial / 8 分 kill

**Fallback 链**（4 档）：
```
deepseek-v4-pro → minimax-M3 → deepseek-v4-flash → glm-5.3
```

---

