> 版本：v2.7.6（自动同步 2026-09-07）











## 四、关键协议

### Phase 0：同意关卡（v2.1.7 金标准）

> **触发边界澄清（v2.5.23 强化，回应 ClawHub SQP-1 MEDIUM，已 downgraded 但加注保险）**：Phase 0 **只在主控识别到「严肃长文写作请求」时触发**——必须**同时满足**：① 主题明确（≥1 个具体题目/关键词）；② 篇幅定位（≥1500 字）；③ 主人语气含「写」「做」「按这个题目来」等执行动词。**显式不触发场景**：① 闲聊 / 头脑风暴 / 探讨方法论；② 用户问「论衡能做什么 / 怎么用」（查问类）；③ 用户在跑别的任务（写代码 / 修图 / 发邮件）；④ 主控判断后请求主人「是否真要启动 Phase 0」——主人说「不」→ 直接退出，不写 status.md 不 spawn。
>
> **二次确认（v2.5.23 新增）**：当主控不确定用户意图（「帮我想想 AI 对教育的看法」可解释为讨论 / 也可解释为开题），主控**必须**先在对话里问一句「你希望我现在就开 Phase 0 还是先聊聊」，主人回复后才走 Phase 0 流程，禁止自动判定。

**触发**：主控接收到论文写作请求后，第一时间执行

**两个「4 选 1」是两道不同关卡，不得混用（v2.6.4 澄清，回应 ClawHub T09 一致性审计）**：
- **定题四态**（是否启动流水线）：① 开工 ② 补充信息 ③ 暂停讨论 ④ 拒绝任务——这是主控对「要不要启动」的意图确认
- **外发同意 4 选 1**（外发数据范围）：① 全部同意 ② 脱敏+SVG+本地 Ollama ③ 部分同意 ④ 全部拒绝——这是对外部服务清单（SKILL.md「外部服务与数据流声明」节）的逐项知情同意；**无有效选择记录 = 未同意 = 不得进入 Phase 1（fail-closed）**；「开工」不隐含外发同意，必须单独问、单独记录到任务简报「外部服务同意记录」段

**Phase 0 必确认范围（缺一不可，全部确认后才可进入 Phase 1）**：
- 主题 / 篇幅 / 目标受众 / 目标语言
- 定题四态确认（是否启动）
- 外发同意 4 选 1（外发数据范围，写入任务简报「外部服务同意记录」段）
- 将创建的文件清单（主人过目确认）

**4 选 1（外发同意）选项**：

> ⚠️ **隐私后果告知（v2.6.6 新增，必读，回应审计「知情同意缺后果警告」）**：以下任一选项同意外发，意味着**未公开草稿全文 / 专有研究 / 访谈材料的实质内容会离开本机**，发往的第三方 provider（Tavily / OpenAI / Google / 模型厂商等）可能按其隐私政策留存、加工这些内容，且不受本地控制。敏感内容一旦发出无法撤回。选择前请确认：这份稿子若被第三方留存，可接受吗？拿不准 → 选 ② 或 ③。

1. ✅ **全部同意**：检索关键词 + 大模型推理全文 + 图像 prompt 全部外发
2. 🛡️ **脱敏+SVG+本地 Ollama**：脱敏措辞 + SVG 封面 + 本地推理，全文不上外部模型
3. 📌 **部分同意**：逐项勾选（□ 检索关键词 □ 大模型推理全文 □ 图像 prompt）
4. ❌ **全部拒绝**：改纯本地，不外发任何数据

**禁止**：未经 Phase 0 直接 spawn 子代理；把定题「开工」当外发同意；无外发同意记录进入 Phase 1

---

### 0 条空卡协议（v2.1.8）
**适用场景**：**T3** 案例检索员检索结果为 0 条（v2.3.0 重命名原 T6→T3）

**输出格式**：
```markdown
## [C-空] 案例检索结果（0 条）

**检索简报**：
- 检索关键词：xxx
- 检索范围：xxx
- 结果：0 条
- 原因分析：xxx
```

**后续流程**：主控读取 `[C-空]` 后告知主人，询问是否需要：
- 调整检索策略
- 主人投喂案例
- 不使用案例

---

### 三检索员并行独立运行（v2.1.8 核心架构）
**并行模式**：T1 ∥ T2 ∥ **T3** 任何量级必须独立 spawn（v2.3.0 重命名原 T6→T3）

**互不干涉铁律**：
- T1 不产数据卡/案例卡
- T2 不产文献卡/案例卡
- **T3** 不产文献卡/数据卡（v2.3.0 改 T6→T3）

**触发条件**：**T4** 分析员在 T1 + T2 + **T3** 全部完成后才 spawn（v2.3.0 改 T3→T4）

---

### 修订回环 ≤2 轮降级（v2.2.0，v2.3.1 定义细化，教训 #64 + #120）
**规则**：
- **「轮」的定义（v2.3.1 重定义）**：0 轮=主控启动 T5 出 v1；1 轮=T6 批判攻击 + T5 出 v2；2 轮=T8 主控亲修出 v3（小幅修补）；超 2 轮=结构性 P1（A/B/C）+ P0 才启 T5 v3
- **T8 小幅修补权限（v2.3.1 新增）**：≤字数 5% / AI 披露修正 / 元叙事清理 / P1-D 事实错误 → T8 亲修；结构性 P1-A/B/C + P0 → T5 独立写手
- **P1 分级（v2.3.1 新增）**：P1-A/B/C（论证补强/结构缺失/反方缺失）→ T5 重启；P1-D（引用格式/元数据/漏引/拼写）→ T8 亲修
- 第 3 轮：自动降级为"人工介入模式"（Acknowledged Limitations）

**降级原因**：
- 防止无限循环（教训 #64）
- 强制人工决策介入（主人判断是否继续/终止/调整方向）

---

### 完成验证铁律（v2.1.3，教训 #46）
**主控收到子代理完成报告后，第一动作**：

```bash
ls -lh drafts/[产物文件]  # 验证文件存在 + 非空
grep -c "关键特征" drafts/[产物文件]  # 验证内容完整性
```

**不采信报告**：T5 写手两次"报告完成但未写盘"事故触发此铁律（v2.3.0 改 T4→T5）

---

### 执行韧化协议（v2.1.0）
**四层防御**：

1. **心跳机制**：每 30 秒一次心跳 ACK
2. **分阶段 ACK**（3 档）：
   - 短任务（<3 分钟）：不需要
   - 中任务（3-8 分钟）：1 次中途 ACK
   - 长任务（>8 分钟）：多次 ACK
3. **模型健康度预检**：spawn 前 1-token ping（30s 无响应降级 fallback）
4. **超时硬卡（角色分级，v2.5.5）**：阈值见主控扩展职责 §二十二（T1-T3 10 / T4 12 / T5 15 / T6 15 / T7 12 / T9 10 / G14 8 分钟）

**Fallback 链**（4 档）：
```
deepseek-v4-pro → minimax-M3 → deepseek-v4-flash → glm-5.3
```

---

