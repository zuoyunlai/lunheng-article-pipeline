> 版本：v2.12.73（自动同步 2026-09-22）

> 🌐 **语言政策**：产出语言由 Phase 0「目标语言」字段**显式选择**（中文 / English / 中英混 / 其他，**不设默认**），全流程以该字段为准；中文特化按**目标语言客观适用**——含中文时 **G14 中文 AI 痕迹闸必跑**（v2.12.40 起不再是可选项），纯外语时记 `n/a`（客观不适用，非「关闭」）；GB/T 7714-2015 引用规范为可选能力。二者均不构成使用者语种限制。

# 性能基准表（四维）

> **用途**：论衡**实测性能数据**的唯一登记模板（v2.12.67 新建，审计 P1-2：此前只有耗时一维，无法外推重量档成本）。
> **数据来源**：每次实战后从 [`status-template.md`](../../templates/status-template.md) §4.7 Stats line 与交付说明**主人侧回填**（agent 零 exec，不自动采集）。
> **纪律**：只登记**实测**数据（禁止估算填充）；一项目一行；档位按 [`字数判定表.md`](字数判定表.md) 判定。

## 一、字段定义（列真源）

| 字段 | 口径 | 来源 |
|---|---|---|
| 字数 | **仅正文**，不含文末附录（M-14） | T8 终检实测 |
| 档位 | 轻量（2000-3000）/ 中段 / 重量（≥5000） | 字数判定表 |
| 耗时 | 主控从启动到 Phase 5 验收的总时长 | status.md 时间戳 |
| token in / out | completion event Stats line `Token usage`（**权威值**；禁用会话文本行展示值） | status.md §4.7 |
| 成本 | Stats line `Estimated cost` 加总 | status.md §4.7 |
| 引用数 | T8 终检 [Lxx] 计数 | 终检记录 |
| 修订轮数 | 计入轮（轮 1 = 3.6/3.7 批判、轮 2 = 4.2 审计；Phase 3.5 不计） | pipeline-overview「修订回环仲裁规则」 |
| 子代理数 | 实际 spawn 成功数 / 计划数 | status.md `spawn_landing` |

## 二、实测登记表

| 日期 | 项目 | 档位 | 字数 | 耗时 | token in | token out | 成本 | 引用数 | 修订轮 | 子代理 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-09-16 | 《个体化命题》实测 | 中段 | 9500 | ~2h | 未捕获 | 未捕获 | 未捕获 | — | 2 | — | Stats line 缺失（v2.12.32 口径二案例）；耗时为 README 既有单点数据迁入 |
| 2026-09-22 | 《谁有权认定你自愿？——同意门槛的“认证政治”》`consent-certification-politics-2026` | 重量（全量档） | 12721 | ~2h40m | 286k（主控） | 5.5k（主控） | ≥$0.31（主控） | 31 | 2 | 15 | 子代理 Stats line 未回传，token/成本仅主控侧精确；status.md 阶段表时间戳与产物 mtime 不一致 |

> ⚠️ **当前 2 行：1 行迁移旧单点（三维缺失）+ 1 行重量档实测（子代理侧三维仍缺失）** —— 子代理 token/成本回传缺口（v2.12.32 同型）仍未治理。下次实战须把 completion event Stats line 沿子代理完成回传路径带回主控。

## 三、外推守则（诚边界）

- 重量档已有 **1 例实测**（2026-09-22《谁有权认定你自愿？》12721 字，约 2h40m），但主控侧 token/成本精确、子代理侧三维缺失；仍**不足 3 行不同档位实测**，README「2-4 小时」保持「估计」措辞（不写「经验证」）。T1-T3 检索轮次、修订回环超限概率、M 门对账成本随字数**非线性增长**，禁止线性外推。
- 本表**不足 3 行不同档位实测前**，README / QUICKSTART 的耗时数字须保持「估计」措辞（不写「经验证」）。
- 回填后同批更新 README §预计项目时间 的指针到本表。

## 四、维护

- 新增数据 → 在 §二 加一行，**不改**历史行（含未捕获的缺失值，如实保留）。
- 本表不设行数上限（预期长期 <20 行；超限再议归档）。

## 五、主控上下文预算模型（v2.12.72 批次 2-E）

> **用途**：把 Stats line 从纯事后记账升级为 Phase 0 的事前预算与运行中预警。
> 本模型估算的是**主控会话累计 token**（主控自身 input + output），不是全体子代理总量。
> 当前只有 1 行重量档主控实测，以下预算均为**保守估计**，不足 3 个不同档位实测前不得写成经验证事实。

| 档位 | 字数范围 | Phase 0 预算起点（主控累计 token） | 预算用途 |
|---|---:|---:|---|
| 轻量 | 2000–3000 | 80k–140k（估计） | 预警基线，不作为成本承诺 |
| 中段 | 3000–9999 | 140k–260k（估计） | 预警基线；9500 字历史行无 token 数据 |
| 重量 | ≥10000 | 260k–420k（估计） | 由 12721 字项目主控 291.5k 实测校准，仍保留安全余量 |

### 5.1 运行中判定

- **余量** = `1 - 主控已消耗 token / 当前会话模型可用窗口`；若平台未提供可核验窗口值，记录 `unavailable`，不得伪造百分比。
- **偏低**：余量 `<30%`，status.md 必须追加落盘减负建议；**危险**：余量 `<15%`，除建议外暂停加载非必要全文并向主人告警。
- **超支预警**：累计主控 token `> Phase 0 预算 × 150%`，记录 `overspend_alert`；不把估算误写成精确成本，也不自动改变流水线判定。
- **校准**：每次实战只用主人侧可核验 Stats line 回填；新增实测行后再调整区间，禁止用缺失的子代理 Stats line 反推主控预算。

### 5.2 落盘减负动作（防教训 #268）

1. 已完成 dispatch 话术和中间产物只保留路径、结论与下一步，不在主控上下文驻留全文。
2. spawn 前只 read 当前角色对应的 dispatch 文件；不得从记忆复制话术。
3. 已落盘卡片按 `read` 的 `offset/limit` 局部读取；需要全文时先写明原因并记录在 status.md。

## 六、端到端活体冒烟登记（v2.12.72 批次 3-D）

> **唯一登记表**：冒烟结果必须来自真实运行期 status.md，不得用 pytest、flow-check 或自造样本替代。协议真源 = [`执行韧化协议-exec.md`](执行韧化协议-exec.md)「端到端活体冒烟协议」。

| 日期 | smoke_run_id | 级别 | 项目/档位 | spawn 落地 | 交接回传 | 审计链 | M 门对账 | status 更新 | 结论 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|
| 尚未执行 | — | — | — | — | — | — | — | — | pending_owner_run | 首次 L1/L2 冒烟须维护者/主人在场；不以纸面测试冒充活体证据 |

**回填纪律**：一行对应一次真实冒烟；失败也保留；L1 与 L2 不合并；completion Stats line 缺失、主控接管、主人决策等待均写入备注，不补估算值。

> **G 可观测性指针**：项目 `status.md` §4.9 的 `status_json` 是运行期指标快照；维护者可据 `spawn_landing`、`tokens`、`revision_rounds`、`display_cap_truncated` 聚合本表字段。缺失值保持 `null` / `unavailable`，不做反推。
