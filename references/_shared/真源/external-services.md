> 版本：v2.13.0（自动同步 2026-09-24）

> 🌐 **语言政策**：产出语言由 Phase 0「目标语言」字段**显式选择**（中文 / English / 中英混 / 其他，**不设默认**），全流程以该字段为准；中文特化按**目标语言客观适用**——含中文时 **G14 中文 AI 痕迹闸必跑**（v2.12.40 起不再是可选项），纯外语时记 `n/a`（客观不适用，非「关闭」）；GB/T 7714-2015 引用规范为可选能力。二者均不构成使用者语种限制。

# 执行前安全须知与外部服务声明

> **本文件是 SKILL.md「执行前安全须知 + 外部服务声明」段的外移正文**（Phase 0 向主人披露时读；主人可据此选择外部服务同意 4 选 1）。

## 执行前安全须知 + 外部服务声明

**文件写入警告**：

- 本流水线运行时**创建和修改文件**——主控与子代理写入 `run/<项目名>/status.md` + 项目文件树（任务简报 / 文献卡 / 数据卡 / 案例卡 / 大纲 / 草稿 / 审计报告 / 定稿 / 图件 / 证据包 / 交付说明），共约 15-25 个文件
- **🔔 心跳周期性写入**：运行期间按心跳协议（启动 + 每约 5 分钟）**仅写入自己的心跳文件** `run/<项目名>/.tmp/<两位角色号>-<角色名>-heartbeat.md`（轻量进度行，供主控监控）。`status.md` **由主控独占写入**，子代理不直接写。**披露 ≠ 同意**：该周期性写入**不因「运行本 skill」而自动生效**，须由 Phase 0 显式同意关卡放行（真源 = [`关键协议.md`](关键协议.md) §确定性同意门）；**未取得同意前不写 `.tmp/`**（fail-closed）
- 仅写入当前 workspace 根目录，**不写 workspace 外**；Phase 0 必须先列出将创建的全部文件清单让主人确认，主人同意后才开始 Phase 1（写盘）
- **<项目名> 由主人 Phase 0 确认**（**可自动命名** —— 允许 LLM 提议项目名、**不强制中文**；主人**可否决 / 改名**），命名必须满足 `[\w\-一-鿿]{1,32}`（该正则已支持中文，**保持**）

**审计反哺不自动 commit**：T7 反哺报告默认只产出 `audits/反哺报告-vN.md`，**不会**自动修改论衡 workspace 下的角色卡；任何对角色卡的改动必须由主人人工 review 后手动 merge。

**Maintainer-only 分区（user-facing 隔离，v2.12.10 起）**：论衡技能运行期使用的所有 `scripts/`、`tests/`、`Makefile`、自审门 `M-Gate-Algorithm.md` 脚本化副本、版本同步脚本、构建发布脚本均属**维护者工具**，与论衡运行时能力**无关**——运行时仅依靠 `references/_shared/` 下的 LLM 推理判定 + `read/write/edit/sessions_spawn` 编排。这些工具仅供主人（仓库维护者）本地或 CI 使用，ClawHub 净化包已剥离，运行时不会出现。

**失败回滚**：任一 Phase 失败，已写入的文件保留在 `run/<项目名>/` 供人工清理，不会自动删除。

**重要隐私提示**：

- **敏感信息**：主人提供的【项目名/主题/纲要】可能含敏感信息（如未公开研究 / 商业机密）——这些会通过外部服务发出。**如敏感请用脱敏措辞 + 本地 Ollama 推理**
- **主人投喂的一手材料**（访谈记录 / 田野调查数据 / 内部文档 / 客户信息）：投喂前主人需确认已取得知情同意，且**必须脱敏**（人名/机构名/可识别信息替换为代号）；论衡对投喂材料的存储/引用/传播不承担合规责任，**主人是数据处理的责任方**
- **封面与格式转换不在流水线内（v2.12.52 定案）**：论衡 agent **不调用** `image_generate`，也不执行任何格式转换（`rsvg-convert` / `pandoc` / ImageMagick / 任何转换 binary）。封面视觉与 docx/pdf/latex 导出、SVG→PNG 转换**不进 Phase 0 选项**，统一在 **T8 终检后**由主控写入「主人自行操作建议清单」（含命令模板 + 执行者 = 主人 host shell），由主人自行执行。图件一律用主控本地 `write` 的 SVG 矢量图（零外发、零 exec）。

**主控 Phase 0 4 选 1 明示同意**（全部同意 / 脱敏+SVG+本地 Ollama / 部分同意 / 全部拒绝——**fail-closed：无有效选择记录 = 未同意 = 不得进入 Phase 1**），写入 `01-任务简报.md`「外部服务同意记录」段作为审计追溯依据。

**检索层口径（区分「默认启用」与「默认关闭」两类）**：

> **本表是「外发服务类别」的唯一真源（共 3 类）**：SKILL.md / `permissions.md` / 任务简报模板 / `关键协议.md` 一律**引用本表**，**不得各自重列**——重列必然漂移（历史上曾出现 4 份互斥清单：本表类别数与 SKILL.md / Opt-in 行 / frontmatter / description 各自不同）。
>
> ⚠️ **本表只是「服务类别」轴**：「**大模型推理全文**（草稿/卡片/大纲全文 → 当前模型 provider）」是**另一条轴（数据形态）**，且是最敏感的外发项，**不在本表内**、不可用本表推导。两轴合见 [`关键协议.md` §4 选 1](关键协议.md) 与任务简报模板 §0。

| # | 类别 | 工具 / 服务 | 默认状态 | 离开本机的数据 |
|---|------|-------------|---------|--------------|
| ① | 检索（默认层）| `web_search` / `tavily_search` / `web_fetch` / `tavily_extract` | **默认启用**（Phase 0 告知并可拒绝）| 检索关键词 + 目标 URL |
| ② | 学术元数据（**opt-in**）| OpenAlex / Crossref | **默认关闭**（Phase 0 显式勾选才用；只读公开 API，无需 Key）| 仅检索关键词（不发送个人信息/机密数据）|
| ③ | 抓取层（可选）| Firecrawl（paper.edu.cn） | **默认关闭** | **抓取目标页面内容 + 请求元数据（URL / 检索词）→ Firecrawl 第三方服务**（受其隐私政策约束）；凭据须在宿主环境外部配置 |

不调 Firecrawl（除非主人 Phase 0 显式勾选启用）。**⚠️ 启用即第三方外发**：抓取的**目标页面内容与请求元数据（URL / 检索词）会发往 Firecrawl 第三方服务**，受该服务隐私政策约束；**未公开草稿 / 客户机密 / 未脱敏材料不得经此路径**。**原第二梯队（万方 / 科情 / NSTL）已于本修订取消**（需 API key + 申请/付费/机构门槛，长期未被使用）。详见 [`references/_shared/真源/中文数据源集成.md`](中文数据源集成.md)。

**🔒 不读取宿主网关配置（精确口径）**：论衡运行**不调用** `gateway` / `config` / 任何宿主配置读取工具，**不读取** `~/.openclaw/openclaw.json` 等宿主配置文件——主控 documented 工具集无 gateway / config / agents_list / cron / message 类工具。宿主可单独配置 gateway 限权访问，与论衡运行无关。**无例外**（v2.12.13 起删除原「spawn 前配置核对」例外：论衡在任何阶段都不读宿主配置）。

**🧭 两层别混：「宿主配置」与「自身会话工具面」是两件事（v2.12.57 消歧义）**：
- **宿主配置文件 / 网关配置 → 不读取、不校验、不修改、不告警**：论衡不读、不改、不校验宿主配置，也**不就宿主配置状态**做阻断、告警或「建议收紧」（配置配方随宿主版本演进，写死在 skill 里必然过期）。
- **当前会话自身可见的工具面 → 做非侵入式自检 + 披露（本技能明确在做的事）**：主控在 Phase 0 核验自身可见工具是否超出 `metadata.tools` documented 集；子代理在每次 spawn 首步按「计数 + 高危类别具名」报告工具面。超限项与越权调用写入 `status.md`「能力自检」段并**向主人披露**。该自检只用 **agent 环境内已有信息**，不读任何宿主机文件。

⇒ 两条并存不矛盾：**对宿主配置不读取 / 不告警；对自身会话工具面做自检 + 披露**。自检细则真源见 [`permissions.md`「能力自检」](../../permissions.md) 与 [`dispatch-header.md`「启动自检」](dispatch-header.md)。**本段是「宿主配置 vs 会话工具面」口径的唯一真源**，其他文件只引用不重列。

**🌐 平台下发面披露（v2.12.74 F1，回应审计「外发 3 类窄于实际外发面」）**：
- **事实**：平台给 worker 下发的工具面**宽于**本 skill 声明（平台不解析 `metadata.tools`；2026-09-20 实测 audit 档可见 71 项 vs 声明 1 项）。宽出面含**未列入下表 3 类的外部服务工具族**：飞书协作写面（`feishu_*`）、Firecrawl 深度抓取与站点交互（`firecrawl__*`）、记忆与知识库（`memory_*` / `ov_*` / `wiki_*`）、插件与技能安装（`plugins` / `add_skill`）等。
- **论衡的承诺口径**：上述工具族**全部已进 `metadata.tools.denied` 声明**（104 项），论衡运行期**一个都不调用**——worker 的实际外发面**以本表 3 类 + 模型推理（轴 A）为上限**，不因平台下发面宽而扩大。
- **Phase 0 披露动作**：主控向主人披露「平台实际下发面宽于声明」这一事实（按「计数 + 高危类别具名」，不逐条枚举），并声明上述不调用承诺。**这不是新的同意项**（同意门仍只管 3 类 + 轴 A）；它是诚实披露——防止主人误以为「worker 只拿得到 3 类外发工具」。
- **强制力边界**：`denied` 声明是自律；唯一机械拦截位置 = 宿主 `tools.subagents.tools.deny`（宿主职责，论衡不附带配方）。

**主人拒绝任一外发项** → 主控调整方案并重做 Phase 0 确认。

---
