# 论衡「计数单一真源」（v2.13.x 审计修订 R-19）
#
# 背景：整族计数散落全仓（「G14 9 类」多处、「13 类常见错误」、「24 中文 + 12 英文」、
#   「dispatch 11 文件」等）。此前靠**人记住 N 个地方**同步 —— 2026-09-25 审计实测已出现
#   整族漂移：同为错误类数出现 12 与 13 两种写法；期刊总数出现 37 与 36 两种写法。
#
# 口径：本文件是**唯一计数真源**。文档可复述数字，但必须与本文件一致 ——
#   由 tests/test_count_drift.py 机械扫描全仓（白名单 = CHANGELOG* / archive/ / reports/）。
#   改数只改这里，再跑 `pytest tests/test_count_drift.py` 让机器列出所有待同步位置。
#
# 维护：本文件**不带版本戳**（非版本化声明文档，避免 check-version 清单无谓扩张）；
#   新增/删除本文件须同步随包清单（维护者侧清单文件，见构建脚本目录）。

# G14 中文 AI 痕迹闸（真源：references/gates/14-中文AI痕迹-gate.md）
g14_classes: 9
# 「防御性写作」是第 9 类（v2.13.x 起为 9 类；此前为 8 类）
g14_defensive_writing: true

# 错误友好化对照表（真源：references/errors.md）
errors: 13

# 内置期刊数据库（真源：references/_shared/真源/期刊数据库.md）
journals:
  zh: 24
  en: 12
  total: 36

# 派发话术文件数（目录 references/dispatch/）
dispatch_files: 11

# 角色
role_cards: 10        # 概念口径：T0 主控 + T1-T7 + T8 终检 + T9 同行评审
role_card_files: 11   # 物理文件数：主控拆 coordinator + 扩展职责两张卡

# T8 终检可发表性判据（真源：references/_shared/真源/可发表性判定表.md）
t8_items: 48
t8_publishable_dimensions: 6

# T9 同行评审维度（真源：references/dispatch/T9-同行评审.md）
t9_dimensions: 6

# M 门项数（真源：references/_shared/真源/M-Gate-Algorithm.md）
m_gate_items:
  form: 8
  exist: 3
  integrity: 2
  total: 13
