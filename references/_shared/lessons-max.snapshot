428
# ─────────────────────────────────────────────────────────────────────────────
# 【2026-09-19 更新记录】423 → 425：主真源续录 #424（裸 grep/awk 不认围栏）
#   + #425（trigger 脚本 exec 大 JSON 静默不报）+ #426（本链，围栏错位偶数必要不充分）。
#   三条均属论衡类（工程/发布实践）。
#   同批改动：本值 + 索引声明（教训索引.md「当前最大编号」）。排除表**未变**（无新增宿主类）。
# 【2026-09-19 修正记录】425 → 426：上一批同批续录了**三条**论衡类条目（#424/#425/#426），
#   本值却只提升到 #425 —— off-by-one 漏计 #426。自审门 H 的参照告警
#   「外部真源 #426 > 快照 #425」指向的是**本值该动**，不是门该放宽
#   （本文件「未列入排除表者一律按论衡类计入」是刻意保守，不放宽）。
#   本批：本值 425 → 426 + 索引声明（当前最大编号 / 论衡类最大编号）同批对齐；排除表**未变**。
# 【2026-09-19 更新记录】426 → 428：主真源续录 #427（结构类判据限定单文件 ⇒ 同类漏检）
#   + #428（门存在 ≠ 门跑过：升版号未跑全量测试）。两条均属论衡类（审计方法 / 工程实践）。
#   同批改动：本值 + 索引声明（§一 / §二 / §三 三处）+ 新增 §三点十 分类补记；排除表**未变**。
# 论衡类条目最大编号（**棘轮：只升不降**）
# ─────────────────────────────────────────────────────────────────────────────
# ⚠️ 第 1 行必须是**裸数字**（反向差集门用 `grep -oE '[0-9]+' | head -1` 解析）。
#
# 🌐 本文件为维护者侧中文运维快照；产出语言由 Phase 0「目标语言」字段
#    显式选择（不设默认），本文件语言不构成对使用者的限制。
#
# 【语义】最近一次核对「主真源」（维护者侧文档，不随包交付）时，论衡类条目的最大编号
#   —— 已扣除排除表中的宿主/通用类条目。
#
# 【为什么需要它】反向差集判据 = 本值（仓库内，hermetic），不依赖仓库外真源 ——
#   否则「已发布的绿」会被墙外追加追溯性推翻。
#
# 【更新时机】任一命中即须**同批**更新本值：
#   ① 主真源新增论衡类条目（编号 > 本值）；
#   ② 索引「当前最大编号」声明变化；
#   ③ 新增宿主/通用类条目 → 走排除表，**不推高本值**。
#
# 【同批铁律】本值 + 索引声明 + 排除表 **三者同批改**，不提交中间态。
#
# 【提醒器】参照告警「外部真源编号 > 快照编号」响起 = 本值该更新了。
#
# 【自证】更新后跑自审门，应见「索引最大编号未落后仓库快照」且无参照告警。
#
# 【只升不降】棘轮语义——本值只允许增大。
# ─────────────────────────────────────────────────────────────────────────────
