354
# ─────────────────────────────────────────────────────────────────────────────
# 论衡类教训最大编号（**棘轮：只升不降**）
# ─────────────────────────────────────────────────────────────────────────────
# ⚠️ 第 1 行必须是**裸数字**（门 H 用 `grep -oE '[0-9]+' | head -1` 解析；数字若不在首位会取错）。
#
# 【语义】最近一次核对主真源（`memory/lessons.md`）时，**论衡类**教训的最大编号
#   —— 已扣除 `LUNHENG_LESSON_EXCLUDE` 中的宿主/通用类（当前默认 340 / 341 / 355）。
#
# 【为什么需要它】门 H 的反向差集判据 = 本值（仓库内，hermetic），**不再依赖仓库外真源**——
#   否则「已发布的绿」会被墙外追加追溯性推翻（实测：宿主类教训 #355 续录后 HEAD 由绿转红）。
#
# 【更新时机】任一命中即须**同批**更新本值：
#   ① 主真源新增**论衡类**教训（编号 > 本值）
#   ② 本索引（教训索引.md）「当前最大编号」声明变化
#   ③ 新增**宿主/通用类**教训 → 把它加进 `LUNHENG_LESSON_EXCLUDE`（**不推高本值**）
#
# 【同批铁律】本值 + 索引「当前最大编号」+ 排除表 **三者同批改**，不提交中间态
#   （教训 #352：半成品版本戳 = 带红门）。
#
# 【提醒器】门 H 的参照告警「⚠ 门 H: 外部真源 #N > 快照 #M」响起 = 本值该更新了。
#
# 【自证】更新后跑 `bash scripts/self-audit-gate.sh`：应见
#   `✓ 门 H: 索引最大编号未落后仓库快照（索引 #N ≥ 快照 #N）` 且**无**参照告警。
#
# 【只升不降】棘轮语义——本值只允许增大；「教训编号不回收」是铁律（编号不可重用）。
#   若确需下调，须在 CHANGELOG 显式说明理由。
#
# 详见 `references/_shared/教训索引.md`「快照更新纪律」段。
# ─────────────────────────────────────────────────────────────────────────────
