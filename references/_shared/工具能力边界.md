> 版本：v2.12.52（自动同步 2026-09-17）

> 🌐 **语言政策**：产出语言由 Phase 0「目标语言」字段**显式选择**（中文 / English / 中英混 / 其他，**不设默认**），全流程以该字段为准；中文特化按**目标语言客观适用**——含中文时 **G14 中文 AI 痕迹闸必跑**（v2.12.40 起不再是可选项），纯外语时记 `n/a`（客观不适用，非「关闭」）；GB/T 7714-2015 引用规范为可选能力。二者均不构成使用者语种限制。

## 五、工具能力边界

> **能力边界声明层**：[`SKILL.md`](../../SKILL.md) frontmatter `metadata.tools`（`base` / `coordinator_only` / `research_extra` / `opt_in` / `denied`）（按版本同步）。**平台不解析 `metadata.tools`** —— ⚠️ **无官方依据**（推断：加载器只消费 `metadata.openclaw` 的**已知 gating 键**；官方 `docs/tools/skills.md`「Gating」节仅枚举 `metadata.openclaw` 的已知键，**从未声明未知子键的处置语义** —— 全库 `grep -rn -i "unknown key|unrecognized key|ignored" docs/tools/skills.md` **0 命中**）。故本层为**声明与部署参考**，非机械强制；真实工具面由宿主 config 决定。本节列项供快速查阅，数量可能随 SKILL.md frontmatter 变化。

### ✅ 可以使用的工具（数量以 SKILL.md frontmatter 为准）
- **文件操作**：read / write / edit
- **Web 检索**：web_search / web_fetch / tavily_search / tavily_extract
- **子代理编排**：sessions_spawn / sessions_yield / sessions_history / subagents（仅限本技能 spawn 的子代理；sessions_history 仅读 self-spawn 子代理历史）
- **规划**：progress_card / session_status

### ❌ 不可以使用的工具

> **唯一真源 = `SKILL.md` frontmatter `metadata.tools.denied`**（**本文件不重列** —— 重列即漂移风险；**逐项清单与数量一律以 frontmatter 为准**）。
> 旧版在此手抄过一份清单，审计实测与 frontmatter **不逐项等价**：既有重复列项（`memory_forget` / `sessions_search` / `sessions_send` 各出现两次），也有漏项（缺 `code_execution` / `automations` / `message` / `gateway` / `secrets` / `sessions` / `conversations_send` / `conversations_turn` / `screen` / `canvas` / `show_widget` / `agents_list` / `get_goal` / `create_goal` / `update_goal` / `suggest_task` / `dismiss_task` / `heartbeat_respond` / `x_search` / `pdf` / `view_image` 等），故**整体删除，只留指针**。同口径见 [`permissions.md`](../permissions.md)「禁用（`metadata.tools.denied`）— 不在此重列」。

### ℹ️ 关键澄清

> **诚实声明唯一真源 = [`M-Gate-Algorithm.md`](M-Gate-Algorithm.md) 顶部「诚实声明」段**（LLM 推理模拟、不执行 shell、零 exec）。本节仅索引，不重列结论。
- **M 门算法**：主控 LLM 通过 `read` 读取算法文档，按伪代码**推理判定**
- **不执行 shell**：算法文档中的 `sha256sum`/`grep`/`awk`/`comm` 命令仅供人类主人手动验证
- **纯推理流水线**：论衡是 LLM 推理 + 文件读写 + Web 检索，没有任何 shell 执行能力

---

