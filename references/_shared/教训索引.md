> 版本：v2.12.35（自动同步 2026-09-13）

# 论衡教训索引

> **用途**：集中索引论衡教训（#11-#334）的**主题分类 + 所在文件**。避免「教训散落 192 处引用、找不到某条教训在哪」的问题。
>
> **原则**：索引只做**主题分类 + 编号范围**，不做逐条详解（逐条详解会随版本迭代快速过期）。逐条内容在对应文档的「教训」标注处。
>
> **教训真源**：主工作区 `memory/lessons.md`（论衡实战教训主文件，当前最大编号 **#354**，2026-09-12 补录 #342-#354（#340/#341 属宿主/通用类，不入本索引），其中 #315 空号未使用）。论衡 SKILL 内只做**引用**，不复制内容（教训 #143，双视图分层发布原则）。
>
> **编号体系统一（2026-09-09，方案 A：全面对齐主真源）**：原论衡侧独立续号与主真源撞号/缺失，已统一——主真源自身撞号重编号 #258-#261（汇报幻觉/子会话回执/Git tag/纵深防御），论衡侧独有教训补录 #262-#287 + #288-#295（原论衡侧 #35/#43/#46-#48/#51/#56-#58/#60/#64/#77-#79/#82-#84/#86/#102/#116/#120/#128/#130-#134/#142/#210/#234/#235），撞号改指向（#190→#258/#259、#191→#259、#194→#192），候选 #225→#287。
>
> ⚠️ **双侧同步纪律**：论衡侧新增教训编号时，必同步回写主真源 lessons.md。v2.5.5/v2.5.6 时 #153-#178 只写了角色卡、主真源停在 #136，造成 28 条缺档（教训 #150，在记忆层的同型问题）。
>
> 📌 **快照更新纪律（2026-09-13 官方复查补）**：反向差集的判据是仓库内快照 [`lessons-max.snapshot`](lessons-max.snapshot)（**hermetic**，不受主真源漂移影响）。本文件「当前最大编号」与快照值**绑定同源**，且 **+ 排除表 `LUNHENG_LESSON_EXCLUDE` 三者同批改**（教训 #352，不提交中间态）。**语义 / 更新时机 / 只升不降 / 自证**完整条文见该快照文件头；门 H 的参照告警「⚠ 外部真源 #N > 快照 #M」就是提醒器。

## 一、教训分类速查（按主题）

| 主题 | 教训编号 | 核心结论 | 主要所在文件 |
|------|---------|---------|------------|
| **改 A 漏 B** | #36, #289, #269, #91, #118, #118.1, #127, #175 | 文档同步漂移是论衡头号死敌 | 自审门 / SKILL.md / 主控卡 |
| **自审工具假绿灯** | #150, #151 | 自审工具「报绿」比「报红」危险 | lessons.md |
| **M 门算法 bug** | #272, #291, #292, #293, #295, #139 | 算法 bug 全靠实战打出来 | M-Gate-Algorithm.md |
| **执行层脆弱** | #22, #288, #263, #65, #274, #103, #105, #119, #126, #278, #279, #282, #153, #154, #259, #193, #192, #285, #236 | 子代理写盘失败 / 假在线 / 超时 | 主控卡 / 写手卡 / 审计卡 |
| **数据信任** | #34, #44, #271, #290, #106, #107, #277, #129, #280, #137, #139, #258/#259 | 信任级别 / 字数口径 / 二手转引 | glossary / 字数判定表 |
| **角色独立性** | #273, #276, #281, #283 | 批判伙伴 / 修订轮独立写手 | 写手卡 / 主控卡 |
| **安全合规** | #265, #266, #123, #143, #147, #148, #149, #200, #202, #284, **#296**, **#297**, **#298**, **#306** | 零 exec / 净化包剥离主控侧运维痕迹（教训引用/真源/路径铁律）/ 剥离后残留 + 剥后收口（空格·标点）/ verdict clean≠无 findings / **平台开递归委派致架构假设静默失效（叶子纪律 + 最小权限 + 诚实标注不声称机械强制）** | SKILL.md / build 脚本 / `scripts/strip-internal-leakage.sh` / `scripts/strip-shell-commands.py` / `references/_shared/关键协议.md` |
| **人在环** | #262, #263, #264, #138, #158 | 四节点 / 洞察补充 / 主控签字 | 主控卡 / pipeline-readme |
| **信任声明** | #177 | M 门「机械化」名不副实 | M-Gate-Algorithm.md |
| **工程纪律** | #135, #175, #176, #177, #180, **#184**, **#195**, **#286**, **#238**, **#241-248**, **#249**, **#251-#255**, **#299** | 自审门脚本化 / 候选池描述化 / M 门诚实化 / 教训编号双侧同步（门 H）/ SVG 内嵌文本视为事实层 / sync 配置 + 门 C 清单联动必同步改 / **论衡 v2.11.0 实战驱动 8 条新教训（T 编号/G14 名实/Warning 默认/双拍板/regex 三轮/T8 真 P0/T8 可发表性/T8 图表）** / **机械化硬门被动失效：修订后必跑硬门三件套 + 门 H 扫裸 #N 格式** / **GB/T 7714 顺序编码闭环 = 真合规（vs 类型标识齐全 = 假合规）/ 保障必须落在机器可执行层 / 论衡 v2.12.0 架构修订落地（判据单源 + 本地脚本 + 双视图同步）/ 无效表格分隔行全库扫描（门 O）** | self-audit-gate.sh / SKILL.md / M-Gate-Algorithm.md / sync-version.sh / 任务简报模板 / checkpoint-card 模板 / T8 dispatch / 08 角色卡 / 可发表性判定表 / paper-ready-check.sh |
| **净化链与发布** | #296, #297, #298, #300, #301, #302, #307, #311, #314, **#329**, **#330**, **#333**, **#350**, **#351**, **#352**, **#353**, **#354** | 净化链缺陷（剥离残余 / 泛化占位符 / 收口 / 死规则）+ 发布对象核验（tag≠Release / 审计报告 / changelog 计数）+ **扫描报告「中间态」不可当结论（结论前读 manifest 完成时间 + 字段完整性断言）** / **回填式维护路径必须幂等（章节尾部分隔行随切分混入 → 每次重跑累积）** / **未跟踪文件不得入净化包（记忆落盘按 cwd 相对解析 → 写进被发布仓库；排除清单默认方向错）** / **发布上传慢 + 查询工具读缓存：管道缓冲掩盖进度（外层超时 >15min 且必须落盘日志）/ 「读不到」≠「不存在」（须**逐版本/标签直查** + `publish already exists` 交叉；**清单类查询也会滞后**） / **半成品版本戳（版本戳与版本记录须同批，不提交中间态）/ 扫描器 fail-closed 建议与实测必需行为正面冲突（只能消歧义、不能消除，须披露未消项） / changelog 缺省即平台自动生成（同号不可重发回改，发布前必须显式传；dry-run 不返回该字段不可断言）** | `scripts/strip-shell-commands.py` / `scripts/strip-anchor-residue.py` / `scripts/build-clawhub-release.sh` / `scripts/changelog-check.py` / `scripts/publish-clawhub.sh` / lessons.md |
| **平台与环境事实核验** | #305, #306, #308, #309, #310 | 平台升级会静默改物理路径与默认策略；路径 / 工具 / 状态 / 摘要一律**现场核验**，不靠印象 | SKILL.md / `references/permissions.md` / `references/_shared/关键协议.md` / 真源路径记载 |
| **发布与版本纪律** | #320, #321, #322, #323, #324, #325, #326, #327, #328, **#332**, **#334** | 定案不得砍头号能力 / 规则只防手写不防自己写 / 自称唯一真源须自证计数 / 产物目录不得同树重建 / 宣布完成前清点并行子批次 / verdict≠findings 两个对象 / 脚本成功退出≠按请求执行 / 替换式净化只抹字面不抹概念 / **扫描报告的缺陷文件归属不可信（全仓搜语义点位 + 反向断言）** / **并发会话链下的抢发版（发版前置闸 = 两查一停：在飞链 / 编号占用 / 工作区干净）** / **拒绝式闸门与调用点时序错配致「上线即自锁」（② 编号占用口径只在分配新号之前成立）** | lessons.md / `scripts/build-clawhub-release.sh` / `scripts/self-audit-gate.sh` / `scripts/sync-version.sh` / `scripts/release-preflight.sh` / `scripts/create-github-release.sh` |

## 二、教训编号 → 文件快速定位（高频教训）

| 教训 | 一句话 | 定位 |
|------|--------|------|
| #262 | 视角一致性 + 数据无源 + 图注精度（原创性悖论实战） | 写手卡「视角与精度铁律」 |
| #263 | 完成验证铁律（T4 v3/v4 报告完成但未写盘） | 主控卡 §十二 |
| #264 | 文末四节完整性闭环 | 写手卡铁律 9 |
| #265 | 禁止 sed -i（静默清空文件） | SKILL.md 启动清单 6 |
| #266 | Phase 0 同意关卡金标准 | SKILL.md / glossary |
| #267 | T3 案例检索任何量级必 spawn | 主控卡 §四 |
| #268 | 派发话术实时读取不复制 | SKILL.md 启动清单 4 |
| #269 | 全文 grep 关键词矩阵（版本升级 SOP） | 自审门 |
| #270 | 修订回环 ≤2 轮硬约束 | 写手卡铁律 10 |
| #271 | 数据信任级别 + 阶段闸门 | glossary §三 |
| #272 | M-Form-3 算法 bug（临时编号残留） | M-Gate-Algorithm.md |
| #273 | T6 批判伙伴独立攻击 | 写手卡铁律 11 |
| #274 | 编排循环防空转（T8→T5 duplicate 事件） | 主控卡 §二十三 |
| #275 | 角色编号重构（T6→T3 等） | SKILL.md / 自审门 |
| #118.1 | 版本升级自审门三处同步 | sync-version.sh 头部 |
| #276 | 修订回环定义细化 + T8 小幅修补权限 | 主控卡 §二十二 |
| #123 | ClawHub scanner UI 隐藏 findings | SKILL.md |
| #277 | 字数口径 = 纯中文字符（禁 grep 一-龥） | 字数判定表 |
| #279 | 硬卡放宽（T6/T7 12-15 分钟） | 主控卡 §二十二 |
| #138 | 人在环四节点（Phase 3.6 非人在环） | 主控卡 §二十一 |
| #283 | 修订二分类（T7 打回 vs 主控触发） | 主控卡 §二十二 |
| #143 | 不为过 scanner 阉割核心能力（双视图发布） | build-clawhub-release.sh |
| #147 | verdict clean ≠ 无 findings | SKILL.md |
| #150 | 自审工具假绿灯比红灯危险 | lessons.md |
| #151 | 退出码与 verdict 必须同源 | lessons.md |
| #153 | T5 写盘后自检 | 写手卡 step 11.bis |
| #154 | 硬卡阈值机械化执行（不准「再等一下」） | 主控卡 §二十二 |
| #155 | T5 M 门子自检 | 写手卡 step 11.ter |
| #156 | 字数判定层级 | 任务简报模板 |
| #157 | 缺口论点标记 + 图号 = 章节顺序 | T4 卡 / pipeline-readme |
| #158 | Phase 3.5 三具体问题选项 | pipeline-readme |
| #159 | T4 §6.3 未来方向三件套 | T4 卡 |
| #160 | T5 素材按需加载硬步骤 | 写手卡 |
| #161 | archive 保留策略配置化 | 任务简报模板 / 主控卡 §二十五 |
| #162 | T7 反哺报告为硬步骤 | 审计卡 |
| #163 | 跨学科理论概念核验（catharsis 类） | 写手卡铁律 16 |
| #164 | AI 味 grep 自抓前移 | 写手卡 step 11.bis |
| #165 | G14/T6 时序澄清（串行非并行） | pipeline-readme |
| #166 | status.md 结构化重写（表格漂移） | status-template |
| #167 | T2 学术计量检索方向（人文主题） | 数据检索卡 |
| #168 | T9 扩写建议 + B 类修订协议 | 审稿卡 / 主控卡 §二十六 |
| #169 | M-Exist-2 sha256 重命名为证据包完整性 | M-Gate-Algorithm.md |
| #170 | AI 使用披露一句话简化 | 写手卡铁律 12 |
| #171 | 先行者文献「本文差异化」段 | 写手卡铁律 9 |
| #172 | 多格式导出主动呈现 | pipeline-readme / 主控卡 |
| #173 | 中文数据源集成「没地方选」 | 任务简报模板 |
| #174 | 中文数据源集成派发话术补全 | T1/T2/T3 卡 |
| #175 | 自审门从文档变脚本 | self-audit-gate.sh |
| #176 | 候选池描述化（去硬编码模型 ID） | SKILL.md |
| #177 | M 门「机械化」诚实声明 | M-Gate-Algorithm.md |
| #180 | 教训编号引用 vs 主真源差集（编号层漏写不报错）| self-audit-gate.sh 门 H |
| #241 | T 编号命名错位（实战）| 任务简报模板（Phase 0 命名规范）|
| #242 | G14 8 维名实分离（实战）| G14 task template |
| #243 | G14 Warning 无默认项| G14 判定规则 |
| #244 | Phase 3 收口两拍板并行 | 编排机制 |
| #245 | T7.5 主控亲为 regex 三轮返工 | T7.5 SOP |
| #246 | T8 主控亲为发现真 P0：附录漏映射 | T8 dispatch / 角色卡 |
| #247 | **T8 主控亲为清单覆盖「可发表性」全维（实战主人终审打脸）** | **T8 dispatch § 15-18 + 08 角色卡 + 任务简报模板 + checkpoint-card 模板 + 修订说明模板 + SKILL.md v2.11.1 段** |
| #248 | **T8 主控亲为清单覆盖「图表」（实战主人反馈）** | **T8 dispatch § 19 + 08 角色卡 + 任务简报模板「可发表性 E」+ checkpoint-card 模板** |
| #249 | **机械化硬门被动失效：修订后必跑硬门三件套 + 门 H 扫裸 #N 格式（修订）** | **sync-version.sh / self-audit-gate.sh 门 H / 版本升级自审门文档** |
| #251 | **GB/T 7714 顺序编码闭环 = 真合规（vs 类型标识齐全 = 假合规）**——网络女权论文 v5 整改实战驱动。T8 国标引用应内置顺序编码验证器，不靠 grep 类型标识数量 | **可发表性判定表 § 维度 4.3 + paper-ready-check.py::check_citation_ordering** |
| #252 | **保障必须落在机器可执行层，不是 SKILL.md 散文**——主人 v2.11.0 实战复盘洞察：v2.11.1 散文补丁越多越漏，T8 主控亲为仍漏检；v2.12.0 必修 = 判据单源 + 脚本化 | **可发表性判定表 + paper-ready-check.sh + SKILL.md v2.12.0 段** |
| #253 | **论衡 v2.12.0 架构修订落地**——判据单源化（48 项 6 维度）+ 本地脚本（paper-ready-check.sh）+ 双视图同步（ClawHub 发布版剥代码块）+ 实战回测发现 6 项真实质量门缺失 | **可发表性判定表 + paper-ready-check.{sh,py} + strip-shell-commands.py + build-clawhub-release.sh** |
| #296 | **净化包剥离主控侧运维痕迹（铁律）**——论衡 v2.12.4 净化包发现 372+ 处教训引用、4 处文件名引用、2 处 GitHub 真源暴露；脚本迭代 7 轮清干净（处理 14 类形态正则 + 代码块内清理 + 表格行处理 + 全角/半角括号兼容） | **`scripts/strip-internal-leakage.sh`** |
| #297 | **净化包「剥离后残留」三类 + 清理链路内建到构建**——剥离字面≠剥离语义：裸编号锚点 / 悬空标点 / 版本 pin 与真源；build 顺序改「追加→清理→最终扫描」，扫描不通过 exit 1 | **`scripts/strip-anchor-residue.py` + `scripts/build-clawhub-release.sh`** |
| #298 | **净化包「格式残迹」：剥离动作本身就是新缺陷源**——替换/删除 token 后周边空白必须同批收口：中文字间空格（`双向 diff`→`双向 对比`）/ 括号内孤立空格 / 加粗标记空格；**收口规则必须同时覆盖正文与代码块两个分支**（dispatch 正文包在围栏内），且加粗规则必须带入界（`>` 不入类，否则吞闭标记后空格） | **`scripts/strip-shell-commands.py` + `scripts/strip-internal-leakage.sh`** |
| #299 | **无效表格分隔行 `| | |`：同类源缺陷只修单点不扫全库**——GFM 要求分隔行只含 `-` `:` `|`，`| | |` 让整表不渲染；上一轮已修过 QUICKSTART 同型缺陷却漏扫全库（errors.md 12 处）；落地为**自审门 O** 全库扫描 | **`references/errors.md` + `scripts/self-audit-gate.sh` 门 O** |
| #306 | **平台开递归委派 = 既有架构假设静默失效**——平台默认开启有界递归委派，子代理实际拿到 `sessions_spawn`/`subagents`/`sessions_list`/`sessions_history`，而论衡假设「角色卡 = 叶子 worker」（主控只接直接子级）；孙辈产物不上传主控 = 产出静默丢失 + token 已花。落地：叶子纪律三层（角色卡/常见协议 + 任务书声明 + 派发头声明）+ 工具面「三层→四层」文档修正 | **`references/_shared/关键协议.md` §叶子纪律 + `references/permissions.md` + `references/_shared/dispatch-header.md` + 角色卡 T1-T7/T9 + `00-主控-扩展职责.md` §三 + `status-template.md`**；v2.12.10 曾收紧为「强制二选一」，**v2.12.13 已回退为声明式**（平台安全扫描标记为高危：skill 不得核验宿主前提） |
| #22 | 失败互不干涉（三角验证降级隔离） | 01-文献检索卡「失败互不干涉」/ 02-数据检索卡「降级隔离」 |
| #34 | 强相关性原则（防材料堆砌，反向淘汰自查） | SKILL.md / T2-数据检索卡「强相关性铁律」 |
| #44 | 「据行业经验估算」限定使用条件 | 00-主控卡 / 00-主控扩展职责 §十八 |
| #65 | 集成测试 > 组件测试（组件能跑 ≠ 系统能用） | M-Gate-Algorithm-appendix.md / 00-主控扩展职责 §七 |
| #103 | M-Integrity-1 时序（大纲是 T3 产物，不能 T2 后即查） | errors.md「大纲检查时序矛盾」 |
| #105 | 任务简报只写需求，不预转述数据内容 | 00-主控扩展职责 |
| #106 | 声明-计数一致性（头部总条数 = 实际 grep 计数） | T2-数据检索卡 / 数据卡-template |
| #107 | 作者必填禁「待核」占位 + 引用模式锁定 | T1-文献检索卡 / 任务简报-template |
| #127 | README 内部 Phase 3.5/3.6 冲突 + 角色编号重构合并修复 | lessons.md（主控侧） |
| #278 | memory 工具降级提示（schema 失配 → 显式提示 + 文件 fallback） | 00-主控扩展职责 |
| #280 | 洞察-数据一致性预检（主人洞察引文需回查可检索性） | 任务简报-template |
| #281 | C7 一处两用识别（同证据两论证维度） | T6-批判卡 / 06-批判卡 |
| #282 | 主控终检亲核对「实际跑过的模型链」（含 fallback 记录） | 00-主控扩展职责 |
| #135 | 实战教训沉淀建议（项目级 → 工作区级两级沉淀） | 00-主控扩展职责 |
| #137 | 引用格式与模式强绑定（禁「编号+内联」混合） | 任务简报-template / 可发表性判定表 |
| #258/#259 | 不转报假数据（编造 + 采信同型） | 00-主控扩展职责 |
| #259 | 任务书 vs 写盘 diff 门 | T5-写手卡 / 00-主控扩展职责 |
| #193 | 排版残迹门 | 05-写作卡 |
| #192 | Token 消耗精确统计（三级降级废弃） | deliverables.md / status-template |
| #200 | H1/displayName 修复 | README.md |
| #202 | 子代理工具面 = 平台硬剥 + 主控策略快照 − 宿主 deny | permissions.md |
| #284 | 路径校验 + 人在环介入 | 路径校验规范.md |
| #285 | 子代理错误码语义分层（资源/认证层错误 vs 产物缺失） | 00-主控扩展职责 §十二点五 / dispatch-header.md |
| #286 | M 门执行协议（每 phase 末尾强制跑 + 写 status.md 记录） | 00-主控扩展职责 / status-template |
| #236 | 真实可用性（存在性 + 动态 1-token ping 双检查） | status-template |
| #238 | 主控必读文档清单 | 00-主控扩展职责 §〇 |
| #254 | 论衡 v2.12.0 修订收尾（教训索引过期 + 硬门三件套扩四件套 + 实战回测捕获 6 项真实缺陷） | lessons.md / SKILL.md |
| #255 | cwd_default 陷阱（skill 的 cwd 相对 skill 目录解析，不是 workspace 根） | SKILL.md frontmatter / permissions.md |
| #256 | token 统计「假前提」（sessions_spawn 返回值无 stats，真实来源 = 子代理完成事件 Stats 行） | SKILL.md / status-template 4.7 / dispatch 9 文件 / deliverables.md |
| #257 | 净化脚本剥离规则误伤代码块 + 零 exec 文档用 subprocess 示例 | strip-shell-commands.py / 路径校验规范.md / 00-主控-coordinator.md |
| #300 | **净化脚本的「标点修补」规则必须区分函数调用**——`strip-anchor-residue.py` 的 JUNK_TIDY 用 `[（(]` 同时匹配半角 `(`，把代码块内所有无参函数调用削成 `Path(base_dir).resolve` / `c.strip` / `time.now`，代码语义静默损坏（v2.12.4/v2.12.5 净化包 6 文件 13 处） | **`scripts/strip-anchor-residue.py` JUNK_TIDY + 新增自审门 P** |
| #301 | **差集类统计结论必须脚本核验**——「本地 32 个 tag 无 Release」凭手工差集得出，脚本核验后实为 131 tag / 128 Release（真缺口 2 个） | **`scripts/changelog-check.py` / CHANGELOG.md / GitHub Release 台账** |
| #302 | **净化规则的「泛化」会制造无宾语占位符**——只把 `.sh` 换成「shell 脚本」会连主语带参数一起吞，留下「跑 `shell 脚本`」式残句（历史构建残留点）；判据：泛化替换后句子必须仍含主语+动作+宾语 | **`scripts/strip-shell-commands.py` 规则 2b + `references/dispatch/T5-写手.md`** |
| #305 | **OpenClaw 升级会自动迁移 Workshop 技能的物理路径**——2026.9.3（state schema 16）把技能从 `workspace/skills/` 迁到 `<state-dir>/agents/<agentId>/agent/workshop-skills`，非主人操作 | **真源路径记载（AGENTS.md 约定 / `memory/projects.md`）** |
| #307 | **GitHub Release 是独立对象：推 tag ≠ 建 Release**——`git push` 只同步 commit + tag，Release 必须显式 `gh release create`；v2.12.8 tag 已推但 Release 漏建，被 `changelog-check.py --online` 抓出 | **`scripts/create-github-release.sh` / `scripts/changelog-check.py --online` / CHANGELOG.md** |
| #308 | **Session summary 的「open issue」会过期**——写完必须复核当前真实状态（v2.12.6/7/8 均已发布，摘要仍标「[待执行] 用户手动发布 ClawHub 净化包」） | **session summary / `memory/2026-09-10.md`** |
| #309 | **不能基于过期印象硬编码项目路径**——写任何文件前先 `ls` 核实真源位置（v2.12.6 残留补漏时按旧路径操作，整条流程「报成功」而项目根本不在那里） | **真源路径核验（写文件前先 `ls`）/ `memory/2026-09-10.md`** |
| #310 | **工具可用性不能靠印象判定**——裸 `python` 不存在 ≠ python 未安装（本机为 `python3`）；发布记录不得凭印象写「本地未装」 | **`Makefile` lint 目标 / `scripts/capability-assert.py`** |
| #311 | **changelog 必须回读审计原始页面的计数**——不能凭印象写「×N」（v2.12.9：NLP 实为 17 条却写 15；门数口径把警告门也算「全过」） | **CHANGELOG.md / `outputs/audits/`** |
| #314 | **审计记录的「对象版本」必须下载该版本扫描报告**——不能沿用上一版 changelog 的基线数字；每轮 publish 单独扫描，scanId / 文件数 / 条数 / 置信度全不同 | **CHANGELOG.md 审计节 / `outputs/audits/` / ClawHub 扫描报告** |
| #316 | **汇报版本/待办必须现场取证**——归档汇报复述会话摘要旧版本会把当前版本错记（v2.12.11 被记成 v2.12.6）；摘要会过期，`git log`/`git tag`/文件实文才是真源 | **`memory/` 汇报 / `scripts/check-version.sh`** |
| #317 | **自审门不得对会被下游改写的对象做等价性硬校验**——门 G 原用 md5 硬校验真源 vs 包内，而净化链必然改写包内容 → 只要包生成就永远无法 PASS（假红灯，反而诱发绕过） | **`scripts/self-audit-gate.sh` 门 G** |
| #318 | **版本同步清单要按版本戳载体格式分类**——`.sh`/`.md` 行首、YAML `version:`、JSON 字段、安装 pin 四种载体格式不同，`sync-version.sh` 漏 YAML 模式 → `phase-order.yaml` 版本戳不同步 | **`scripts/sync-version.sh` / `references/_shared/phase-order.yaml`** |
| #319 | **披露已知残留必须实测计数**——不得凭点名清单写文件数（v2.12.11 changelog 写「2 个文件」实测 14 文件 50+ 行）；计数类数字一律现场实测 | **CHANGELOG.md / `outputs/audits/`** |
| #331 | **写入式维护路径的「续行副作用」**——`sync-version.sh` 的 header 模式用 `sed -i "${CLOSE_LINE}a\ … "` 追加版本戳行，sed 的 `a\` 把**尾部续行**当插入文本 → 每个受管文件每次 sync 多插 1 个空行（实测 README.md：v2.12.10 = 0 个 → v2.12.19 = 9 个，教训索引 47 行）；末尾 `trim()` 只裁多余的版本行、不管空行 → 无回收层。修法 = 抽 `normalize-version-header.py`（`normalize_header()` 纯函数，归一化后统一补写，`normalize(normalize(x)) == normalize(x)`），并让 header 模式**不再走「已同步即跳过」短路**（否则历史污染永远清不掉） | **`scripts/normalize-version-header.py` / `scripts/sync-version.sh` / `tests/test_sync_version_header_idempotent.py`** |
| #332 | **并发会话链下的「抢发版」**——发版动作（升版号 / tag / push / GitHub Release / 净化包）被当成本链的「下一步」，没检查全局状态：v2.12.18 推送的同时两条子链在本仓推进 v2.12.19 / v2.12.20 → 收口后远端 master 落后本地两版、中间两版无 tag、净化包只到 2.12.18。修法 = 发版前置闸「两查一停」：**在飞链**（同项目 `status=running` 会话/子会话）/ **编号占用**（`git tag -l` + `git ls-remote --tags` 双向）/ **工作区干净**（`git status --porcelain`）任一不过即非 0 退出，通过时打印四行现状（远端 master / 本地 HEAD / tag 区间 / 在飞链=0）；发版是**收口动作**不是推进动作 | **`scripts/release-preflight.sh` / `scripts/create-github-release.sh` / `tests/test_release_preflight.py` / `Makefile`** |
| #333 | **未跟踪文件不得入净化包**——子会话记忆按 **cwd 相对路径**解析 → 「写当天日记」落到被发布仓库的 `memory/`（`?? memory/`）；`build-clawhub-release.sh` 的 rsync 排除是**白名单式逐项列举**（29 条手工 `--exclude`，无 `memory`）→ 主控侧运维笔记默认**入包**；验收只比文件数（81 看起来正常）→ 泄漏被数字掩盖。修法 = 记忆落盘一律**绝对路径**（根 = 工作区）/ 排除清单补 `memory` / 净化包验收加**反向断言**（包内文件全部可追溯到 HEAD 已跟踪文件；`git status --porcelain` 非空含未跟踪时拒绝构建——与 #332 前置闸第 3 查同源） | **`scripts/build-clawhub-release.sh` / `scripts/release-preflight.sh` / 会话记忆落盘约定** |
| #334 | **发版前置闸「编号占用」与「tag 必须先存在」互斥 → 正常发版路径被闸门自锁**——`create-github-release.sh` 第 2 步硬要求本地已有 tag，第 6.5 步又无条件调 `release-preflight.sh`；而闸 ② 把「已有该 tag」判为编号占用 → 实测补发 v2.12.19：`EXIT=11`，Release 未创建，只剩 `--skip-preflight`（文档写明「不得作为常规发版路径」）能走通。根因 = 检查项与调用点**时序语义错配**（② 是给「分配新号之前」用的），且 12 项单测全是闸的**孤立**单测、无一条覆盖「真实发版链路能否走通」——门自身全绿 ≠ 链路可达。修法 = 闸加 `--allow-existing-tag`（② 细化为「编号是否被**本链之外**的人占用」：tag 须属本链历史 + 远端同号同对象，其余仍拒绝；默认关闭），`create-github-release.sh` 写路径自动带；补端到端正向回归（temp 仓库 + fake `gh`，断言退出 0） | **`scripts/release-preflight.sh` / `scripts/create-github-release.sh` / `tests/test_create_release_e2e.py` / `tests/test_release_preflight.py` / `references/agents/00-主控-扩展职责.md` §十四** |
| #330 | **回填式维护路径必须自证幂等**——`changelog-check.py --fill` 的 `split_changelog()` 切出的章节文本**天然含其末尾的 `---` 分隔行**，`cmd_fill()` 又追加一条 → 每次重跑净增「章节数」行（141 章节：183→324→465），伪 diff 掩盖真实变更；修法 = 写回前剥离尾部再统一补写，并用「连跑两次零 diff」作验收（不能靠相邻行判重，分隔行之间有空行） | **`scripts/changelog-check.py`（`strip_section_separator` / `render_changelog`）/ `tests/test_changelog_fill_idempotent.py`** |

| #335 | **净化链的「连续空格塌缩」不区分行首缩进 → 包内 YAML frontmatter 层级被整体摧毁**（≥ v2.12.9 潜伏至今）——`strip-internal-leakage.sh` 阶段 7 的 `re.sub(r'  +', ' ', line)` 把 frontmatter 的 2/4/6 层缩进压成 1 空格 → `metadata.openclaw` 变 null、`version`/`requires`/`tools` 被拉成 `metadata` 直接子项；代码围栏内容被掩码故幸免（差异只在「在不在围栏里」）⇒ 文本 diff 看不出破坏。修法 = 前瞻锚定只塌缩句中空格 `(?<=\S)  +` + 结构敏感区域加**结构断言**（parse 后核对键路径）+ 验收用结构等价而非文本 diff | **`scripts/strip-internal-leakage.sh` / `scripts/build-clawhub-release.sh`** |

| #336 | **矛盾类缺陷只改「被点名的那一处」→ 修完反而把矛盾固化**——v2.12.18 只改了 gate 流程图里一个括注，全仓另有 9 处把 G14 Warning 简写成「自动触发 T5 修订 1 轮」⇒ 「措辞含混」升级成「两处正面对撞」，扫描器 SDI-4 置信度 **0.96** 命中（#328 同型复发）。修法 = 先全仓搜两侧关键词列全点位 → 定规范语义写进**单一真源**（gate 流程图）→ 其余站点只引用 → 验收用**反向断言**（相反表述 grep 必须为 0）；**SDI/一致性类命中先读再判，勿批量归为假阳性** | **`references/gates/14-中文AI痕迹-gate.md` / 全仓 Warning 语意站点（12 文件）** |

| #338 | **「可选项」膨胀 → 同一条款四份清单互斥**（三处同族：外发同意 5/4/2/4 类互斥 · 方法论足迹「默认开」被标「可选」· G14「默认 enabled」被列进「默认关闭」且流程真源 `t6_g14` 无 `condition` 字段）。根因：把「默认/条件开」写成「可选」+ 不区分成本 + 工具级与服务级混写 + 流程真源无可选性表达。修法 = **可选项准入判据**（只给有真实成本或取舍者）+ 三态显式化（默认启用/按条件/按模式）+ 一条款一真源 + 层级分离 + `phase-order.yaml` 节点补 `condition/degrade/exempt` | **`references/_shared/external-services.md` / `gates/14-中文AI痕迹-gate.md` / `_shared/phase-order.yaml` / `SKILL.md` / `_shared/关键协议.md`** |

| #339 | **合并清单前必须先判定「是否同一语义轴」——两轴合并会丢掉最敏感项**：v2.12.25 修「4 份互斥清单」时，把「外发数据形态轴」（含 **大模型推理全文**）与「外发服务类别轴」（5 类）当成同一件事归并 ⇒ 最敏感的「草稿全文→模型 provider」从同意记录消失（包内该词 9→7 处），ClawHub 判 v2.12.25 为 **suspicious**（判词直指 consent flow inconsistent about full drafts）。修法 = 先判语义轴（能否互推）再合并；不许用抽象表述覆盖具体项；加**守恒断言**（原有项计数不得减少）；同意/合规类改动**只能加强不能削弱** | **`references/templates/任务简报-template.md` / `_shared/external-services.md` / `SKILL.md`** |
| #342 | **「计分早于执行」= 假绿灯**：`self-audit-gate.sh` 把 `TOTAL_PASS/TOTAL_FAIL` 与 exit 判定**写在门 S/门 T 之前** ⇒ 这两门报 ✗ 也不进 FAIL、不改退出码，脚本仍打印「✅ 自审门全过」。发现方式 = 加门后 PASS 数字**没变**（21→21）。修法 = 计分/汇总/exit 移到**全部门之后**；**新门必须做反向注入验证**（人为注入断链→期望 exit=1），没做反向验证的门 = 未验证的门 | **`scripts/self-audit-gate.sh`** |

| #343 | **权限「声明」与权限「执行体」必须同源；双份清单必然漂移**：`SKILL.md` frontmatter `denied` 禁用 `memory_store`/`memory_forget`/`sessions_search`，而 `scripts/capability-assert.py` 把它们写进允许白名单（判定只查 `FORBIDDEN_CAPABILITIES`）⇒ 文档说禁用、脚本实际放行，且既有门全绿。修法 = 执行体**不再维护第二份清单**，运行时读 frontmatter 真源（允许面 = base/coordinator_only/research_extra/opt_in；禁用面 = denied）+ **denied 优先** + `--selfcheck` + 机械门（denied 每项都必须被拒） | **`scripts/capability-assert.py` / `SKILL.md` / `scripts/self-audit-gate.sh`（门 T）** |
| #344 | **「只有负向检查」的链路是 fail-open**：净化链只查「违规模式 = 0」，剥离规则**过度匹配**时同样全绿 ⇒「净化成功、内容损坏」无人发现（#150 同族）。修法 = 净化前记**基线**（字符数 + 标题数）→ 净化后正向校验（文件在 / 非退化 / 保留率 ≥ 阈值 / 锚点在 / frontmatter 可解析）。**两条硬规矩**：① 正向门必须**基线相对**（首版写成「必须有 ≥1 标题」立刻误报 0 标题的片段文件）；② 阈值必须**先跑一遍拿分布**（实测最低保留 49% → 取 35%） | **`scripts/pkg-integrity.py` / `scripts/build-clawhub-release.sh` / `scripts/strip-internal-leakage.sh`** |

| #345 | **随包分发的文件，注释也在扫描面；参数即动作目标**：① 给 `phase-order.yaml`（随包分发）写的注释里带 `scripts/flow-check.py` → 构建侧残留扫描命中，构建失败（「只是注释」不成立）；② 未跟踪断言写成无 `else` 的 `if git … fi` → 非 git 环境**静默跳过**唯一的 #333 门；③ `VERSION` 直接拼进 `$OUT_ROOT/$VERSION` 且紧接 `rm -rf` → `../../x` 即删根外目录（**同仓 tag 类脚本早已校验 = 做法不一致即缺陷信号，应横扫同类站点**）；④ 「所有删前先备份」的承诺与无条件 `git reflog expire + gc --prune=now` 直接矛盾。修法 = 格式白名单 + 路径包含性两层校验 / fail-closed + 显式豁免开关 / 不可逆操作 opt-in + 强制执行 `git bundle create` + `verify` | **`scripts/build-clawhub-release.sh` / `scripts/publish-clawhub.sh` / `scripts/cleanup-skill-store.sh` / `references/_shared/phase-order.yaml`** |
| #346 | **修剪文档前必须先查「哪些内容被机械门锚定」——「看起来重复」≠「可以删」**：为压 SKILL.md 体量把 G14「8 类维度」/T9「6 维度+4 档」枚举换成指针 → **立刻破 3 个测试**；`test_rules_consistency.py` **有意**以 SKILL.md 作 T9/G14 的**第三处漂移锚点**（防「改 A 漏 B」）。修法 = 动刀前 `grep -rn "<关键字>" tests/ scripts/` 确认无锚定；**「重复」分两类**：锚点式（删了失去漂移检测→保留）vs 纯拷贝（一份真源+无校验副本→收敛为指针）| **`SKILL.md` / `tests/test_rules_consistency.py`** |

| #347 | **体量/配额类目标要用「棘轮门」**：SKILL.md 超官方上限 24% 却**没有任何门**在管（只能靠人偶然想起）。修法 = 记录当前值 `SKILL_CHARS_CEIL`，超过即 fail，**瘦身后同步下调**（只许变小）；且**上限数字同时出现在门与测试里必须一致**（否则是 #343 的双份清单漂移）→ 加断言两者相等的测试。推广：凡「应该收敛但没门」的度量（体量/断链数/警告数）都适合先立棘轮 | **`scripts/self-audit-gate.sh`（门 V）/ `scripts/link-check.py`（门 U）/ `tests/test_doc_quality.py` / `CHANGELOG.md`** |

| #348 | **汇总审计发现时必须逐条登记全部发现项与处置状态——不得用「自选汇总数」表述覆盖面**：三份并行审计（安全/流程/可维护性）settle 后，对外声明写成「发现 **11 项**缺陷，三批**全部修复**」；回读原文才发现**流程审计单独就有 P0×3+P1×6+P2×9 = 18 项**，其中 9 项**根本没进那三批**、仍活在已发布代码里 ⇒ 已发布声明失真（「全部修复」为假）。根因：① 把「我挑出来修的那批」当成「审计发现的全集」（三批的「P1×4」实为**安全审计**的发布链 4 项，被拿来盖住了流程审计的 P1×6）；② 子代理结果 settle 时**截断**，按摘要而非原文核对覆盖面。修法 = 建「**发现项 × 处置状态**」登记表逐条核对（编号必须取自报告原文，含「已修 / 已由新门顺带修掉 / 已知不修+理由」三态）；对外声明只写核销表里已闭环的项；**截断的结果不算已核对**（须回子会话读全文）；发版前跑「审计发现是否逐条有处置」而非「我修的这批是否全绿」 | **`CHANGELOG.md` / 对外发布声明 / 审计收口流程** |
| #349 | **ClawHub 的「Outcome: Pass」≠ 无风险——四家扫描器结论可并存且方向相反**：v2.12.30 扫描页面显示 **Outcome: Pass**，而完整报告 ZIP 内 **NVIDIA SkillSpector 判 `suspicious` / severity HIGH / score 79 / DO_NOT_INSTALL（21 条）**；同一报告里 Tencent AIG = `clean`/`benign`（8 条全标 expected）、static-analysis = clean、VirusTotal = 0 malicious/64 undetected。根因 = **Outcome 是门禁聚合**（AIG+static+VT 这类能定性者），**SkillSpector 的 findings 是启发式清单**、不参与门禁，两者不是同一维度结论、不可互推。修法 = 报扫描结论必须**拉完整报告**（`clawhub scan download <slug> --version <v> -o x.zip`，含 manifest sha256）并**分扫描器陈述**，同时说明「门禁结果」与「启发式发现」之别；21 条须按 **unique issueId 归并**（本例 21 条 = **6 类根因**跨 17 文件扩散，其中 13 条是同一「默认中文」政策项）——按条数读会把 13 条当 13 个问题；**AIG 明确标 `expected` 且有理由者（AE1/AE4 等）不得自行改判为缺陷** | **ClawHub 扫描复核流程 / `scripts/link-check.py`（门 U 第二类）/ `SKILL.md` / `references/_shared/external-services.md`** |
| #350 | **发布上传「慢」被误判成「卡死」——管道缓冲掩盖进度，超时给太短导致连杀两次**：82 文件净化包 `clawhub publish` 正式上传阶段 **>10 分钟**；输出走 `\| tail -30` 管道 → **缓冲**致全程无进度可见 → 误判「可能卡死」→ 600s 超时 → **被 SIGTERM**；核实服务端未落库后重试 → **再被杀**，白等约 20 分钟，最终主人手动上传。修法 = ① 外层超时 **>15 分钟**；② 输出**重定向到日志文件**（`> /tmp/x.log 2>&1`）用 `tail` 读**文件**，不读进程 stdout；③ 判断存活/推进用 `pgrep` + `ss -tinp`（看 `bytes_sent` 是否增长）而非等输出；④ 非幂等动作重跑前**先核实服务端是否已落库** | **发布链（`scripts/publish-clawhub.sh`）/ `memory/lessons.md`** |
| #351 | **`clawhub inspect` 带读缓存/限流——「读不到」≠「不存在」；且 `--versions` 列表本身也会滞后**：v2.12.31 时上传已成（重试被 `Version 2.12.31 already exists` 拒绝 = 铁证）而 `inspect --json` 仍报 `tags.latest=2.12.30`；**v2.12.33 复验进一步证伪原修法**——`--versions --limit 200` 列表仍停在 2.12.32（`count=50`）不含新版本，而 `--version 2.12.33` / `--tag latest` 已可查到。修法 = 核实落库用**逐版本/标签直查**（`--version <v>` / `--tag latest`，读 `skill.tags.latest` + `latestVersion.version`）+ **写路径铁证** `publish → already exists`，或直连 registry API；**不得只凭清单类查询否定落库**。同族 #309/#310：**状态查询工具自己也会返回过期值** | **发布链复核流程 / `memory/lessons.md`** |
| #352 | **半成品版本戳（版本戳与版本记录须同批）**：只改受管文件不写 CHANGELOG/README 正文 ⇒ `changelog-check` 红 + README 正文行测试红。修法 = 版本戳 + CHANGELOG 章节 + README/QUICKSTART 正文 + 棘轮号**同一批**改完，不提交中间态 | **发布链 / `memory/lessons.md`** |
| #353 | **扫描器 fail-closed 建议与实测必需行为正面冲突（只能消歧义、不能消除）**：ClawHub AIG `T05` 逐字引用 v2.12.32 新写的「工具面超限→继续」判据并建议「能碰 exec 就拒绝多 Agent 模式」——而该 fail-closed 路径已被实测证伪（T2/T3 首轮零产物、流水线自锁）。主人裁决 A 档 = **保持行为、只消歧义**（阻断级前置 + 铁律「超限 ≠ 调用许可」+ 裁决只放行继续）。要点：**预期该条仍报**、须在交付说明披露取舍；**绝不**为页面干净破坏实测验证过的行为 | **审计链 / `memory/lessons.md`** |

| #354 | **ClawHub changelog 缺省 = 平台自动生成（文本可与本版无关），且同版本号不可重发修正**：`--changelog` 缺省 `""`（`publish.js:30`）→ 平台 `changelogSource:"auto"`——v2.12.33 自动文本以「Removed redundant file: skill-card.md」开篇（该文件 2.12.31 已移除）且未提本版头号修复；CLI **不读** CHANGELOG.md（`dist/` 全仓无该路径）；同号不可覆盖 ⇒ 发错只能等下版。修法 = 发布脚本从 CHANGELOG.md 该版章节提取（主题行 + `###` 标题 ≦40 行）显式传 `--changelog`，提取为空则 fail-closed（`exit 3`）。**复验副产物**：dry-run 分支早退、**不构造请求体** → JSON 里无 `changelog` 字段 ⇒ 拿 dry-run 断言该字段 = **假失败、阻断正确发布** | **发布链（`scripts/publish-clawhub.sh`）/ `memory/lessons.md`** |

## 三、使用说明

- **查某条教训**：上表「定位」列 grep 对应文件。
- **查某主题**：上表「分类」列定位主题范围。
- **完整教训库**：主工作区 `memory/lessons.md`（当前最大编号 **#354**，2026-09-12 续录至 #354；论衡相关新增 = #296 净化包剥离主控侧运维痕迹铁律 / #297 剥离后残留三类 / #298 剥后收口 / #299 无效表格分隔行（门 O）/ #300 标点修补误伤函数调用 / #301 差集结论须脚本核验 / #302 泛化制造无宾语占位符 / #305 平台升级迁移技能物理路径 / #306 平台递归委派致叶子假设失效 / #307 推 tag≠建 Release / #308 session summary open issue 过期 / #309 路径不靠印象 / #310 工具可用性不靠印象 / #311 changelog 计数须回读 / #314 审计对象版本须下载扫描报告 / #316 汇报版本与待办须现场取证 / #317 自审门不得对会被下游改写的对象做等价性硬校验 / #318 版本同步清单要按版本戳载体格式分类 / #319 披露已知残留必须实测计数 / **#320-#328 发布与版本纪律（定案不砍头号能力 / 规则只防手写 / 真源须自证计数 / 产物目录不同树重建 / 宣布前清点并行批次 / verdict≠findings / 脚本成功退出≠按请求执行 / 替换式净化只抹字面 / 扫描报告缺陷归属不可信）/ #329 扫描报告「中间态」不可当结论 / #330 回填式维护路径必须自证幂等（--fill 尾部分隔行累积）/ #331 写入式维护路径必须自证幂等（版本戳后空行累积，sed `a\` 续行副作用）/ #332 并发会话链下的抢发版（发版前置闸 = 两查一停）/ #333 未跟踪文件不得入净化包（记忆落盘按 cwd 相对解析 + 排除清单默认方向错）/ #334 发版前置闸自锁（编号占用口径与「tag 必须先存在」互斥；门类改动须配「应当放行」正向样本）/ #335 净化链空格塌缩压平行首缩进（frontmatter 层级被毁；须结构断言兜底）/ #336 矛盾类缺陷只改点名处 → 修完反而固化矛盾（SDI 命中是免费一致性审计）/ #338 可选项膨胀 → 同一条款四份清单互斥（可选项准入判据：只给有真实成本或取舍者）/ #339 合并清单前先判「是否同一语义轴」（两轴合并会丢掉最敏感项；须守恒断言）/ #342 自审门「计分早于执行」= 假绿灯（计分与 exit 必须在全部门之后；新门须反向注入验证）/ #343 权限声明与执行体必须同源（denied 优先；双份清单必然漂移）/ #344「只有负向检查」= fail-open（正向门须基线相对；阈值须先拿分布）/ #345 随包文件注释也在扫描面 + 参数即动作目标（同族脚本做法不一致即缺陷信号）/ #346 修剪前先查「被机械门锚定的内容」（锚点式重复 ≠ 可删）/ #347 体量/配额类目标用棘轮门（只许降；门与测试的上限必须一致）/ #348 汇总审计发现须逐条登记处置状态（不得用自选汇总数表述覆盖面；截断的报告不算已核对）/ #349 ClawHub「Outcome: Pass」≠ 无风险（门禁聚合与启发式清单不可互推；须拉完整报告分扫描器陈述）/ #350 发布上传慢 + 管道缓冲掩盖进度（外层超时 >15min 且必须落盘日志） / #351 `clawhub inspect` 读缓存（「读不到」≠「不存在」，核实落库须**逐版本/标签直查** + `publish already exists` 交叉；`--versions` 清单同样会滞后）/ #352 半成品版本戳（版本戳与版本记录须同批，不提交中间态）/ #353 扫描器 fail-closed 建议与实测必需行为正面冲突（只能消歧义、不能消除；不得为页面干净破坏实测行为）/ #354 ClawHub changelog 缺省 = 平台自动生成（同号不可重发回改，须发布前显式传 `--changelog`；dry-run 不返回该字段不可断言）**（#315 空号未使用）；论衡 SKILL 内不复制，教训 #143 原则）。
- **收录范围**：本索引只收**论衡相关**教训。#303（外部模型上线前核对官方模型名）/ #304（WSL2 systemd user service）/ #312–#313（provider 删除与凭据容器）属宿主/环境类，不进本索引，真源见主工作区 `memory/lessons.md`。

## 四、维护规则

- 新增教训 → 在对应文档「教训 #N」标注处 + 本索引「分类」表补一行。
- 教训编号**永不复用**（即使过时也保留编号，教训 # 体系）。
- 本索引只做**主题分类 + 定位**，不做逐条详解（避免快速过期）。
