> 版本：v2.5.6（自动同步 2026-08-25）



# 论衡（lunheng-article-pipeline）— 通用深度长文多 Agent 流水线 运行手册（v2.2.14）

> **定位（v2.2.4）**：论衡是**通用深度长文引擎**，不只用于公众号，而是可产出**学术论文、商业评论、行业分析、公众号深度长文**等各类深度长文。学术论文用 `[Lxx]/[Dxx]` 编号引用；公众号/商业评论用内联（机构，年份）引用（M 门 v2.2.4 新增内联引用分支）。

> 使用方式：主控 Agent（论衡）按本文档派发任务。角色定义见 `agents/`，模板见 `templates/`。

## 📖 核心概念词汇表（单一真源，优先阅读）

**开始使用前，强烈建议先阅读**：[`glossary.md`](glossary.md)

词汇表集中定义了所有核心概念，避免在多个文档中重复查找：
- 9 张角色卡（T0-T7 + T9）+ T8 终检职责（v2.3.0 重构：T6 案例检索 → T3，T6 = 批判伙伴，T7 = 审计员，T8 = 终检主控亲完成；**v2.4.0 新增 T9 同行评审**）
- 三层防御体系（M 门/F 模式/G 清单）
- 数据信任级别（3 档）
- 关键协议（Phase 0/0 条空卡/并行独立运行/修订回环/完成验证铁律）
- 工具能力边界（15 项可用 / 11 项禁用，见 [glossary.md § 五 工具能力边界](glossary.md#五工具能力边界v2212-前置声明)）
- 版本号管理（4 层真源，v2.4.0 更新：skill 化后取消 openclaw.json 层）
- 教训沉淀体系（#1-#144）

---

> **⚠️ v2.1.7 Phase 0 强同意关卡（ClawHub scanner F5 修正）**：本流水线下面任何一个阶段调用以下工具，**会**发数据到**外部服务商**——主控必须告诉主人明示同意，否则不可调用：
>
> | 工具 | 外发服务商 | 发送内容 |
> |------|---------|---------|
> | `web_search` | OpenClaw 内置 web provider | 检索关键词 |
> | `tavily_search` / `tavily_extract` | **Tavily AI** | 研究主题/URL |
> | `image_generate`（**封面**，可选默认关闭） | **OpenAI gpt-image-2**（默认）→ Google gemini / minimax-image / SVG 本地 | 主题 + 品牌 prompt |
> | **数据图表 SVG** | **本地内置**（主控 `write` 手写 SVG，零外发，不依赖外部） | — |
> | 大模型推理 | 当前模型 provider（deepseek/MiniMax/Anthropic 等） | 文献/数据/案例/草稿/大纲全文 |
> | `memory_get` / `memory_search` | 仅本地 OpenViking（**不外发**） | 检索关键词 |
>
> **项目名/主题/论文纲要可能含未公开信息**——主控 Phase 0 必须问主人：
> 1. 同意全部外发？（默认）
> 2. 脱敏措辞 + SVG 封面 + 本地 Ollama（gemma4:31b）推理？
> 3. 只同意一部分（如文献检索同意、封面文生图拒绝）？
>
> 主人选定后写入 `01-任务简报.md` 作为「外发同意项」记录。**未同意前不可跳到 Phase 1**。
> v2.0（2026-08-11）：新增 Phase 2.5 大纲确认、status.md 状态机、修订说明、引用分级核验、数据时效评级、防选题漂移 G0 检查、AI 去味禁做清单、证据包交付、期刊模板。
> v2.1（2026-08-11）：新增配图节点（Phase 4.5）。v2.2（2026-08-13）：新增案例卡 + T6 案例检索员 + 三角验证 + G2.5 + 时间锚点 + 中文新闻源 + 强相关性铁律（教训 #34）。v2.3（2026-08-13）：调度预检 + 8 分钟硬卡 + 写手视角一致性/数据无源/图注精度 + 审计 G0.5/G6 + 融合协议 + 反哺报告 + 论据类型自标（教训 #35）。v2.4（2026-08-13）：**原创性保证**——T1 先行者检索 + T3 差异点声明 + 审计 G7 原创性审计（防「重复/改写已公开文章」）。v2.0.2（2026-08-14）：**安全收紧**——回应 ClawHub SQP-2 finding：反哺报告不自动 commit 角色卡 + Phase 0 显式确认 <项目名> 与文件清单 + 工具集收窄到 13 项（移除 exec/browser/cron/凭据访问路径）。v2.0.5（2026-08-14）：封面恢复 image_generate 工具（默认 OpenAI gpt-image-2，考虑普适性），回应教训 #38 over-correction（工具白名单变 15 项）。v2.0.6（2026-08-14）：T6 案例检索员干跑验证完成 + Skill 包补 设计文档.md + 回应 ClawHub Finding 2（drafts/初稿-v1.md 写入加 v2/v3 覆盖声明）+ Agent README 版本变更行重复清理 + 文档同步漂移修复（MEMORY.md 13→15 工具数 / 1→2 findings / SOUL 误述）。v2.0.8（2026-08-14）：image_generate fallback 链补充 minimax/minimax-image-01 作为最终 fallback（论衡 agent imageGenerationModel.primary 仍保持 OpenAI gpt-image-2 普适默认，fallback 顺序 OpenAI → Google → minimax → SVG）。v2.1.0（2026-08-14）：**激进重构——执行层韧化 + 审计补盲区**。**执行层**：7 张角色卡全部注入「执行韧化协议」——30 秒心跳 + 分阶段 ack（按任务时长 3 档分级）+ 模型健康度预检（1-token ping，30s 无响应降级 fallback）+ 8 分钟硬卡（6 分警告/7 分 partial/8 分 kill）；论衡 agent `model.fallbacks` 扩展为 4 档（minimax-M3 → deepseek-flash → glm-5.2）；主控「介入机制」补 6 步标准动作（拍醒/查 sessions/换模型重派/接受 partial/写介入日志/重试上限 2 次）。**审计补盲区**：T5 新增 **G8 成品度**（8 项必查：过程语言/角色元数据/临时编号/元话语/修订文件/占位符/结构对称/字数偏差）+ **G9 时序合理性**（4 项必查：总耗时/分阶段 ack/心跳记录/文件时间戳）。回应教训 #43（执行层脆弱 + 成品度盲区）。v2.1.1（2026-08-14）：**回应 ClawHub 7 项新 findings**。① 修写手矛盾（95% confidence）：禁做清单 #3「主体声音 ≠ 第一人称经历」替换原「无第一人称」混淆表述；② 限「据行业经验估算」使用条件（92%）：主控洞察融合协议加 5 条限定（三源全失败 + 段落顶标 [行业估算] + 不超 3 处 + T5 G2.5 估算专项 + 禁伪装精确度）；③ 首次 image_generate 需主人同意（91%）：主控/SKILL.md 加「封面生成前必须先询问」机制；④ 分析员触发条件段（89%）：顶部加 allowedCallers/when/exclusions/boundary check 四部分；⑤ ⑥ ⑦ #2/#6/#7（语言/文件写入/硬编码 locale）部分预期内不修 #2/#6/#7 部分预期内不修，README 加语言与定位声明。教训 #44 已写。v2.1.2（2026-08-15）：**补 ClawHub F5/F9 外部传输警告 + 文档同步修复**。① SKILL.md / README.md / 论衡 SOUL.md / pipeline/README.md 加「⚠️ 外部服务与数据流声明」（明示 Tavily / OpenAI gpt-image-2 / Google gemini / minimax-image 等外发项 + 主人拒绝调整方案 + 脱敏/SVG/本地 Ollama 备选）；② 主控卡加 Phase 0 外部服务告知职责；③ 设计文档.md 团队架构补 T6 案例检索员可视化（之前第三个并行框为空）+ 7 角色编号完整化 + G8/G9 + v2.1.0/v2.1.1 增量补全；④ status-template.md + 任务简报-template.md 「T6 终检交付」→「T7 终检交付」编号冲突修复（T6 已是案例检索员）+ 新增 T6 案例检索行（可选，默认 Skipped）；⑤ SOUL.md team 表补 T6 + v2.1.1 安全约定同步；⑥ 论衡 MEMORY.md 版本号 v2.3→v2.1.1 + 角色卡数 6→7 + 执行韧化协议补全；⑦ AGENTS.md 流水线协议加 T6 + Phase 0 外部服务 + 8 分钟硬卡；⑧ 主控 workspace MEMORY.md 订正「ClawHub verdict=BENIGN」误述（实际 Review/10 项 findings）。教训 #45 已写。v2.1.3（2026-08-15）：**执行层可靠性补强 + 审计一致性 + 元数据边界**（教师场域孤岛实战教训 #46）。① 主控「完成验证铁律」——收到完成报告第一动作 ls 验证产物存在+非空+关键 grep，不采信报告（T4 v3/v4 两次「报告完成但未写盘」事故）；② 修订任务目标拆分——>2 个独立目标拆任务或主控 edit；③ 小修订主控直接 edit——定点修复 <5 处不 spawn T4；④ T5 新增 G10 核心模型一致性审计（五方主体三处表述不一致事故）；⑤ 写手铁律 #8 元数据边界——[xx说明]/GAP标注/图位详细说明不进正文；⑥ 终检必查项加统计口径动态更新 + 先行者引用闭环（[先xx] 文末对应）；⑦ 主人在环三节点→四节点（新增 Phase 3.5 主人深度洞察补充窗口）。教训 #46 已写。v2.2.0（2026-08-15~18）：P0-A 机械硬门（M 门 + comm diff）+ P0-B 失败模式清单（F1-F7 替代 G 部分）+ P0-D max 2 revision + P1-C 报告隔离（教训 #64）。v2.2.1（2026-08-18）：P0-C 数据信任级别 + P1-A 双完整性门 T2.5/T5.5（教训 #77）。v2.2.1.1（2026-08-18）：补文献卡 + 先行者清单 template（教训 #78）。v2.2.1.2（2026-08-18）：3 个 M 门算法修复 + 数据卡双格式支持（教训 #85）。v2.2.2（2026-08-18）：P1-B 批判伙伴 T8 + P2-A Disclosure 模式（教训 #86，路线图三发最后一发）。v2.2.3（2026-08-18）：**GLM-5.2 → GLM-5.3 全栈迁移**——GLM-5.2 即将下线 + OpenClaw 配置 ID 错配（实际 chat ID 是 glm-5-2-260617，教训 #89）；论衡角色卡 + _shared 文件 + README + 任务简报 + ClawHub 副本全部替换为 coding-plan/glm-5.3；OpenClaw config 删除 GLM-5.2 实体。教训 #90。v2.2.4（2026-08-19，AI安全隐患实战闭环 + 深度长文定位）：**7 项改进**——① 修订 SOP 固化（先 cp v2 再改 v2，归档中间态，哈希校验，教训：实战 P1-2）；② 修订轮强制独立写手（主控不代执行，防自我说服）；③ M 门内联引用分支（公众号/商业评论内联引用下 M-Exist-1 不再空转，手动回溯 24 条）；④ T4 无主数据自查（不得凭记忆写未经核实数字，实战误写 2020 年 2500 万深伪案）；⑤ T5 修订任务书（打回时产出可执行修订表）；⑥ 补检索文献回填校验（补检索 L 总数 == 文末清单条目数）；⑦ 第 3 轮出口正常化（局限性模式是诚实出口非失败）。**定位升级：深度长文引擎**——学术论文/商业评论/行业分析/公众号深度长文通用。v2.2.5（2026-08-19）：对 v2.2.4 自指质量审计，修复 4×P1（设计文档漏 commit / 补检索回填等式错误 / 内联引用系统化 / 修订轮边界冲突）+ 6×P2。v2.2.6（2026-08-19）：**版本升级自审门**——新增 `_shared/版本升级自审门-v2.2.6.md`（7 门机械化自审，教训 #95），主控卡加引用；背景：v2.2.2 新增 T8 时 SKILL.md 三处漏同步，主人人工发现才补。

v2.1.4（2026-08-15，**测试期 finding，本地修订未发布**）：**文末「引用来源」四节完整性闭环**（教师场域孤岛实战教训 #47）。① README「定稿引用规范」升两节→**四节**（数据来源/案例来源/参考文献/先行者文献，[C-主xx] 主人洞察单独说明）；② 写手卡铁律 #9「文末「引用来源」清单完整性」——双向 diff 自检 + 测算值如实标注；③ 主控卡终检必查项 ⑨「文末四节完整性闭环」——机械化 grep diff 双向校验；④ 审计卡 G4「文末「引用来源」四节完整性」——任意漏引/孤儿条目 P1；⑤ 交付前必走 `grep -oE "[DC]-?[LC]-?[主]?[xx]+" + comm -23/-13` 三件套。教训 #47 已写。

**v2.1.4 修复的具体事故**：教师场域孤岛定稿主控对话环节主人发现文末缺数据/案例两节来源清单（35 条 [Dxx] + 8 条 [Cxx]/[C-主xx]），写手只搬运了 [Lxx] 参考文献一节，主控终检未卡，审计 G4 未覆盖 final 阶段。机制升级确保后续项目四节完整无漏。

## 目录

- [🚫 不适用场景（边界明示 — v2.1.5 新增，v2.1.5.1 修正表述）](#不适用场景边界明示-v215-新增v2151-修正表述)
- [快速开始](#快速开始)
- [调度模型健康度预检（v2.3 新增，教训 #35）](#调度模型健康度预检v23-新增教训-35)
- [流水线全景](#流水线全景)
- [status.md 状态机（主控维护，角色交接时更新）](#statusmd-状态机主控维护角色交接时更新)
- [派发话术（主控复制即用）](#派发话术主控复制即用)
  - [文献检索员（并行①）](#文献检索员并行①)
  - [数据检索员（并行②）](#数据检索员并行②)
  - [案例检索员（v2.1.8 强化：任何量级必 spawn，与 T1∥T2 三方并行互不干涉，教训 #56）](#案例检索员v218-强化任何量级必-spawn与-t1t2-三方并行互不干涉教训-56)
  - [分析员](#分析员)
  - [写手](#写手)
  - [批判伙伴（v2.2.2 新增：写手 v1 后、审计前 spawn，教训 #86）](#批判伙伴v222-新增写手-v1-后审计前-spawn教训-86)
  - [审计员](#审计员)
  - [同行评审（T9，v2.4.0 新增：可选触发）](#同行评审t9v240-新增可选触发)
  - [G14 中文 AI 痕迹检测器（v2.4.0 新增）](#g14-中文-ai-痕迹检测器v240-新增)
- [修订回环](#修订回环)
  - [v2.2.0 修订回环 ≤2 轮硬约束（教训 #64 子教训 3）](#v220-修订回环-2-轮硬约束教训-64-子教训-3)
- [M 机械化硬门（v2.2.0 新增，教训 #64 + #65）](#m-机械化硬门v220-新增教训-64-65)
  - [M 门算法规约（v2.2.0 哲学最纯粹版）](#m-门算法规约v220-哲学最纯粹版)
- [F 失败模式防御（v2.2.0 新增，教训 #64）](#f-失败模式防御v220-新增教训-64)
  - [F1-F7 速查](#f1-f7-速查)
- [F8 数据信任失败模式（v2.2.1 新增，教训 #77）](#f8-数据信任失败模式v221-新增教训-77)
- [F9 论证强度不足（v2.2.2 新增，教训 #86）](#f9-论证强度不足v222-新增教训-86)
- [AI 使用披露（Disclosure，v2.2.2 新增，教训 #86）](#ai-使用披露disclosurev222-新增教训-86)
- [交付（Phase 5）](#交付phase-5)
  - [定稿引用规范（主人 2026-08-12 确立，水与麦单修正 + v2.1.4 四节闭环，教训 #47）](#定稿引用规范主人-2026-08-12-确立水与麦单修正-v214-四节闭环教训-47)
  - [PDF 生成规范（主控负责，水与麦单修正）](#pdf-生成规范主控负责水与麦单修正)
  - [修订规范（教训 #11：改稿必先另存版本号）](#修订规范教训-11改稿必先另存版本号)
- [配图生成规范（Phase 4.5）](#配图生成规范phase-45)
- [成本与模型建议](#成本与模型建议)
- [模型配置与更换指南（v2.3）](#模型配置与更换指南v23)
  - [三层模型配置（从外到内）](#三层模型配置从外到内)
  - [更换的自由、途径、权限](#更换的自由途径权限)
  - [能力分层原则（换模型时遵循）](#能力分层原则换模型时遵循)

---

## 🚫 不适用场景（边界明示 — v2.1.5 新增，v2.1.5.1 修正表述）

论衡是「论文/深度文章」**写作流水线**，**擅长主动检索已发布证据 + 整合主人投喂的证据**。

**论衡能主动采集**（T1 文献检索 / T2 数据检索 / T3 案例检索 sub-agent，v2.3.0 改 T6→T3）：
- ✅ 已发布的学术文献（PubMed / CNKI / Web of Science 等数据库）
- ✅ 已发布的统计数据（教育部 / 统计局 / 行业协会等公开数据）
- ✅ 已发布的案例与报道（媒体 / 法院判决 / 行业报告等公开案例）
- ✅ 政府发布的统计 / 报告 / 调查 / 政策文件

**论衡不能主动采集**（需要主人提前准备素材投喂）：
- ❌ **一手原始数据采集**：实验设计 / 调查问卷投放 / 用户访谈 / 田野调查 → 需要主人亲自调研，原始数据投喂为「数据源」
- ❌ **统计分析**（SPSS/R/Python 跑模型）：论衡可以引用统计结果，但**不执行统计计算**。如需跑回归/聚类/因子分析，请主人用专门工具，结论以「数据 + 方法描述 + 结果」形式投喂
- ❌ **图表原始数据采集**：论衡生成的是**数据可视化**（SVG 矢量图，参考 `图表-SVG-template.md`），数据本身需主人提供。如需爬虫/OCR/语音转文字，请主人用专门工具，原始数据投喂后论衡制作图表
- ❌ **原创图片 / 视频生成**：论衡有 `image_generate` 工具生成**封面**（数据图表则是 SVG 本地 write 生成，零外发），但**不能拍摄实物照片 / 录制视频**。如需实物素材，请主人拍摄后投喂文件路径，论衡可在文末引用
- ❌ **代码执行**：`exec` 工具不在 15 项白名单内（denied）。如需跑代码验证论据，请主人用专门环境执行，结果投喂为证据

**判断口诀**：问「这个证据是**已发布**的数据 / 文献 / 案例吗」——是，论衡主动采集；不是（是一手原始数据 / 自己拍的素材 / 自己跑的计算），主人投喂后再用。

## 快速开始

```
1. 主控确认主题（Phase 0）→ 写 01-任务简报.md + 初始化 status.md
2. 主控记录 cases 需求（含 0 条场景的显式声明），写入 status.md（v2.1.8 取消三档决策，教训 #56）
3. 主控调度模型健康度预检（v2.3 新增，详见下节）→ 决定该次 T 派发用哪个模型
4. 并行 spawn 文献检索员 + 数据检索员（用下面的派发话术）
5. sessions_yield 等两者完成
6. spawn 分析员 → 分析大纲产出 → Phase 2.5 送主人确认 → 通过后 spawn 写手
7. spawn 审计员 → 审计不过 → 写手交修订说明 + 修订稿（≤2 轮）→ 终检交付（含证据包）
```

## 调度模型健康度预检（v2.3 新增，教训 #35）

> 实战中 T3 连续 3 个模型（deepseek-v4-pro / minimax-m3 / claude-opus-5）全部超时，论衡主控亲自产出大纲。本机制旨在预判 + 降级 + 硬卡超时。

**主控 T 派发前必须执行 3 步**：

1. **健康度预检**（避免一上来派发到已知慢模型）：
   - **实战教训（v2.5.6 标记为历史段）**：v2.3.x 时代深度绑定 DSH 平台的 5 个模型 ID（deepseek-v4-flash/glm-4-flash/qwen3-coder/minimax-M3/claude-opus-5），实战教训 #35 + #119 + #126 都来自该绑定。**v2.5.6 P1-3 修订**：候选池仅描述能力，具体模型由 Phase 0 自检映射。换 OpenClaw runtime 后本段失效，由能力档描述接管（详见 SKILL.md 「模型分档」）。
   - 实战教训（教训 #35）：deepseek-v4-pro：推理/分析默认，但有过超时历史（v2.3.x 绑定）
   - 实战教训（教训 #126）：minimax-M3：推理/分析 fallback，但近期 T3 三连超时主要责任方（v2.3.x 绑定）
   - 实战教训（教训 #119）：claude-opus-5：审计顶配（T7 审计员主模型），但成本高 + 有 kkaiapi 接口静默无响应前科（v2.3.x 绑定）
   - **T7 审计员派发前必查**（v2.3.1 新增，教训 #119）：T7 属「批判审计=顶配」能力档，候选池见 SKILL.md 模型分工表；派发前 1-token ping 预检，若顶配模型静默无响应 → 走候选池下一档，并在产物头标注兑底事实。
  - **角色级 fallback 分级（v2.3.12 P0-2 能力抽象，已取代 v2.3.7 硬编码链）**：v2.3.7 的硬编码 fallback 链（T7 审计 fallout 链 / T5 写手链 / T4 分析链 / T1-T3 检索链）已废弃，统一为「能力档 + 候选池」（候选池唯一真源见 SKILL.md 模型分工表），角色卡不再硬编码单一模型，候选池按本机可用模型自动适配。
   - **主控派发前查 status.md 顶部「论衡本轮可用模型」表**（论衡会话启动时自动填）

2. **硬卡阈值（v2.5.5 P0 硬性化，原 8 分钟统一硬卡已废弃）**：
   - **详见主控卡 §二十二「硬卡阈值表」**：T1-T3 10 分钟 / T4 12 分钟 / T5 15 分钟 / T6-T7 12-15 分钟 / G14 8 分钟
   - 实战教训：原统一 8 分钟硬卡偏紧，导致「主任务其实写好了但被误判超时 → fallback 覆盖」（教训 #126 + #131）
   - v2.5.5 修订：按角色分级硬卡，不允许主控「再等一下」——超阈值立即 kill + 接受 partial
   - 不允许「静默等 10 分钟」

3. **主控兜底产物明确标注**（v2.0 机制没标 → 写手不知道这是主控产物不是 T 产物）：
   - 任何 T 阶段主控亲自产出 → 必须在文件头标注「主控兜底 v1.0」+ 注明失败原因
   - 写手收到时能识别这是非 T 产物，调整引用对齐策略

## 模型配置与更换指南（v2.3.10 补全）

> **背景**（主人 2026-08-22 指出）：operations.md 引用「模型配置与更换指南」段，但 v2.3.0 重构时 TOC 加入了三个子标题（`三层模型配置`/`更换的自由、途径、权限`/`能力分层原则`），**正文未补**——造成文档漂移（教训 #60 双形式同步漂移反模式）。本节为 v2.3.10 补全。

### 三层模型配置（从外到内，v2.3.13 能力抽象后）

v2.3.13 起模型不再硬编码，模型解析分**三层**（从唯一真源到兑底兜底）：

1. **SKILL.md 模型分工表**（能力档 + 候选池，**唯一真源**）
   - 检索=便宜快 / 分析写作=强推理 / 批判审计=顶配 / 主控=稳定路由
   - 每个能力档的候选池按优先级排列，换系统/换模型改这一处即可

2. **Phase 0 模型自检**（运行时映射）
   - 主控启动扫本机可用模型（`models list`），每个能力档从候选池选第一个可用模型
   - 顶配档候选池全不可用 → 显式告知主人，禁止静默降级

3. **openclaw.json 模型**（仅兑底兜底，非硬配置）
   - `agents.list[].model.primary + fallbacks` 仅作 Phase 0 自检失败时的最后兜底
   - 不再需要为论衡单独建 agent 或手动配模型（v2.3.13 skill 化）

### 更换的自由、途径、权限

| 层面 | 谁能改 | 怎么改 | 生效时机 |
|---|---|---|---|
| OpenClaw agent 配置 | 主人（手动 edit openclaw.json）或 gateway.patch | 需 backup 后改，gateway 重启生效 | 重启后 |
| 角色卡推荐模型 | 主人 / 主控（经主人同意） | edit 角色卡顶部，sync-version.sh 同步 | 下一轮会话 |
| 本轮可用模型 | 主控在 status.md 顶部填 | write status.md 头部「本轮可用模型」表 | 即时生效 |

**主人的自由度**：
- ✅ 任意层都可改，无需经过论衡批准
- ✅ OpenClaw 兼容的任何模型都可接入（含 Ollama 本地）
- ✅ fallback 顺序可自由调整
- ⚠️ 改完需**重跑论衡首单测试**，验证模型组合不超时/不崩

### 模型能力抽象 + 自检 + 预算闸门（v2.3.12 新增，P0-1/P0-2/P0-3）

> **背景**：v2.3.12 前模型名硬编码在角色卡/SKILL.md，换系统即失效；且主控无「余额不足」预检，claude-opus-5 余额不足时顶配审计**静默降级**（本机测试 T6/T7 全兑底 minimax-m3，A 级抽验仅 17%）。

- **能力抽象（P0-2）**：模型名 → **能力需求 + 候选池**（检索=便宜快 / 分析写作=强推理 / 批判审计=顶配 / 主控=稳定路由），候选池见 SKILL.md 模型分工表。
- **Phase 0 模型自检（P0-3）**：主控启动时扫本机可用模型（`models list`），每个能力档从候选池按优先级选第一个可用模型，写入 status.md「本轮可用模型」表；顶配档候选池全不可用 → **显式告知主人**「本机无顶配审计模型，审计/批判深度将降级，是否继续」。
- **预算闸门（P0-1）**：派发 T6/T7 前查顶配模型余额，< $0.1 直接走候选池下一档并告知主人深度降级；同一项目已发现余额不足 → 后续顶配角色直接降级（不重复试错）。

### 能力分层原则（换模型时遵循，v2.3.12 能力抽象升级）

> **v2.3.12 P0-2 升级**：模型名不再硬编码，抽象为「**能力档 + 候选池**」——角色卡只写能力需求，具体模型由主控 Phase 0 自检后按本机可用模型池映射（候选池见 SKILL.md 模型分工表）。下表「候选池」= **本机默认映射示例（非硬编码）**，换系统/换模型按候选池自动适配。

按角色能力需求分层选模型，**避免「T7 审计用便宜快模型」「T1 检索用顶配」错配**：

| 能力档 | 角色 | 能力需求 | 候选池 | 错配后果 |
|---|---|---|---|---|
| 检索 | **T1/T2/T3** | 便宜快（抽取+分类） | 见 SKILL.md 模型分工表 | 用顶配 = 浪费 + 慢；用太弱 = 信息丢失 |
| 分析写作 | **T4/T5** | 强推理 | 见 SKILL.md 模型分工表 | 用 flash = 大纲粗糙 / 文章失味 |
| 批判审计 | **T6/T7** | **顶配防漏判** | 见 SKILL.md 模型分工表 | 用弱模型兑底 = 批判无刃 / 审计降级（v2.3.7 P0-1 教训） |
| 主控 | **T0** | 稳定路由 | 见 SKILL.md 模型分工表 | 主控崩 = 全流水线停 |

**核心原则**：
- **审计顶配不省**：T7 是最后一道防线，论文质量全压这——宁可其他地方慢
- **检索不奢侈**：T1/T2/T3 是机械化检索，便宜快模型够用
- **写手分场景**：内联引用 / 学术编号 = deepseek-v4-pro；公众号短文 = flash 够
- **本地 Ollama 可用**：涉敏 / 零外发场景用 gemma4:31b / qwen3-coder:30b

**见** `pipeline/operations.md` 成本与模型段（示例表与 fallback 链）

---

## 流水线全景

```
Phase 0 定题       主人确认主题/期刊模板/篇幅/配图意向（无/要图）+ **v2.5.0 可选项主控必呈现**（3 项 6 选项，教训 #172 + #173，主人实测反馈「没地方选」）→ 01-任务简报.md + status.md
Phase 1 并行检索   T1 文献检索员(文献卡A/B/C分级+信任级别已发布) ∥ T2 数据检索员(数据卡🟢🟡🔴+冲突并列+信任级别已发布/主人投喂/二手转引，v2.2.1 必填)
                ∥ **T3** 案例检索员(cases/案例卡.md+信任级别已发布/主人投喂/二手转引，**任何量级必 spawn**，含 0 条场景走空卡协议，v2.3.0 重命名原 T6→T3)
[🔓 **Phase 1.5 关键二手数据定向回查** v2.5.5 P1 新增，教训 #129 + #139落地]：T1/T2/T3 三方并行检索后、主控跑 T2.5 闸门前，主控按任务简报「待复核关键 Dxx」字段 spawn **T1b 定向回查子会话**（同一 subagent 类型 + 定向检索 prompt）。触发条件：① 任务简报标 [Dxx 需复核]；② T2 数据卡 ≥1 条标 🔴二手转引 且原始来源未回溯；③ T9 评分 ≤ 4维 ≥3 分。产出 literature/回查报告-v{N}.md → T2 据此更新 [Dxx]（替换原始来源链接 / 升信任级别）→ **M-Gate 重跑**（M-Form-6 + M-Exist-3 重核验）。预期收益：T9 证据强度评分从 3/5 升 4-5/5，主线数据信任级别从 🔴 升 🟢。**为什么不合并到 T1/T2**：三方并行阶段未定据点，单独子会话能针对复核需求精检索；实战 口腔 AI 综述 D12 FDA 44 款/D13 ADA 2-5% 二手转引未回溯是该阶段专属问题。
[🔒 T2.5 完整性门 v2.2.1 主控 checkpoint]：数据条目数≥任务简报需求数 + 信任级别完整 → 通过才派 **T4**（v2.3.0 改 T3→T4，不通过→触发 T2 重检索或主控补数据；v2.3.2 删主人签字，教训 #136）
Phase 2 分析       **T4** 分析员 → 分析大纲.md（论点-论据映射 + 反方论证 + v2.2 三角验证标注 + v2.4.6 建议图表，v2.3.0 改 T3→T4）
Phase 2.5 大纲确认 主人过目大纲 → 确认/修改 + 拍板建议图表（图位数量/类型/数据源）（人在环，改方向成本最低）；主控把关三角验证（[L]+[D]+[C]）
Phase 3 写作       **T5** 写手 → 初稿-v1.md（禁做清单 + 反方论证 + 字数预算；引用标[Lxx]/数字标[Dxx]/案例标[Cxx]；修订 v2/v3 显式覆盖同路径，v2.3.0 改 T4→T5）
Phase 3.5 洞察补充 主人过目初稿 v1 → 主控问主人洞要补（**v2.5.5 三具体问题选项**，教训 #158） → T5 写手 v2 融入（人在环，v2.1.3 教训 #46）
[🔒 **T6** 批判伙伴 v2.2.2 主控 spawn，v2.3.0 改 T8→T6，v2.3.0 引入 Phase 3.6 独立节点]：T5 写手 v2（已含主人洞察）后、T7 审计前，从反方攻击论证（C1-C7）；轻量档（≤3000 字）可跳过；产出 analysis/批判报告-vN.md
Phase 4 审计       **T7** 审计员 → 审计报告（G0覆盖度/G1分级核验/G2溯源/G2.5案例核验/G3逻辑/G4格式/G5规范/G11时效告警/G12信任级别一致性 v2.2.1 新增/G13 AI 使用披露 v2.2.2 新增/G14 中文 AI 痕迹检测 v2.4.0 新增，v2.3.0 改 T5→T7）
Phase 4.2 修订     写手交 修订说明 + 修订稿 → 审计员复核 → 最多 2 轮 → 仍不过升级主控（v2.2.0 ≤2轮硬约束+ Acknowledged Limitations 模式）
Phase 4.5 配图     仅当 Phase 2.5 拍板有图位：写手已在正文标 [图N] 图位 → 主控 `write` 手写 SVG 生成数据图表（数字与数据卡/[Cxx] 一致，参考 `图表-SVG-template.md`）
[🔒 **T9 同行评审** v2.4.0 新增（**v2.4.6 按模式默认开启**：行业分析/学术默认开启，公众号可选；**v2.5.0 期刊匹配助手**：学术模式默认输出 Top 3 推荐期刊 + 综合匹配度，详见 `_shared/期刊数据库.md` + `_shared/期刊匹配算法.md`）]：**v2.5.5 修订**—**只跑终稿阶段（Phase 5 后 + v3）**，不跑 v2。Phase 4.5 期间学术论文 spawn T9（v2 是中间态，不评审）。Phase 4.5 主控按「文章类型」判定默认——行业分析/学术论文默认开启（主人可关），公众号默认关闭（主人可选）。开启后 spawn T9 模拟期刊审稿人 6 维度评分（原创性/方法论/证据强度/论证结构/写作质量/引文规范）→ 输出 audits/审稿报告-vN.md（26-30 accept / 21-25 minor / 16-20 major / <16 reject）+ 期刊匹配 Top 3。

### T9 评分必汇报（v2.5.5 P1 新增，主人实测教训）

**T9 完成后主控必须在对话里向主人呈现评分**（不只写文件）。人对话中必须在 Phase 4.5 报告 T9 结果：

```
## T9 同行评审结果

**总评分**：XX / 30
**建议**：accept / minor / major / reject

**6 维度评分**：
- 原创性：X/5
- 方法论：X/5
- 证据强度：X/5
- 论证结构：X/5
- 写作质量：X/5
- 引文规范：X/5

**Top 3 期刊推荐**（学术模式）：
1. 《期刊名》（匹配度 X%，适合 XXX 方向）
2. 《期刊名》（匹配度 X%）
3. 《期刊名》（匹配度 X%）

报告文件路径：`audits/审稿报告-v{N}.md`

**主人决策**：
□ A. 接受定稿，进入 Phase 5 终检
□ B. 继续修订（T5 v3+ → T7 R2 → T9 再跑）
□ C. 其他调整
```

**为什么必汇报**：T9 评分是**给主人看的**（是否可投稿），不是给 T7 审计员的。主控如只在文件里写、不在对话中呈现 = 主人需手动开文件看 = 人在环节点失效（主人反馈：v2.5.5 口腔 AI 实战 T9 评分未在对话中呈现）。
[🔒 **G14 中文 AI 痕迹闸** v2.4.0 新增，**v2.5.5 时序澄清，教训 #165**]：**实际串行，不是并行**——T6 在 Phase 3.6 跑 v2 → G14 在 Phase 4.5 跑 v2 → T7 审计。原描述「与 T6 并行调用」是文档 bug。修正后路径：
- Phase 3.6：T6 攻击 v2（论证 C1-C7）
- Phase 4.5：G14 检测 v2（8 类 AI 痕迹）
- **真正可优化**（v2.6.0 候选）：G14 前移到 Phase 3.6，与 T6 同批 spawn = 真并行，但需重审 T6 攻击对象（现在 v2 没 G14 信号 = T6 可能误判 AI 味 = 仍待修）
- 0-2 类 Pass / 3-4 类 Warning 触发 T5 修订 1 轮 / 5+ 类 Fail 触发 T5 修订 2 轮 → 输出 audits/G14-检测报告-vN.md

### v2.5.5 AI 味 grep 自抓前移（教训 #164，主人《口腔 AI》反馈）

v2.4.0 G14 检测在 v2 阶段才跑 → 30 处破折号要等 v3 修订。**v2.5.5 提前到写手 v1 自抓**：

| 模式 | 阈值 | 超阈值动作 |
|------|------|----------|
| `grep -c "——"` 破折号 | ≤ 8 | 替换为「，」「：」「；」 |
| `grep -c "值得注意的是"` | ≤ 3 | 删除或换「**核心点**」 |
| `grep -c "首先.*其次.*最后"` | ≤ 1 组 | 替换为「**第一**」「**第二**」「**第三**」 |
| `grep -c "综上所述"` | ≤ 1 | 删除 |
| `grep -c "本章将"` | ≤ 1/章 | 替换为「**本章**」 |

详见写手卡 step 11.bis「AI 味 grep 自抓前移」。
[🔒 **T7.5** 完整性门 v2.2.1 主控 checkpoint，v2.3.0 改 T5.5→T7.5]：审计报告最新版 + P0/P1 清单 + M 门全 exit 0 + 论文交付物 vs 操作员报告隔离 → 通过才派 **T8**（v2.3.0 改 T7→T8，不通过→触发 T7 重审或主控补审；v2.3.3 删主人签字，教训 #138）
Phase 5 终检      **T8** 主控终检 → final/定稿.md + 图件/（如有）+ 证据包/(文献+数据+案例+审计) + 交付说明.md + M-Gate-Report-v2.2.1.json → 高频问题反哺报告（不自动 commit 角色卡 — v2.0.2，v2.3.0 改 T7→T8）→ **v2.5.0 多格式导出**：主人选 `--format md/latex/docx/pdf`（默认 md 零额外 token），按 `_shared/format-export.md` 跑 pandoc + rsvg-convert
```

**🔒 = v2.2.1 主控阶段闸门（教训 #77）**——T2.5 在 T2 → T4 间，T7.5 在 T7 → T8 间（v2.3.0 改 T3→T4/T5.5→T7.5/T7→T8）；不引入新 agent，主控用 `update_plan` + `read` 工具实现；**「不绕过交接直接派发」**。

### 图位编号规则（v2.5.5 P1 新增，教训 #157）

**图位编号 = 章节出现顺序，不是任意编号**：

1. Phase 2.5 拍板时，**图位标题先留空**，只拍板图位数量 N（如 N=3）
2. T5 写手按章节出现顺序编号：[图1] → [图2] → [图3]（**不允许断号**）
3. **不允许 [图1][图3][图4] 出现**——这是 bug，不是图位缺失
4. 主控 Phase 4.5 SVG 生成时：
   - 先 grep `\[图N\]` 全集，按章节顺序排列
   - 章节内出现顺序 = SVG 文件命名顺序
5. T7 审计员 P1 检查 `[图N：标题]` 出现次数 ≥ 拍板图位数量 + 编号连续

**实战背景**（v2.5.5 口腔 AI 项目）：v3 正文出现 [图1][图3][图4] 断号 = 缺编号规则约束。T5 写手按拍板数量拍，但编号任意。

### 先行者文献「本文差异化」段（v2.5.5 P2 新增，教训 #171 主人反馈）

**每条 [先xx] 文献后必加「本文差异化：」段**（主人在 Phase 0 拍板是否加每条 vs 仅主论点）：

```
## 先行者文献

[先05] 各律师事务所. 高净值人士非婚生子女 / 家族财富传承 / 王思聪私生子等实务分析[EB/OL]. 北京嘉浮/上海君悦/天达共和等, 2025-2026. https://javy-lawyers.com/news/dongtai/1803715749257166849.html

本文差异化：本文是各律师事务所 2025-2026 实务观点的延伸——将"实务对冲工具"延伸到"法理+历史+国际视野"的综合论证，差异在于证明"财产外流风险"有合法对冲工具（保险/信托/婚内财产协议/撤销赠与/必留份/举证倒置）且无须减损非婚生子女份额；加入日本 2013 司法级证伪与 CRC Art. 2 国际法视角。
```

**「本文差异化」段必含 3 要素**：
① **本文延伸** — 站在该先行者的哪个角度延展（不是重复）
② **差异在于** — 与该先行者的关键不同（视角/范围/数据/结论）
③ **新贡献** — 加了哪些先行者没有的内容（数据/反驳/国际视野/历史纵深/跨学科）

**为什么必加**（主人反馈）：v2.5.4 [先xx] 仅呈现原文 → 读者看不出本文增量价值 → 原创性保证仅抽象有、具体无。v2.5.5 后 T5 必加。

## status.md 状态机（主控维护，角色交接时更新）

每个任务状态：`Inbox → Assigned → In Progress → Review → Done | Failed`
- 每个角色交接时必须更新 `status.md` 对应行
- 失败要留原因；任一行停留超硬卡阈值无进展（按主控卡 §二十二「硬卡阈值表」：T1-T3 10 分钟 / T4 12 分钟 / T5 15 分钟 / T6-T7 12-15 分钟 / G14 8 分钟）→ 主控介入（v2.5.5 P0 硬性化，原统一 8 分钟硬卡已废弃）
- 模板：`templates/status-template-lite.md`（精简版实战用）/ `templates/status-template.md`（完整版调试用）

## 模板加载策略（v2.2.14 优化，节省 80%）

| 场景 | 加载版本 | 原因 |
|------|---------|------|
| 实战项目（Phase 1-5） | **`*-template-lite.md`** | 精简版只含骨架 + 必填字段 |
| 培训新人 / 调试模板 | **`*-template.md`** | 完整版含字段详解 + 实战案例 |

7 个模板精简版总大小 8.7KB（vs 完整版 42.6KB），**节省 80%**。主控实战派发时优先用精简版。详见 `templates/README-模板拆分方案.md`。

## 设计文档加载策略（v2.2.16 优化，节省 89%）

| 场景 | 加载版本 | 原因 |
|------|---------|------|
| 实战项目（Phase 0-5） | **`设计文档-架构.md`** | 架构篇只含团队架构 + 角色定义 + 工作流程 |
| 培训新人 / 哲学理解 | **`设计文档-哲学.md`** | 哲学篇含关键防坑 + 与单 AI 区别 |

原 32.9KB 设计文档拆分为架构版（3.6KB）+ 哲学版（1.8KB），**实战节省 89%**。主控实战派发时优先用架构版。详见 [`../PERFORMANCE-PROFILE.md`](../PERFORMANCE-PROFILE.md)。

## 派发话术（v2.5.6 拆分，按需加载）

> **v2.5.6 拆分**（第三方审查建议 #1 token 优化）：原 269 行派发话术段拆成 10 个独立文件 `references/dispatch/`，主控 spawn 哪个角色才读哪个，避免一次加载 23.6K tokens 的全部派发话术。
>
> **使用说明**：完整角色职责/铁律在 `references/agents/0X-*.md`（9 张卡），`references/dispatch/` 只列「主控复制即用」的最小派发话术。优先级：完整角色卡 > 派发话术。

### 派发话术索引（主控按需读）

| 角色 | 派发话术文件 | spawn 时机 |
|------|------------|-----------|
| T9 同行评审 | `references/dispatch/T9-同行评审.md` | Phase 4.5（可选）|
| G14 中文 AI 痕迹检测 | `references/dispatch/G14-中文AI痕迹检测器.md` | Phase 4.5（与 T6 并行）|
| T1 文献检索 | `references/dispatch/T1-文献检索.md` | Phase 1（并行①）|
| T2 数据检索 | `references/dispatch/T2-数据检索.md` | Phase 1（并行②）|
| T3 案例检索 | `references/dispatch/T3-案例检索.md` | Phase 1（并行③）|
| T4 分析 | `references/dispatch/T4-分析.md` | Phase 2 |
| T5 写手 | `references/dispatch/T5-写手.md` | Phase 3 |
| T6 批判 | `references/dispatch/T6-批判.md` | Phase 3.6 |
| T7 审计 | `references/dispatch/T7-审计.md` | Phase 4 |
| T8 终检 | `references/dispatch/T8-终检.md` | Phase 5（主控亲完成）|

> **主控 spawn 前必读对应派发话术**（教训 #57：不要凭记忆复制，读 dispatch/ 最新版）。spawn 哪角色 → 读哪文件，**不需要一次读完 10 个**。
