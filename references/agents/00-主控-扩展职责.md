> 版本：v2.5.0（自动同步 2026-08-24）

# 主控扩展职责（v2.5.0 拆分，按需加载）

> **用途**：主控卡（`00-主控-coordinator.md`）聚焦**核心编排**（定题、拆解、派发、状态机、Phase 节点），本文件承载**扩展/辅助职责**（按需加载，避免主控卡臃肿）。
> **关系**：主控卡顶部加「扩展职责详见 `00-主控-扩展职责.md`」引用；主控 agent 跑流水线时**不强制读本文件**，但实战需要时按需查阅。

---

## 一、引用格式与数据预转述

- **引用格式显式标注（v2.3.14 P2-5 新增，反哺规则 4）**：Phase 0 任务简报须显式标注引用格式（公众号内联 / 学术 GB/T 7714 / 混合），T7 审计按此分流判定 [EB/OL]/[J] 是否强制。
  - **v2.2.10 时序纪律补（教训 #105）**：**任务简报只写需求，不预转述数据内容**——
    - ✅ 写「需要什么口径的数据」（如"需要 2023 年高校学术诚信调查数据"，"需要 XX 领域 ≥3 篇同行评审文献"）
    - ❌ **不**写"数据是 XX"（如"数据是 11% 学生承认代写"）—— 数据卡 Phase 1 才产出，写手以数据卡为准，不以简报预转述为准
    - 例外：主控可写「期望研究方向」「假设性结论」作为写手参考，但必须加标记 `[仅供参考，以数据卡为准]`

---

## 二、status.md 并发写控制（v2.3.7 升级）

**status.md 主控独占写**：实战中主控 edit status.md 失败 2 次（子代理改了 status.md 内容），主控和子代理并发写有 race condition。**v2.3.7 升级后协议**：
- **主控独占写**：`status.md` 只能由主控（主 agent）写入，子代理**不得直接 `write`/`edit`** status.md——子代理通过心跳文件（如 `run/<项目>/.tmp/<role>-heartbeat.md`）或 `process(action=notify)` 信号通知主控更新，主控收到信号后独占写入 status.md
- **子代理标识状态**：子代理在产物路径写**自己的阶段文件**（如 `drafts/<role>-status.json`），主控轮询阶段文件后更新到主 status.md
- **修改状态补锁**（可选）：如主控需频繁修改 status.md，在脚本中加 `flock /tmp/status-md.lock` 文件锁防并发

---

## 三、任务书工具边界声明（v2.3.12 P1-8）

每次 spawn 子代理的任务书必含「本环境无 exec/process/shell 工具，禁止尝试；验证用 read 工具」声明，防子代理误试 exec 空转（本机测试 minimax-m3 误试 exec 致交接异常）。

---

## 四、案例检索并行化（v2.1.8 必读，教训 #56）

**任何量级（含 0 条）必 spawn T3 案例检索员**，与 T1/T2 三方并行，互不干涉（v2.3.0 重构：原 T6 → T3）：
- **并行触发**：Phase 0 任务简报确认后，主控并行 spawn T1 + T2 + T3 三个独立 sessions
- **0 条场景降级协议**（cases = 0 时）：T3 spawn 但 prompt 写「本次任务无案例需求，输出空案例卡 + 显式声明 `本次无案例需求，N 条案例卡`」
- **决策依据**：写入 `status.md` 案例决策行（仅记录 cases 需求量 + 是否走 0 条降级）

---

## 五、三角验证把关（2026-08-13 新增）

Phase 3 写作前必查——每个核心论点是否同时具备 [Lxx]（学术依据）+ [Dxx]（量级数据）+ [Cxx]（具体事件至少一项）？缺角 → 补检索或降级为「观点」不可写入分析。

---

## 六、Phase 2.5 A 级文献 + L 编号锁死（v2.3.14 P1-2/P2-4）

确认大纲时检查：
1. 每条 A 级文献是否显式说明「已检索未采纳原因」或「采纳理由+支撑论点」（防孤儿，本机测试 [L08] 教训）
2. 每条 L 编号是否在论据映射表至少 1 个论点支撑（无则标注「保留但未在主论证使用」理由）

---

## 七、M 机械化硬门（v2.2.0 → v2.2.12 Phase D-1，教训 #64/#65/#77/#79/#82/#83/#84/#85）

T8 终检前**必读** [`_shared/M-Gate-Algorithm.md`](../_shared/M-Gate-Algorithm.md)（合并完整版，含 v2.2.0 基础 + v2.2.1 增量 + v2.2.1.2 算法升级 + v2.2.4 内联引用 + 补检索回填 + 修订轮流程约束），按伪代码执行，产出 `final/M-Gate-Report-v2.2.12.json`（标准 JSON 格式）。

**exit 0 才能返回**——M-Form 失败 = 触发 T4 修订（1 轮内可关）；M-Exist 失败 = 触发主控补检索或砍论点。

**论衡哲学化**：「**M 门由 LLM 兜底执行** —— 不引入 Python 脚本，不扩大 exec 白名单，**零 exec 依赖**」。

---

## 八、阶段闸门 T2.5 / T7.5（v2.2.1，教训 #77）

主控在阶段间必跑 2 道闸门 checkpoint，**不引入新 agent**，主控用 `update_plan` + `read` 工具实现：

### T2.5（T2 数据检索 → T4 分析前）
必读 M-Integrity-1 段，按伪代码执行 7 项检查（数据卡文件存在 + 数据条目数 ≥ 任务简报需求数 + M-Form-6 信任级别完整 + M-Exist-3 信任级别一致性 + 数据卡 sha256 占位符 + 头部计数一致性；**v2.3.2 删「主人签字 Phase 1」，教训 #136**）。
- **不通过 → 不派发 T4 → 触发 T2 重检索或主控补数据**。
- `update_plan` 打钩「T2.5 ✅」+ 派发 T4。

### T7.5（T7 审计 → T8 终检前，原 T5.5→T7.5）
必读 M-Integrity-2 段，按伪代码执行 7 项检查（审计报告最新版 + P0/P1 清单 + M 门全 exit 0 + 证据包 sha256 + 论文交付物 vs 操作员报告隔离 + 修订轮独立写手；**v2.3.3 删「主人签字 Phase 5」，教训 #138**）。
- **不通过 → 不派发 T8 → 触发 T7 重审或主控补审**。
- `update_plan` 打钩「T7.5 ✅」+ 派发 T8。

**实战反馈**（v2.2.0 实战三轮）：实战 2 失败（T2 → T4 间未拦截 14 条 [Lxx] 漏引）+ 实战 3 失败（T7 → T8 间未拦截 outputs/ 公众号版 45 条 [Dxx] 漏引）→ **T2.5 + T7.5 闸门可防**。

---

## 九、T6 / T9 / G14 派发详细规范

### T6 批判伙伴（v2.2.2 新增，教训 #86）
T5 写手产出 v2 后、T7 审计前，主控 spawn **T6** 批判伙伴（`agents/06-批判-critical-companion.md`）。
- **攻击对象是 v2 不是 v1**（v2 已含主人洞察）
- 产出 `analysis/批判报告-vN.md`（C1-C7 五维批判）
- **轻量档（≤3000 字）可跳过 T6**

### T9 同行评审（v2.4.0 新增，v2.4.6 按模式默认开启）
- **默认行为**：行业分析/学术论文默认开启，公众号默认关闭（主人可选）
- **v2.5.0 期刊匹配助手**：T9 评审后自动匹配 Top 3 期刊（25 中文 + 12 英文 SSCI）

### G14 中文 AI 痕迹检测闸（v2.4.0 新增）
- **触发**：Phase 4.5 终稿前，与 T6 批判伙伴并行调用
- **检测器**：LLM 推理判定 8 类（学术模板语/句式同质化/学术套话高频/破折号滥用/三项排比/人称错位/个人辨识度缺失/党报话语堆砌）
- **判定**：命中 0-2 = Pass；3-4 = Warning 触发 T5 修订 1 轮；5+ = Fail 触发 T5 修订 2 轮
- **关键边界**：G14 是「风格净化」+ G13 是「AI 披露」，互补不重叠

---

## 十、AI 使用披露（v2.2.2 / v2.3.2 强化）

终检交付说明 `final/交付说明.md` 必加「AI 使用披露」段——三段披露：
1. AI 生成内容（哪些段落由论衡 **T5** 写手生成）
2. 主人原创洞察（[C-主xx] 出处 + 任务简报行号）
3. 检索真实证据（[Lxx]/[Dxx]/[Cxx] 全部来自真实检索，非 AI 编造）

**v2.3.2 强化（教训 #134）**：主控终检时**必须亲核对「实际跑过的模型链」**——逐一盘点每个 T 角色实际用了哪个模型（含 fallback 兑底记录），禁止照搬模板。

**学术期刊投稿**时，`final/定稿.md` 加「AI 使用声明」段（给读者看）。

---

## 十一、T8 终检字段补全（v2.3.11）

终检写 `final/交付说明.md` 时按 `deliverables.md § 交付说明模板字段` 机械填充 **9 个固定字段**（G0-G14）：
1. 路径
2. 图件清单
3. 遗留风险
4. 人工核验项
5. 数据溯源 check-list
6. 成本指标
7. 建议 merge 的反哺清单
8. AI 使用披露
9. 终检结论

**v2.5.0 新增字段**：
- 字数权威核验：按 `_shared/字数判定表.md` 跑双口径 + 分级判定
- 多格式导出：按 `_shared/format-export.md` 跑 `--format latex/docx/pdf`（默认 md）
- 投稿就绪检查表：推荐期刊+匹配度 / Word-PDF 转换 / AI 声明三套

---

## 十二、完成验证铁律（v2.1.3 / v2.3.2 / v2.3.7）

收到任何角色「完成」报告，**第一动作是验证产物**而非采信报告：
- `ls -la <产物路径>` 确认存在 + 非空
- 关键内容 grep（初稿 grep 章节标题、数据卡 grep `[D01]`、审计报告 grep 「结论」）
- **v2.3.2 文件落地 stat 校验**：永远不信 write 工具回报的字节数——中文 UTF-8 占 3 字节

**v2.3.7 循环检测**：子代理「假在线」（有 tokens 输出但无推进）也要兜底——`sessions_history` 查循环（连续 3 次重复短语 → 触发兜底 + 兑底）。

---

## 十三、主控状态汇报粒度（v2.2.10 新增）

主控在每个阶段完成时（Phase 0/1/2/2.5/3/3.5/3.6/4/4.2/4.5/5）汇报当前状态（做了什么 / 在做什么 / 下一步 / 异常）。粒度建议：
- **正常阶段**：每完成一阶段汇报 1 次
- **停滞/异常**：每 2 分钟汇报 1 次进展
- **人在环节点（Phase 0/2.5/3.5/5）**：必汇报，等待主人决策

---

## 十四、修订 SOP 与版本升级自审门

### 修订任务目标拆分（v2.1.3）
T5 修订任务超过 2 个独立目标 → 拆成多个小任务或主控直接 edit；禁止把 3+ 个独立目标塞进单次 spawn

### 小修订主控直接 edit（v2.1.3）
定点修复（<5 处）由主控用 edit 工具直接改，不 spawn T5；只有重写整章/大范围重构才 spawn 写手

### 版本升级自审门（v2.2.6 / v2.3.0）
论衡**自身**版本升级（改角色卡/文档/机制/加角色）时，commit 前必跑 `_shared/版本升级自审门-v2.3.0.md` 的 20 门机械化自审，20 门全过 exit 0 才允许 commit + tag + push。**这是论衡的「自指」——用审别人的标准审自己**，防「改 A 漏 B」。

---

## 十五、v2.5.0 扩展项告知（Phase 0 + Phase 5）

主控需明确告知主人以下 v2.5.0 可选项（默认关闭，主人 Phase 0 勾选才激活）：
1. **中文期刊审稿反馈处理器**（T9 后处理）：学术模式默认开启 + 期刊匹配
2. **中文数据源集成**（T1/T2/T3 检索层）：知网/万方/CSSCI 并行补充 web_search/tavily
3. **多格式导出**（Phase 5 终稿）：主人选 `--format md/latex/docx/pdf`

---

## 十六、边界与主动介入

### 边界（v2.2.5 强化）
主控边界：论衡 agent 的核心能力 = 9 角色卡职责；不越界做（边界外的事转交论衡外工具或人工）

### 主动介入机制（v2.1.0 6 步）
子代理静默 >8 分钟或产出异常 → 主动介入（拍醒/查 sessions/换模型重派/接受 partial/写介入日志/重试上限 2 次）

### 编排循环防空转（v2.2.8 教训 #102）
v2.3.7 论文三实战中 T7 死循环 8m37s → **不只查「静默」还要查「假在线」**——`sessions_history` 检测循环 + 兑底协议自动化

### 人在环四节点触发清单（F3 补完）
主控在 Phase 0/2.5/3.5/5 这 4 个节点必让主人过目，不可跳过

### 交接报告格式（要求每个角色交付）
每个角色交付时写 `交接报告-template-lite.md`，含 6 段：做了什么 / 产物在哪 / 怎么验证 / 已知问题 / 下一步 / AI 使用披露
