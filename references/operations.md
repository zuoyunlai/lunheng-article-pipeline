> 版本：v2.12.24（自动同步 2026-09-11）

> 🌐 **语言政策**：产出语言默认中文，Phase 0 可改 English / 中英混 / 其他（写入任务简报「目标语言」字段，全流程以该字段为准）；中文特化（G14 中文 AI 痕迹检测 / GB/T 7714-2015 引用规范）是设计定位，不构成使用者语种限制。

# 论衡配图 + 写作禁做清单 + 成本模型

> **背景**：v2.2.8 Phase D 优化。SKILL.md 中配图生成规范 + 写作禁做清单 + 成本与模型建议三段（共 32 行）抽到本文件，SKILL.md 改为索引形式。

## 配图生成规范（Phase 4.4）

> **术语澄清**：「零 exec」= 不执行 shell 命令（`exec` 工具禁用），是**执行能力边界**；「零外发」= 不向外部发送数据，是**数据传输边界**。两者**不是一回事**：数据图表 SVG 两者都满足（本地 write，零外发零 exec）；**封面（image_generate）会外发**（发送图像生成 prompt 到宿主配置的外部图像服务；vendor 路由 / 失败降级为宿主级配置行为，论衡不规定多 vendor 链）——勿将「零 exec」误解为「不向外部发送任何数据」。

- **数据图表**：主控用 `write` 工具手写 SVG 矢量图（参考 [`templates/图表-SVG-template.md`](templates/图表-SVG-template.md)），**禁止文生图**（数字不可控）。图位来自写手标注的 [图N]，图上数字必须来自数据卡，与正文 [Dxx]/案例卡 [Cxx] 一致，生成后抽查核对。SVG 是文本，主控 LLM 本地 `write` 生成——**零外发、零 exec**（不执行 shell、不调用外部服务，纯本地写文件）。
- **封面视觉**（**默认关闭**）：**仅当主人在 Phase 0 勾选「启用封面生成」后**，主控才调用一次 image_generate 工具——图像 provider 与失败降级均为**宿主 OpenClaw 配置决定**（论衡文档不规定 vendor 路由），prompt 严格遵循主人「极简自然」调性 + 主题关键词 + 文字排版位置预留。**发送内容 = 图像 prompt（含主题/品牌关键词，可能含项目上下文）**；主控必须在调用前向主人说明这一外发范围并取得同意（防未授权调用 + 成本/数据外发）。出图先送主人确认风格再定稿。宿主图像 provider 不可用 / 主人不同意调用时：主控**不自行跨 vendor 降级**，改走内置备选——**SVG 矢量封面**（本地 write 程序化生成，零外发）或主人人工上传
- **中文字体**：SVG 文本元素用 `font-family` 指定 CJK 字体栈（如 Noto Sans CJK SC），避免目标渲染环境缺字体；不使用 matplotlib 等需 exec 的工具链（数据图表 = 主控 write 手写 SVG）
- **图件入 final/图件/**，交付说明列清单；数值标签与来源注释避免同侧重叠（来源统一放底部）
- **案例示意图**（可选）：复杂事件链可用 SVG 画时间线/关系图，不画文生图

## 写作禁做清单（AI 去味 10 项，审计员逐项查）

1. 排比堆砌（连续 3 组以上同构排比 → 拆掉）
2. 金句密度过高（每千字不超过 1 句"点睛句"）
3. 无第一人称（要有"我认为/本文认为"的主体声音）
4. 缺具体细节（论证必须有具体案例/数字/场景支撑）
5. 过度防御性表述（"值得注意的是""不可否认"滥用）
6. 抽象概念密度过高（一段超 2 个抽象大词 → 拆解+举例）
7. 段落结构过度对称（避免每段"总-分-总"机械对称）
8. 学术过渡词过多（"综上所述""进而""由此可见"克制使用）
9. 表格过多（非必要不列表）
10. 缺研究者个人观察（正文要有自己的判断和发现）

## 成本与模型建议（模型可自由更换）

> 下表模型名为示例，非锁定。任何 OpenClaw 兼容模型都可替换，更换途径见 `references/pipeline-readme.md` 的「模型配置与更换指南」段。**v2.3.12 P0-2 起模型名抽象为「能力档 + 候选池」**，角色卡只写能力需求，具体模型由主控 Phase 0 自检后按本机可用模型池映射。

| 能力档 | 角色 | 候选池 | 理由 |
|------|------|------|------|
| 检索 | 文献/数据/案例检索 | 见 [`_shared/模型候选池.md`](_shared/模型候选池.md) | 机械检索，最便宜可靠 |
| 分析写作 | 分析/写作 | 见 [`_shared/模型候选池.md`](_shared/模型候选池.md) | 推理与成文质量 |
| 批判审计 | 批判/审计 | 见 [`_shared/模型候选池.md`](_shared/模型候选池.md) | 专抓错，必须顶配 |
| 主控 | 主控 | 见 [`_shared/模型候选池.md`](_shared/模型候选池.md) | 判断与终检 |
